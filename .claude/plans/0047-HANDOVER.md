# Handover — plans 0047 (schedule-failure diagnosability) and 0046 B/C/D (service account), 2026-09-16

You are taking over mid-flight. **Read in this order:** (1) this file; (2) `CLAUDE.md` (authoritative, loaded every session); (3) `.claude/plans/0047-schedule-failure-diagnosability.md` — its **Spec** is APPROVED and is what you build; its **Review** section records three adversarial gate rounds; (4) `.claude/plans/0046-HANDOVER.md` §1 (gotchas), §5–§6 (verified facts), §12; (5) `.claude/plans/0046-service-account-scheduled-task.md` — its **Revision (2026-09-16)** block at the top supersedes the older text below it.

## Owner instructions (verbatim intent, 2026-09-16)
- "yes approved" → plan 0047 is approved. Build it: **Slice A1 (engine), then Slice A2 (classifier + docs)**.
- "do and complete all and we can release" → after 0047, do **0046 B → C → D** (service-account username + password, signal honesty, docs). **ONE release at the end; no patch release in between.** SD54's ask is 0046-B.
- "use Fable conservatively — use Opus and Sonnet" → delegate: implementers/reviewers/gates on `opus`, mappers/digests on `sonnet`; never read a large workflow output yourself — digest it via a Sonnet agent (see the memory note `workflow-result-digests`).
- Process is `docs/claugentic-WORKFLOW.md`: for 0046 B/C/D the deep spec is written JIT per slice → adversarial review → the owner approves each slice's plain-English block (keep those to ~6 lines — the owner is fatigued by long messages). **Never write code before a spec is approved.**
- **Land gate:** a slice is landed only when CI's own `test  pass  <duration>` line has been READ AND QUOTED (`gh pr checks <N> --watch` in the background, then read the file; report `${PIPESTATUS[0]}`; `--fail-level` is a false green). **Never merge** — the owner merges. Push the branch and open the PR; that is expected.
- Never send email from the owner's account; never run anything needing the owner's UAC; never put an email address in any repo file (`scripts/check_no_emails.py` is a CI gate — first names + SD numbers only).
- Hard constraints: gMSA / SYSTEM / machine-scope secrets are OUT; Slice 1's refusal of a passwordless foreign account is CORRECT; `validate_run_as_user` is not widened; nothing may claim SD60's failure is fixable in-app.

## State of the repo
- Branch **`claude/0046-schedule-failure-diagnosability`** off `origin/main` (`8e1f24e`), in worktree `.claude/worktrees/unity-christian-mapping-edaf9b`. Commits so far are plan files only (`1340367` … `8686c29`). **No source code has been written yet.**
- 0046 Slice 1 (engine refusal) is on `main` (PR #117). PR #116 is another person's work — OFF LIMITS.
- The venv python is `C:/Users/shan.peiris/Documents/Integrations/DistrictSync/.venv/Scripts/python.exe` (no `.venv` in the worktree; no `make` on the machine — copy the inline command out of the `Makefile` for `validate-config`). Full suite ≈ 15 min (background it; a 0-byte output file means running). CI ≈ 18 min.
- Any local app/CLI run needs `DISTRICTSYNC_DATA_DIR` set (else it writes into the real profile). Pipeline output must go to a SHORT temp dir (the scratchpad path breaks Windows MAX_PATH — a 262-char `tool-results` path also failed to open).
- `.gitattributes` enforces LF: write bytes or normalise; check with `grep -q $'\r'`.
- `bandit -r src/ -q -c pyproject.toml` (the `-c` is required). Tree gate: a new `src/**/*.py` needs a line in `docs/claugentic-ARCHITECTURE_TREE.md` or the commit is aborted.

## District evidence (all read from the owner's mail thread; no addresses anywhere)
- **SD60 (Jarrod)** — CONFIRMED `0x80070520` (`ERROR_NO_SUCH_LOGON_SESSION`), six times in his pasted `etl_tool.log`, on the DIRECT path after running the app as administrator; his three elevated attempts logged nothing (defect A4 live). Windows' description was EMPTY, so the code reached the message only via the hex fallback. Cause: the policy "Network access: Do not allow storage of passwords and credentials for network authentication" (`DisableDomainCreds`=1) — Strongly implied, not yet confirmed by the district. **Nothing in-app fixes it, and 0046-B's service account hits the same policy.** Options for them: IT lifts the policy for that server · logged-on-only schedule · manual runs from Convert. The owner has a suggested reply in the session transcript; they send it, not you. Screenshots were id-only attachments and could not be fetched; the diagnosis does not depend on them.
- **SD51 (Kim's district; the admin typed a different admin account's credentials at the UAC prompt)** — the elevated child ran as that account, could not read the owner-only-DACL'd request file, and reported "The elevated request was missing" + "try again in a moment". Folded into 0047 as **defect A8** (a `PermissionError` → `DIFFERENT_ACCOUNT_SENTINEL` rung in `elevated_apply._apply`, before the generic `OSError`; the same leg in `delete_task_elevated`; `_MSG_DIFFERENT_ACCOUNT`'s copy names the cause). Advice for them now: sign in with an account that is a local administrator and run DistrictSync from it.
- **SD54 (Ted)** — wants a service account (username + password) — that is 0046-B.

## What to do next, in order
1. **Implement 0047 Slice A1** exactly per the plan's Spec A1 (engine: `src/scheduler/messages.py` NEW, `task_com.py`, `windows.py`, `elevated_apply.py`, `__init__.py:141`, `schedule_status.py` import, tree/INVARIANTS/`CLAUDE.md` row, five test files; RED-FIRST, then perturb each guard and watch it go red — list the perturbations in the PR). Use `claugentic-dev-harness:implementer` on `opus`. Run all local gates. Then Stage 7 Verify: fan out `lens-reviewer` (security · observability-ops · testing · reliability-resilience · api-and-contracts · maintainability-structure · docs-traceability) + `yagni-sentinel` + `honesty-reviewer` (copy) → `synthesizer-gate` verdict; fix CHANGES_REQUIRED; then push, open the PR (body ends with the attribution line from the system reminder), watch CI, quote the pass line, report "land gate met". Owner merges.
2. **Implement 0047 Slice A2** on a branch off A1's (PR base = A1's branch until A1 merges, then retarget to `main`). Same verify + land gate. Load the `districtsync-design` skill (it touches `components.py`).
3. **0046-B** (service account): write the JIT spec from plan 0046's Revision block + Spec "Slice 2" + the handover's §11 carried items; adversarial review; owner approves the ~6-line plain-English block; implement; verify; land. Open owner decisions to ask in ONE line each: A10 delete-then-create vs in-place (spike showed in-place replaces the whole definition); two UAC-click measurements (a mistyped account's HRESULT; S0 keyring read under batch logon).
4. **0046-C** (signal honesty: `_is_contradiction` / `_is_missed_run` quiet under a foreign principal; `run_result_verdict` over `last_result`; A9 note), then **0046-D** (docs: extend `docs/partner/headless-sftp-setup.md`; DECISIONS; ROADMAP). Then the release (`docs/developer/release.md`; verify the tag lands on the release commit — the v3.18.1 tag was mis-tagged once).
5. **Plan 0048 (auth-workflow robustness) is NOT approved yet.** A digest of the 18-agent analysis is at the session scratchpad `auth_workflow_digest.md` (may be gone in a new session) — the YAGNI minimum is three slices (engine truth · honest promises + copy · a `windows-latest` CI leg, since CI runs tests on Ubuntu only). Ask the owner one yes/no before planning it.

## Memory notes worth reading (auto-memory dir)
`running-local-gates`, `districtsync-land-gate`, `workflow-result-digests`, `sd60-schedule-error-request-sent`, `model-usage-preference`, `pipeline-output-short-path`.
