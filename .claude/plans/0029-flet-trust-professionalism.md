# 0029 — Flet UI trust & professionalism redesign

- **Status:** Approved — Implementing (user approved 2026-07-08; build started)
- **Resumable from:** Slice 0 (product/decisions persistence) + Slice 1 (config freshness) in flight
- **Blockers:** none
- **Flags:** `backfill CUT (panel consensus: source log is 98.6% test pollution) — existing pre-update history will not appear in the new store; honest fresh-start copy — ACCEPTED by user at approval` · `Slice 11 APPROVED IN-SCOPE by user (pre-adoption window): cross-platform app-data relocation, industry-standard per-OS paths`
- **Disposition at close:** per template
- **Roadmap item:** docs/claugentic-ROADMAP.md → "0029 Flet UI trust & professionalism redesign"
- **References:** `docs/claugentic-ARCHITECTURE_TREE.md` · `docs/claugentic-DECISIONS.md` · plans 0012/0013 (git history) · investigation wf_84cdd881-3a2 (46-agent verified findings) · advisory panel wf_43f08384-433 (10 advisors, 98 items — incorporated below)

## Problem

Shan's 2026-07-08 field test of the released v3.4.0 Flet UI surfaced ~12 verified root-cause defects plus a product-level gap: the UI was ported for feature parity (Plan 0013) and repeatedly **asserts state it never verified**. DistrictSync is a trust instrument — a district admin must believe the nightly sync is working without watching it. All claims below carry file:line evidence and were independently adversarially verified:

1. **Stale AppConfig snapshot** — `shell.py:170` loads `AppConfig` once at startup and binds the instance into Home/Run History/Mapping/Help via `functools.partial`; screens rebuild per navigation but from the frozen snapshot. District switch never propagates until restart; Mapping's own "current mapping" card stays stale (`mapping.py:124` vs `_on_apply` at `mapping.py:165` which loads a *separate* fresh instance); the Apply gate (`mapping.py:145-147`) compares against the stale value so a switch cannot be reverted without restart; Home's onboarding branch (`home.py:143`) never leaves "not set up yet" after in-session setup. Setup/Convert already load fresh (`setup.py:94`, `convert.py:147`) — the pipeline always used the right config; display/trust defect, not data corruption.
2. **Run history store is not production-grade** — Run History parses `__DISTRICTSYNC_RUN__` lines from the rotating diagnostic log (`run_log.py:58` reads only the live `etl_tool.log`; 5MB×3 rotation silently erases history; RotatingFileHandler is not multi-process-safe against the scheduled CLI writer on Windows). **pytest pollutes the real user log**: `src/main.py:47` runs `get_logger` at import time and `tests/conftest.py` has no path isolation — 365 of the 370 records in Shan's live log are test fixtures. Free-text `error=str(e)` (paths/columns) is persisted (`pipeline.py:244`). Manual Convert runs never appear at all (`convert_job` bypasses `run_pipeline`/`_emit_run_log` by design — `convert.py:102`).
3. **Schedule state divergence** — `setup.py:303` persists `schedule_time` + `schedule_registered` **before** `register_task` runs and never reverts on failure; status everywhere (`home_status.py:257`, `nav.py:74`, Run History footer) trusts the config boolean and never reconciles with the real task (`query_task` is dead code that also omits `/V` — `windows.py:544-565`). Live proof: config says "scheduled 15:36" while the task was externally deleted (Event 141). "Save setup" (`setup.py:133`) silently ignores the schedule section. No unregister affordance (`delete_task` has zero callers). Task action path is pinned to wherever the exe ran from (e.g. `Downloads\`) with no warning — and when the pinned exe is gone, the task fires and fails **without ever writing a run record** (a blind spot only the task's own LastTaskResult can see).
4. **Elevation dead-end** — unattended registration (Password logon + `RunLevel Highest`, `windows.py:260-266`) genuinely requires an elevated caller; the only affordance is "quit and re-open as administrator" (`setup_errors.py:57-61`).
5. **SFTP test trust gaps** — the test is real (live paramiko auth verified) but: it **writes a typed password to the keyring before testing** (`setup.py:587-588`) so a typo'd password clobbers a working credential even when the test fails; it tests live unsaved form fields (`setup.py:471-477`) while the nightly uses saved config (`pipeline.py:447-451`); success copy (`setup.py:628-629`) never names host, user, or credential source.
6. **Nav model** — state-dependent destination reordering (`nav.py:100-114`) destroyed spatial memory and read as instability; programmatic navigation never moves the rail highlight (`shell.py:215-217`; `build_nav` discards the rail reference, `shell.py:14-15`), desyncing all three `on_navigate` call sites.
7. **Silent district defaults** — `AppConfig.sis_type` defaults to `"myedbc"` (`app_config.py:30`), pre-selected in Setup (`setup.py:170`); Convert falls back to `configs[0]` alphabetically (`convert.py:253`).
8. **Convert output dead-end** — success view (`convert.py:396-422`) never shows where files were written; the PII "never render a raw path" rule (`convert_result.py:15-20`) is over-applied to a local, app-owned config path. Empty `output_dir` silently falls back to the *input* dir (`convert.py:147`).
9. **Chrome/input craft** — Exit is a no-op: Flet 0.85.3 `Window.destroy()`/`close()` are `async def` (`flet/controls/core/window.py:355/367`); `shell.py:223`, `shell.py:243` (latent), `launcher.py:104` call them synchronously. No `page.window.icon`, no brand `.ico`, no `--icon` on either `flet pack` (Makefile:37-76, `.github/workflows/flet-pack.yml`). No `TextField` wires `on_submit`.
10. **Onboarding triple-door** — while unconfigured: Setup-led nav order + a Setup tab + a Home onboarding hero all point at one job.

## Goals / Non-goals

- **Goal (product bar):** the UI avoids asserting states it can't confirm; **every success names WHAT it checked and WHEN** (e.g. "connection to <host> tested just now", "task is registered — next run tonight at HH:MM") rather than promising future outcomes; "where am I" and "where are my files" always answerable; setup ends in an honest, checked finish line; run history is a clean, district-scoped, durable ledger; professional craft baseline (brand icon, Enter works, Exit works, stable nav).
- **Goal:** first-run Setup is a stepped wizard (Folders → District → Schedule → Delivery → checked summary) that graduates into a flat Settings surface (user-approved direction). Schedule and Delivery steps are **skippable** ("set up later") so the aha moment isn't gated on an admin password + live SFTP credential being at hand.
- **Goal:** schedule registration self-elevates per-operation via a normal UAC prompt (user-approved); the app stays non-admin.
- **Goal:** dedicated run store (SQLite, per-user app-data dir) with hard test isolation, replacing log-parsing; ScheduledTasks read-back as the primary schedule signal **when readable**, with an honest "can't confirm right now" state when not.
- **Non-goal:** YAML mapping editor (ROADMAP); Linux/macOS elevation parity; any change to ETL transform semantics or output CSV contracts (SD74 snapshot byte-identical); multi-user/server features; auto-update; push alerting on failed/missed nightly runs (→ ROADMAP — the panel's premise-challenge: a pull cockpit can't alert an admin who doesn't open it; genuinely valuable, separate egress/PII surface); visual design-system redesign (tokens/theme/components stay).

## Approach

**One combined redesign program** (user-chosen). Trust-critical logic continues the established split: pure COUNTED modules (tested) + thin view glue (coverage-omitted). Design decisions (2c-revised after the 10-advisor panel):

- **D1 — Config freshness via load-per-build.** Screens rebuild per navigation; bind `AppConfig.load` (a supplier) instead of a loaded instance — the pattern Setup/Convert already use. Reactive store rejected (YAGNI). Mapping's Apply writes through + re-renders its own surface; its gate compares against the *persisted* current value. Freshness on navigation only; a manual **Refresh** affordance on Home/Run History covers the Watcher who leaves the app open overnight (cheap; re-runs the surface build).
- **D2 — Run store = SQLite (`history.db`) in `paths.user_data_dir()`.** Written via one shared `write_run_record()` seam called from **both** `run_pipeline` and `convert_job` (the manual path must produce rows too). Hybrid schema: promoted typed columns for filtering (`timestamp` TEXT naive-local-ISO (matches existing records), `sis_type`, `source` CHECK-constrained ∈ {manual, scheduled, cli, unknown}, `status` CHECK-constrained, `error_category` bounded enum, `schema_version` INTEGER) + one `record` JSON column holding the **exact flat record dict the derivation modules already consume** — so `home_status`/`run_history` need zero shape changes and exactly one reader exists. Composite index `(sis_type, timestamp)`. `PRAGMA user_version = 1`; **no migration framework now** (YAGNI) — additive-only schema rule recorded in DECISIONS, all statements name columns explicitly (no `SELECT *`/positional INSERT), and a writer seeing `user_version` higher than it knows **does not migrate/downgrade** (two exe versions share this DB: the pinned scheduled exe + an updated UI). WAL + `busy_timeout=5000` (WAL = readers concurrent with a writer, writers serialized — busy_timeout makes the rare UI-write-during-nightly-write wait, not error); DELETE-journal fallback if WAL unavailable. Open-use-close per operation (no shared connection; UI reads on UI thread, Convert writes on the worker thread). Reader contract mapped explicitly: missing DB → `[]` (read path never creates the DB), 0 rows → `[]`, any `sqlite3.Error`/`OSError` → `None`. Write path is the only creator/migrator; on "database disk image is malformed" it quarantines (`history.corrupt-<ts>.db`) and recreates, logging the event (a torn write must not brick the ledger forever). **No compaction** (≈1.8k rows/~400KB per year — YAGNI; retention → ROADMAP if ever real). **No backfill** (panel consensus: the only source is the already-polluted log — 365/370 test fixtures; importing it would seed the clean ledger with garbage). Instead, **honest fresh-start empty states**: the store records its own `created_at`; when the store is fresh but the install is not (config exists / schedule registered), Run History/Home say "Run history starts fresh with this update — earlier runs aren't shown", never "No sync has run yet".
- **D2a — Log-line parity (ops).** The `__DISTRICTSYNC_RUN__` line **stays** in the diagnostic log and gains the same enrichment (source, sis_type, error_category): the record dict is built once and written to both sinks. The log keeps the rich free-text error/anomaly detail (ops needs "KeyError: final mark" at 2am); the store carries only the bounded `error_category` + counts (privacy split). Store lifecycle events (WAL fallback taken, quarantine/recreate) are logged INFO/WARNING. `docs/partner/troubleshooting.md` documents `history.db`, the log's ops role, and the unchanged exit-code contract.
- **D2b — Store writes are best-effort and strictly non-fatal, positioned after output commit.** Wrapped try/except at both call sites; a store failure (locked, disk full, corrupt) logs WARNING/ERROR and never alters `PipelineResult`, the CSVs, or the exit-code contract (0/1/2/3); the failure-path write must never raise and mask the original ETL exception (`pipeline.py:425-426`). The enriched log line is the durable fallback when the store write fails.
- **D2c — `source` propagation:** the registered task's action injects `DSYNC_SOURCE=scheduled` (single-sourced with the action builder in `register_task`); CLI default `cli`; `convert_job` passes `manual`. Without the marker every nightly run would mislabel — the field's whole purpose is "did the NIGHTLY sync run".
- **D3 — Test isolation is a correctness fix.** Autouse conftest fixture patches the **load-bearing deep seam `paths.user_data_dir`** (patching `user_log_file` alone is a no-op for `logger.py`, which imported the name by reference at module top — `logger.py:4`), AND covers `AppConfig` — which today bypasses `paths.py` entirely via import-time `APP_CONFIG_DIR = Path.home()/".districtsync"` (`app_config.py:19-20`): refactor AppConfig to resolve through `paths.user_data_dir()` at call time (single source; also unblocks any future relocation). The store resolves its path at call time via `paths.user_history_db()` — never a module-level constant (or every pipeline/e2e/SD74 test would write the real `history.db`). Fixture also swaps the keyring backend (no test touches the real Credential Manager) and restores/close global logging handlers on teardown (a leaked FileHandler blocks tmp_path cleanup on Windows). A **canary test** deliberately calls `AppConfig.save()` + `get_logger()` + a store write under the fixture and asserts the real log AND config.json AND history.db are untouched. `get_logger` moves out of import time and is explicitly called in **every** entry path (CLI dispatch + Flet launcher), each covered by a test. Honest register: this is *redirect-the-seams + guard tests* — it holds while write paths route through the patched seams; the canary is the tripwire, not a mechanical impossibility.
- **D4 — Schedule truth via a ScheduledTasks-module reader, tri-state.** New reader combines `Get-ScheduledTask` (exists / state / action path) + `Get-ScheduledTaskInfo` (NextRunTime; LastTaskResult captured from the same query) — `Get-ScheduledTaskInfo` alone cannot report existence or action path. Reader reuses the register-script hardening verbatim: FIXED script text referencing only `$env:DSYNC_TASKNAME` (never interpolated), name through `validate_task_name` first, `-EncodedCommand`, datetimes emitted culture-invariantly (ISO round-trip), bounded `subprocess` timeout. Contract is **three-state** and owned by a new pure COUNTED module `src/ui_flet/schedule_status.py`: `LIVE` (task found; next run known) / `MISSING` (definitively queried and absent → re-register route) / `UNKNOWN` (PowerShell missing, timeout, access denied — e.g. an elevated-registered task unreadable by the filtered token). **Only MISSING may claim "not scheduled"; UNKNOWN renders "we couldn't confirm the schedule right now" and never falls back to asserting "scheduled" from the config boolean.** All consumers (Home verdict, Setup readout, Run History footer/empty-state copy, nav badge) consume this one typed derivation — the read-back result is fetched once per surface build **off-thread** (existing `page.run_thread` pattern, "checking the schedule…" placeholder), injected into the pure derivation; `nav.py` stays subprocess-free. `schedule_status` also detects the "task fired but no run record exists" contradiction via LastTaskResult (the Downloads-pinned-exe blind spot) and logs a WARNING when read-back contradicts the config flag. Config writes move **after** registration success; Unregister affordance added (idempotent: deleting an absent task presents as "already not scheduled"; may itself require the D5 elevation for an elevated-registered task — same path). Registering from a transient location (Downloads/Temp) warns before pinning.
- **D4a — Two distinct facts, never conflated:** `setup_completed` (durable config fact: the wizard finish line was reached at least once — folders + district committed; schedule/delivery possibly deferred) vs `schedule live now` (D4 read-back). **Wizard entry gates on `not setup_completed` only.** A previously-configured install whose task broke lands on **Home with a WARNING + a fix path routed to Setup's schedule section** — never back into first-run onboarding (the Event-141 Firefighter must not be greeted as a newcomer). `nav.needs_setup` is redefined accordingly and all four current consumers of `schedule_registered` move to the appropriate fact.
- **D5 — Per-operation elevation, isolated in `src/scheduler/elevation.py`** (SRP: a generic privilege-elevation IPC primitive, not task-scheduler logic; `windows.py` consumes it). `ShellExecuteExW(lpVerb="runas")` on the **absolute System32 `powershell.exe` path** (elevation raises PATH-hijack stakes) with `SEE_MASK_NOCLOSEPROCESS` → `WaitForSingleObject` with a **bounded timeout** (never INFINITE; on timeout: terminate child, calm degradation) + `GetExitCodeProcess`. Inbound (password-bearing) channel: DPAPI **CurrentUser**-scope encrypted JSON blob (structured JSON, not KEY=VALUE — paths can contain `=`) with app-scoped optional entropy, owner-only ACL set explicitly, in `paths.user_data_dir()` (one app-data location); **fail closed on SID mismatch** (over-the-shoulder UAC under a different admin account cannot decrypt CurrentUser DPAPI — that is correct; never widen to LocalMachine scope, which would downgrade a domain credential's confidentiality; surface "elevation ran as a different account — log in as an administrator or use the no-password schedule"). Outbound result: **plaintext** JSON (it carries no secret — a sanitized ok/message) written **atomically** (temp + rename); parent handles child-exited-but-result-missing/partial by degrading to "couldn't confirm registration" and re-deriving via D4 read-back. The elevated child's message passes the same `_clean_ps_stderr` + `DSYNC_`-strip sanitization before surfacing or storing; the script keeps the never-echo-`$env:DSYNC_TASK_PW` property. Best-effort startup sweep deletes orphaned handshake files. Whole flow runs off the UI thread with visible states: in-flight ("waiting for the Windows permission prompt…"), declined (`ERROR_CANCELLED` 1223 → calm copy), accepted-but-failed (child crashed / result missing / registration error → through `classify_schedule_error`), timeout. UAC-success is verified by the D4 read-back, not assumed from exit code alone.
- **D6 — SFTP test side-effect-free and provenance-honest:** `test_connection(password_override=...)` threads a typed password transiently to `client.connect()` only — never keyring, never logs, never the returned message; a test asserts `store_password` is not called on the test path. Keyring written only on explicit Save. Success copy names what was tested: "Connected to `<host>` as `<user>` using the password saved in this computer's credential manager" / "…using the password you just entered — click Save to keep it". Unsaved host/user edits soften the claim: "these settings work — Save to use them for the nightly sync" (present-tense, test-scoped; never "the nightly sync can deliver" for unsaved values).
- **D7 — Nav: fixed order always** (Home, Convert, Run History, Setup, Mapping, Help); newcomer guidance via initial selection (launch into the wizard while `not setup_completed`) + a "needs attention" badge on Setup driven by `schedule_status` (lands with the schedule-truth slice, where its signal exists). `build_nav` exposes the rail; `select_by_id` syncs the highlight — one change fixes every programmatic hop. Rail label stays "Setup" (spatial memory); the graduated surface titles itself **"Settings"** with a one-time transition cue after wizard completion.
- **D8 — Wizard→Settings hybrid**, pure state machine in `src/ui_flet/setup_flow.py` (five concrete named steps — **no data-driven step engine**, YAGNI). `setup_flow` is genuinely pure: it consumes **injected verification results** (`folders_valid`, `schedule_state: LIVE/MISSING/UNKNOWN`, `delivery_tested: ok/failed/skipped`) — the view/boundary layer performs all I/O and feeds results in. Steps persist to their existing sinks as they commit (folders/district → AppConfig, schedule → task then config, SFTP → keyring+config); **resume derives the first unsatisfied step from real state — no stored cursor** — and **reconciles against side effects already performed** (read-back for a task the Schedule step already registered; keyring for a stored credential) showing "already scheduled/stored" instead of double-registering. No single step may flip `setup_completed`; the finish line is the only completion signal, so mid-wizard abandonment never reads as "set up". Back navigation + "Step 2 of 5" indicator; Enter advances when the step gate is satisfied; focus moves to the new step's first field. Per-step async states enumerated (schedule: UAC in-flight/declined/failed/timeout; finish: read-back tri-state; delivery: testing/ok/failed/skipped). **Finish-line copy is honest and adaptive** — it names what was checked and when, never guarantees the future: SFTP configured → "You're set up. Tonight at HH:MM DistrictSync will build <district> and try to deliver to SpacesEDU — we tested the connection to <host> as <user> just now and it worked."; SFTP skipped → "…will build <district> into your output folder. Set up delivery whenever you're ready."; schedule skipped → "…Run conversions from the Convert tab; add a nightly schedule whenever you're ready." Settings mode: **one** Save that reconciles — re-registering the task when **any field baked into the task action changed** (input_dir, output_dir, sis_type, sftp flag, run_time — not time only), with the live "Currently registered: daily at HH:MM — next run <t>" readout beside it.
- **D9 — Explicit district everywhere:** `sis_type` default `""`; "Choose your district" placeholder; auto-select only when exactly one config exists; Convert refuses to run without an explicit district (no `configs[0]` fallback). Existing installs unaffected.
- **D10 — Convert output visibility within the PII rule:** `ConvertResult` stays path-free; the *view* renders the resolved output folder + "Open folder" (`os.startfile`); resolved output dir shown on the form before running; empty `output_dir` blocks with a routed "set your output folder in Setup" instead of silently writing into the input folder.

Alternatives rejected: whole-app `--uac-admin` (PII posture, mapped drives, breaks scheduled launch); runs.jsonl (no concurrent-writer story); reactive config store (YAGNI); log filter-at-read (rotation loss + pollution remain); "restart as admin" relaunch (loses typed state; session stays elevated); backfill (source data is polluted); compaction + migration framework + data-driven wizard steps (YAGNI, panel-confirmed); DPAPI-encrypting the outbound result (no secret in it).

## Architecture & holistic fit

- **Codebase fit:** preserves the three-layer split and COUNTED-pure vs view-glue convention. New modules: `src/history/store.py` (+ `__init__.py`) — data layer, UI-neutral and platform-neutral (pipeline writes, UI reads; no `ui_flet` import); `src/scheduler/elevation.py` — Windows elevation IPC primitive consumed by `windows.py`; `src/ui_flet/schedule_status.py` — the single pure owner of schedule-status derivation + precedence (mirrors `home_status.LatestReason`; `query_task`'s replacement returns a typed frozen dataclass, not `dict[str,str]`); `_friendly_schedule_time` relocates here from `home_status` (cohesion). `src/ui_flet/setup_flow.py` — pure wizard state machine; the Setup **view** decomposes into per-step render helpers + a settings-scroll renderer (no god-view). Exactly **one** run-record reader post-migration: the store reader reconstructs the existing flat-dict record shape; `run_log.py` retires (its `[]/None` contract and tests migrate to the store reader). No new dependencies (stdlib `sqlite3`, `ctypes`).
- **Product fit:** direct implementation of the Discover deliverable (trust instrument; Installer/Watcher/Firefighter personas — D4a exists precisely so the Firefighter is never treated as an Installer). Personas + journey states persist to `docs/claugentic-PRODUCT.md` at program start (not the last slice) so per-slice review uses the persona lens.
- **Quality dimensions to uphold:** `data-and-persistence` (store contract, additive-only schema, quarantine recovery, non-fatal writes — including its privacy/PII facet: bounded error_category in the store, rich detail only in the local log, per CLAUDE.md's top-priority student-PII scope), `reliability-resilience` (tri-state read-back, elevation timeout/orphan cleanup, store degradation), `security` (DPAPI scope fail-closed, System32 pin, sanitization across the elevation boundary, keyring-on-Save-only, validators on all new subprocess surfaces — including the secret/PII-leak facet of the elevation handshake), `observability-ops` (log-line parity, store lifecycle logging, exit-code contract provably unchanged, troubleshooting docs), `product-ux` (per-step states, honest empty states, fix routing, keyboard flow), `testing` (isolation canary, failure-path fixtures, honesty about manual-Verify surfaces), `maintainability-structure` (single-source schedule status, one reader, SRP module placement).
- **Future-proofing:** `user_version` + additive-only rule keeps a v2 schema open without building migration machinery; `source` tag enables later filtering UI; `schedule_status` gives Linux read-back a slot; elevation primitive is reusable for a future elevated Unregister. Not building now: history export, retention, push alerting (ROADMAP), pluggable wizard steps.

## Affected files

- `src/ui_flet/shell.py` — supplier-based config binding; rail reference + highlight sync; async exit; `page.window.icon`; wizard-vs-home launch predicate (`setup_completed`).
- `src/ui_flet/nav.py` / `nav_rail.py` — fixed order; badge (schedule_status-driven); rail/selection setter; `needs_setup` → `setup_completed` semantics.
- `src/ui_flet/screens/mapping.py` — write-through apply + self-refresh; gate vs persisted value.
- `src/ui_flet/screens/home.py` / `help.py` / `run_history.py` — fresh config per build; schedule verdict + empty-state copy from `schedule_status` + store meta (fresh-start vs first-run); Refresh affordance; task-missing fix CTA routes to Setup.
- `src/ui_flet/screens/setup.py` — wizard/settings hybrid rendering (per-step helpers); Enter-to-submit (+ gate re-checks); SFTP test copy + side-effect-free test; save-after-success; Unregister; single reconciling Save (any task-arg change); off-thread elevation + read-back with in-flight states.
- `src/ui_flet/setup_flow.py` (new) — pure wizard state machine (injected verification results).
- `src/ui_flet/schedule_status.py` (new) — pure tri-state schedule derivation + precedence + schedule copy.
- `src/ui_flet/screens/convert.py` / `convert_result.py` — output folder display + Open folder; explicit district; no input-dir fallback; `write_run_record(source="manual")`.
- `src/ui_flet/run_log.py` — retired; reader moves to store (same record shape + `[]/None` contract).
- `src/ui_flet/home_status.py` / `run_history.py` — consume store reader + `schedule_status`; `_friendly_schedule_time` relocates.
- `src/history/store.py` + `src/history/__init__.py` (new) — SQLite store (write/read/quarantine-recreate; lazy path via `paths.user_history_db()`).
- `src/etl/pipeline.py` — build record once → both sinks via shared `write_run_record`; `source` param; `error_category` taxonomy (bounded, mirrors `LatestReason` pattern); non-fatal wrapping.
- `src/scheduler/windows.py` — reader rewrite (Get-ScheduledTask + Get-ScheduledTaskInfo, fixed script, invariant datetimes, timeout); register integrates elevation; `DSYNC_SOURCE` in action; structured results.
- `src/scheduler/elevation.py` (new) — ShellExecuteExW runner + DPAPI inbound + atomic plaintext result + sweep.
- `src/config/app_config.py` — resolve dir via `paths.user_data_dir()` at call time; `sis_type` default `""`; `setup_completed` field.
- `src/utils/paths.py` — `user_history_db()`.
- `src/main.py` — `get_logger` deferred to entry paths; CLI `source` default.
- `src/utils/logger.py` — call-time path resolution compatible with the fixture seam.
- `tests/conftest.py` — autouse isolation (paths seam + AppConfig + keyring backend + logging teardown) + canary test.
- `assets/districtsync.ico` (new) + `Makefile` + `.github/workflows/flet-pack.yml` — brand icon end-to-end.
- `src/ui_flet/launcher.py` — async close fix; explicit `get_logger`.
- `tests/test_ui_flet_render_smoke.py` — mount-render smoke coverage extended to every new/rewritten surface (per-slice requirement).
- `docs/claugentic-INVARIANTS.md` — store invariants (additive-only schema, sole-creator write path, no-downgrade, non-fatal writes).
- `docs/` — ARCHITECTURE_TREE (new files), DECISIONS (D1–D10 + additive-only schema rule + backfill/compaction rejections), PRODUCT.md (personas, at program start), `partner/troubleshooting.md` (history.db, log role, exit codes), CHANGELOG.

## Research / grounding

- **Files reviewed:** all file:line citations above (46-agent investigation, each claim independently verified: 34 Verified / 1 Unconfirmed-latent); Flet 0.85.3 installed source; live machine state (config.json, etl_tool.log, TaskScheduler event log Events 106/140/141, keyring existence, output dir). Panel evidence: `app_config.py:19-20` import-time dir binding; `logger.py:4` by-reference import; `convert.py:102` convert_job bypasses `_emit_run_log`; `home_status.py:257`/`run_history.py:137`/`nav.py:74` = the four `schedule_registered` consumers; `windows.py:475` subprocess without timeout.
- **Harness docs consulted:** CLAUDE.md; `docs/claugentic-WORKFLOW.md`; `docs/FLET_1.0_CONVENTIONS.md`; standards catalog per lens (panel run).
- **Findings:** `-Force` already present (re-registration worked; Task Scheduler GUI needed F5); Setup/Convert model the fresh-load pattern; `filepicker.py` save-gate reusable; `verdict`/`humanize`/`home_status` single-source modules extend cleanly; SFTP test marshalling (`setup.py:633-644`) is the off-thread pattern D4/D5 reuse.

## Risks & mitigations

- **SD74 snapshot / output contract regression** → zero transform/loader changes; store write is additive, non-fatal, post-commit; snapshot + contract + exit-code tests green every slice (explicit test: forced store-write failure changes neither exit code nor CSVs).
- **Elevated-registered task unreadable by the filtered token** → tri-state contract classifies as UNKNOWN (never MISSING); manual-Verify item explicitly covers "register elevated → read back non-elevated"; schtasks fallback if needed.
- **Elevation handshake edge cases** → bounded timeout + atomic result + missing-result degradation + startup sweep + fail-closed SID mismatch; real DPAPI round-trip covered by a Windows-only test (no UAC needed); UAC prompt itself on the manual checklist (success / declined / wrong password / different-admin account).
- **SQLite on roaming profiles (WAL on SMB)** → busy_timeout + DELETE-journal fallback + `None` sentinel + quarantine-recreate; store stays in the single app-data dir (relocation deferred — see Slice 11).
- **Wizard scope creep / god-view** → five concrete steps, injected results, per-step render helpers; `yagni-sentinel` re-reviews at Verify.
- **Mid-program pause shipping incoherent onboarding** → the wizard and the one-front-door collapse land **together in Slice 8** (non-splittable); everything before it leaves the old coherent Setup intact.
- **Existing deployed installs** → config migration additive; store created lazily; empty-`sis_type` affects fresh installs only; honest fresh-start history copy for upgraders.
- **Flet 0.85.3 API traps** → conventions doc; view changes exercised via runtime-qa at Verify; Slice 2's surfaces are honestly manual-Verify (see Test strategy).

## Test strategy

- **Store:** round-trip with fixed timestamps (no `now()` in assertions); reader contract (missing DB → `[]` without creating it; 0 rows → `[]`; corrupt/locked → `None`); quarantine-recreate; non-fatal write (forced failure → pipeline result/exit/CSVs unchanged; failure-path write never masks the original exception); record-shape equivalence against the derivation modules; connections context-managed (WAL sidecar handles must not block tmp_path teardown). No two-process concurrency test (would test SQLite's own guarantee); a locked-DB fixture tests **our** degradation instead.
- **Isolation:** the canary (AppConfig.save + get_logger + store write under the fixture → real profile untouched, all three artifacts); entry-path logging tests (CLI + launcher both configure the sink).
- **Schedule:** reader parse fixtures — task-not-found / exists-never-run / non-zero LastTaskResult / invariant-datetime samples; `schedule_status` precedence table (LIVE/MISSING/UNKNOWN × config hint × fired-but-no-record contradiction); save-after-success ordering; Unregister idempotence.
- **Elevation:** blob encode/decode (real DPAPI round-trip, Windows-only, UAC-free) ; result-file protocol (atomic write, missing/partial handling); declined/timeout mapping; sanitization across the boundary (message never contains `DSYNC_`/password).
- **SFTP:** `password_override` threading; `store_password` NOT called on test path; copy provenance variants.
- **setup_flow:** full transition table (resume-from-state reconciliation, skips, no-step-flips-`setup_completed`, adaptive finish copy variants); nav fixed-order + badge model; mapping gate/revert; convert district gating + output-dir resolution/blocking.
- **Honest manual-Verify checklist (runtime-qa):** Exit closes (dev + frozen); Enter submits on all 7 fields with gates re-checked; icon on window+taskbar (frozen); UAC success/declined/wrong-password/different-admin; register-elevated→read-back-non-elevated; wizard end-to-end on a clean profile. Slice 2 and the elevation UX rest on this checklist, not unit tests — stated plainly.
- **Render-smoke gate (per-slice requirement):** extend the automated mount-render smoke suite (`tests/test_ui_flet_render_smoke.py`) to every new or rewritten screen/surface a slice touches (wizard steps, settings mode, reworked Home/Run History/Convert/Mapping states) — per DECISIONS 2026-07-06, coverage-omitted Flet views once shipped a mount crash green; view correctness must not rest on manual-Verify alone.
- **Regression:** full suite + SD74 snapshot + contract + `make validate-config` green per slice; 80% gate holds.

## Decomposition (slices)

- [ ] **Slice 0 — Program grounding**: persist PRODUCT.md (personas/journeys from Discover), DECISIONS entries for D1–D10 + panel rejections (backfill, compaction, migration framework, outbound DPAPI, badge-by-reorder). Small, unblocks persona-lens review of every later slice.
- [ ] **Slice 1 — Config freshness + mapping integrity** (D1): supplier binding; fresh loads; mapping write-through/self-refresh/gate; Refresh affordance on Home/Run History. Lands complete: every stale-display symptom gone.
- [ ] **Slice 2 — Chrome & input craft**: async exit ×3, Enter-to-submit + gate re-checks, brand `.ico` + `page.window.icon` + `--icon` both builds. Manual-Verify slice (stated).
- [ ] **Slice 3 — Nav integrity** (D7 core): fixed order; rail reference + highlight sync on programmatic nav; launch-selection logic. Small and foundational; badge deferred to Slice 5 (its signal doesn't exist yet).
- [ ] **Slice 4a — Test isolation + seam foundation** (D3): autouse fixture on the deep `paths.user_data_dir` seam + AppConfig-through-paths refactor + keyring backend swap + logging handler teardown + `get_logger` deferral to entry paths (CLI + launcher, each tested) + the canary test. A suite-wide correctness fix and hard prerequisite for testing the store; lands vertically complete on its own.
- [ ] **Slice 4b — Run store** (D2/D2a/D2b/D2c): store module (`paths.user_history_db()` lazy resolution) + shared `write_run_record` from pipeline AND convert_job + source tags **including the `DSYNC_SOURCE=scheduled` injection into `register_task`'s action (pinned here — a small `windows.py` edit, so scheduled runs are labeled correctly from the store's first day; the reader rewrite stays in Slice 5)** + enriched log-line parity + reader repoint (run_log retirement) + honest fresh-start empty states. Store invariants recorded in `docs/claugentic-INVARIANTS.md` (additive-only schema; write path sole creator/migrator; higher `user_version` → never migrate/downgrade; store write strictly non-fatal, never masks the original ETL exception).
- [ ] **Slice 5 — Schedule truth** (D4/D4a): `schedule_status.py` + reader rewrite + all four consumer migrations + `setup_completed` split + save-after-success + Unregister + action-path warning + Setup badge + contradiction WARNING log + task-missing fix CTA routing to Setup.
- [ ] **Slice 6 — Per-op elevation** (D5): `elevation.py` + handshake + off-thread UX states + integration into register/unregister; security-sensitive (honesty+security lenses at Verify).
- [ ] **Slice 7 — SFTP truthfulness** (D6): side-effect-free test, provenance copy, Save-only keyring writes.
- [ ] **Slice 8 — Setup wizard→settings + one front door** (D8, D9 setup-side, D10 IA): `setup_flow.py`, per-step renderers, skippable steps, adaptive honest finish line, resume/reconcile, settings mode with single reconciling Save (any task-arg change), "Settings" title + transition cue, keyboard flow, unconfigured-Home collapse to the single hero→wizard door. **The `AppConfig.sis_type` default `""` flip + "Choose your district" placeholder land here (pinned)** — the wizard's District step is where the empty default becomes coherent UX. **Non-splittable pair with the IA collapse** (pausing between them would ship a fourth door). Depends on 1,3,5,6,7.
- [ ] **Slice 9 — Convert output + explicit district** (D10, D9 convert-side): resolved-output display + Open folder + no input-dir fallback + explicit district + `source=manual` display polish.
- [ ] **Slice 10 — Close**: troubleshooting/partner docs, ARCHITECTURE_TREE/CHANGELOG sweep, whole-feature closing Verify against the Stage-1 job-to-be-done (Installer/Watcher/Firefighter journeys end-to-end), retrospect harvest.
- [ ] **Slice 11 — Cross-platform app-data relocation (APPROVED in-scope; build order: immediately after 4a, before 4b so `history.db` is born in the new location)** — replace `~/.districtsync` with the industry-standard per-OS user-data directory via the `platformdirs` library (the de-facto standard; tiny, pure-Python, PyInstaller-safe): Windows `%LOCALAPPDATA%\DistrictSync` (non-roaming — correct for a WAL SQLite store), macOS `~/Library/Application Support/DistrictSync`, Linux `$XDG_DATA_HOME/districtsync` (default `~/.local/share/districtsync`). Single change point: `paths.user_data_dir()` (everything already resolves through it at call time after 4a). Transparent one-time migration: on startup, if the legacy `~/.districtsync` exists and the new dir doesn't, move config + logs (+ store with `-wal`/`-shm` sidecars if present) and leave a `MOVED.txt` breadcrumb in the old location; migration is idempotent and failure-safe (on any error, keep using the legacy dir and log a warning — never strand the user between two locations). User rationale recorded: pre-adoption window makes this the right time; must be professional and industry-standard on all three OSes. `requirements.txt` + PyInstaller hidden-imports updated; `docs/partner/troubleshooting.md` documents the per-OS paths.

---

## Review  _(synthesizer-gate, plan-gate altitude, Stage 3 — RUNNING AS: Opus 4.x, clean-context separate-role pass; same-model as the planner — a reduction of rubber-stamping risk, not a guarantee)_

**Verdict: PASS** (all 5 required changes from the CHANGES-REQUIRED pass are resolved; re-verified against the plan file). This is a strong, genuinely-reasoned plan: the design (D1–D10) is architecturally sound (SRP module placement, one-reader store contract, pure COUNTED derivations with injected I/O, tri-state read-back that never asserts unconfirmed schedule state), the path choice (full pipeline) is correct, YAGNI is aggressively upheld (backfill/compaction/migration-framework/data-driven-steps/reactive-store all cut and consistently absent from every slice), and the Architecture & holistic fit section is real, not hand-waved. The load-bearing structural claims were spot-checked against source and hold (`logger.py:4` by-reference import → patching the deep `paths.user_data_dir` seam is correct; `app_config.py:19-20` import-time dir bypass; `pipeline.py:244` free-text error; `nav.py:74` `needs_setup`; `conftest.py` has no isolation today).

**Required changes — ALL RESOLVED:**

1. **Split Slice 4 — RESOLVED.** Now **4a (D3 — test-isolation + seam foundation)** and **4b (D2/D2a/D2b/D2c — SQLite store)**; the D-section→slice mapping is clean (D3 moved to 4a, no overlap, no orphaned reference to the old monolithic Slice 4), each lands vertically complete, and 4a's 2-artifact canary evolves to the 3-artifact canary in 4b (captured in Test strategy: "all three artifacts").

2. **Mount-render smoke gate — RESOLVED.** Added as an explicit per-slice requirement in Test strategy (extend `tests/test_ui_flet_render_smoke.py` to every new/rewritten surface) and listed in Affected files, citing the DECISIONS 2026-07-06 near-miss.

3. **Store invariants in `docs/claugentic-INVARIANTS.md` — RESOLVED.** Named in Slice 4b and Affected files with all four invariants (additive-only schema; sole-creator/migrator write path; higher `user_version` → never migrate/downgrade; store write strictly non-fatal / never masks the original ETL exception).

4. **`privacy-pii` mapping — RESOLVED.** The pseudo-module is gone; its facets are folded into `data-and-persistence` (bounded `error_category`, log-vs-store detail split) and `security` (secret/PII-leak facet of the elevation handshake), citing CLAUDE.md's top-priority PII scope. Every dimension now maps to an actual module.

5. **Pinned edits — RESOLVED.** `AppConfig.sis_type` default `""` flip pinned to **Slice 8** (the wizard District step); `DSYNC_SOURCE=scheduled` injection into `register_task`'s action pinned to **Slice 4b** (small `windows.py` edit so scheduled runs label correctly from the store's first day; the reader rewrite stays Slice 5). Consistent with D2c; no 4→5 mislabel gap.

**Sizing/completeness (per slice):**
- **Slice 0** — OK (small grounding; add the INVARIANTS.md entries here per #3).
- **Slice 1** — OK.
- **Slice 2** — OK (manual-Verify honestly stated; also add its touched screens to the mount gate per #2).
- **Slice 3** — OK (small, foundational; badge correctly deferred to 5).
- **Slice 4a** — OK (isolation/seam foundation; a self-contained correctness fix that lands complete on its own).
- **Slice 4b** — OK (SQLite store on the clean 4a foundation; owns the `DSYNC_SOURCE=scheduled` pin + the INVARIANTS.md entries).
- **Slice 5** — Large but cohesive (schedule-truth must land as a unit — the `setup_completed` vs schedule-live split can't half-land without the Event-141-firefighter-as-newcomer bug). Accept, but the Stage-4 spec must confirm the PowerShell reader rewrite + 4-consumer/`setup_completed` migration fit one session; build the pure `schedule_status.py` + reader parse-fixtures first, then the consumer migration, within the slice.
- **Slice 6** — OK (cohesive, security-sensitive; correctly depends on 5).
- **Slice 7** — OK (small).
- **Slice 8** — Largest slice; the **non-split call is sound** (pausing between the wizard and the one-front-door collapse would ship a fourth door, and the settings-graduation can't split from the wizard without a half-done "no surface to edit" state). Accept as non-splittable, but the Stage-4 spec must confirm it lands in one session and should land the pure `setup_flow.py` state machine + its full transition-table tests as the first coherent sub-unit before the view decomposition.
- **Slice 9** — OK (owns the D9 default flip per #5a).
- **Slice 10** — OK (close + whole-feature Verify against the Stage-1 job-to-be-done).
- **Slice 11** — correctly recommended-DEFER; a genuine open user decision (Flags line honest).

**Harness impact:**
- **`docs/claugentic-INVARIANTS.md`** — new entries for the store contract (#3); the file already exists.
- **Mount-render smoke gate extension** (#2) — a durable test-gate improvement covering the rewritten/new Flet screens.
- **DECISIONS** D1–D10 + additive-only schema rule + panel rejections (already scoped to Slice 0) — good.
- **PRODUCT.md** personas persisted at program start (Slice 0) — good.
- **Stage-9 candidate (flag for Slice 10 harvest, not required now):** the "test-isolation via patched deep seams + canary tripwire" pattern (redirect-the-seams + guard test) is a reusable lesson worth a CLAUDE.md / standards note.
- **No new agent role required.**

---

## Spec  _(per slice, after Review passes — Stage 4, JIT per slice)_

### Slice 0 — Program grounding
- **In plain English:** writes down the product's personas/journeys (from the Discover deliverable) and the program's design decisions so every later slice is reviewed against them. No code changes.
- **Files & changes:** `docs/claugentic-PRODUCT.md` — persist the Installer/Watcher/Firefighter personas, the three journeys (first-run / daily trust check / incident recovery) with their state tables, the trust-instrument bar, and the resolved open questions (wizard hybrid; explicit district with auto-select-iff-exactly-one; one front door; stable nav; output-folder-at-view-layer; provenance-named test copy; store-backed district-scoped history). `docs/claugentic-DECISIONS.md` — dated one-liners: D1–D10 design decisions; rejections (backfill — polluted source; compaction + migration framework — YAGNI, additive-only rule + user_version stamp instead; outbound DPAPI — no secret; nav-reorder prominence — spatial memory; whole-app UAC — PII posture); Slice-11 approval rationale (pre-adoption window, cross-platform platformdirs). `docs/claugentic-ROADMAP.md` — add "push alerting on failed/missed nightly runs" line; update 0029 status line.
- **In-scope standards dimensions:** docs & traceability only.
- **Tests to add:** none (docs slice).
- **Acceptance criteria:** PRODUCT.md names the three personas + journey states; DECISIONS has one dated line per decision/rejection; no source file touched.

### Slice 1 — Config freshness + mapping integrity (D1)
- **In plain English:** switching district (or completing setup) is reflected everywhere immediately — Home greeting, Help, Run History, and the Mapping screen's own card; a switch can be reverted; Home/Run History gain a Refresh button so a Watcher who leaves the app open can re-check without navigating away.
- **Files & changes:**
  - `src/ui_flet/shell.py` — stop binding the startup `AppConfig` instance into screen factories; each factory invocation loads fresh (`AppConfig.load()` inside the factory lambda / a supplier arg), so every navigation renders current state. Startup nav-model derivation may keep the startup config for now (nav order/launch selection are reworked in Slices 3/5 — note this as a known, bounded remainder in the slice commit message, not silent).
  - `src/ui_flet/screens/mapping.py` — `_on_apply` re-renders the surface after save: recompute `summarize_config` for the new current, replace the current-mapping card content, and re-derive the Apply gate. Extract the gate predicate into `src/ui_flet/mapping_catalog.py` as a pure COUNTED fn `can_apply(pending: ConfigSummary | None, persisted_sis: str) -> bool` (pending loaded_ok AND pending.sis_type != persisted_sis); the view compares against the *persisted* value (fresh load), never a captured instance.
  - `src/ui_flet/screens/home.py` / `run_history.py` — accept an optional `on_refresh` callback; render a small secondary "Refresh" button (components.secondary_button) that re-invokes the current screen build via the shell (`select_by_id(current_id)`).
  - `src/ui_flet/screens/help.py` — no change beyond receiving the fresh instance (verify the greeting updates).
- **In-scope standards dimensions:** `maintainability-structure` (supplier pattern already used by Setup/Convert; pure gate extraction), `product-ux` (stale-identity trust fix; Refresh affordance), `testing` (pure gate fn COUNTED; render-smoke extension).
- **Tests to add:** `can_apply` truth table (loaded_ok × same/different × revert-after-apply scenario) in the mapping_catalog tests; render-smoke coverage for Home/Run History (with on_refresh) and Mapping post-apply re-render; regression: full suite + SD74 + contract green.
- **Acceptance criteria:** with the app running: Mapping Apply updates its own card immediately and allows reverting to the previous mapping without restart; after a switch, navigating to Home/Help/Run History shows the new district; completing setup in-session flips Home off the onboarding hero on next navigation; Refresh re-reads state in place; no test touches the real user profile (existing per-file fixtures still in place until 4a).

### Slice 2 — Chrome & input craft
- **In plain English:** Exit closes the app, Enter submits forms, and the window/taskbar/exe show the myBlueprint icon instead of the generic Flet logo. These are OS-interaction surfaces — correctness rests on the stated manual checklist plus the automated render-smoke gate.
- **Files & changes:**
  - `src/ui_flet/shell.py` — fix the un-awaited coroutine no-op: Flet 0.85.3 `Window.destroy()` is `async def`, so `do_exit` (line 223) and `on_window_event` (line 243) must `await` it (async handlers are supported — `base_control.py:450`) or route via `page.run_task(page.window.destroy)`; both paths share one exit helper so they can't drift; keep `os._exit(0)` as the post-await fallback. Same fix in `src/ui_flet/launcher.py:104` (boot-error dialog Close).
  - Icon: add `assets/districtsync.ico` (multi-resolution 16–256px, generated from `docs/assets/myblueprint-logo.png`; commit the binary). Set `page.window.icon` in shell's window-setup block with runtime path resolution (dev source tree vs frozen `sys._MEIPASS`; a small pure helper, tested). Add `--icon assets/districtsync.ico` to `flet pack` in BOTH `Makefile` `build-win` and `.github/workflows/flet-pack.yml` (covers the Explorer exe icon AND the frozen runtime titlebar via flet_cli's view-exe patching; `DistrictSync.spec` is generated output — don't hand-edit). Ship the .ico in the bundle so the runtime path resolves when frozen.
  - Enter-to-submit: wire `on_submit` on all 7 Setup TextFields — run-time (`setup.py:226`) + Windows password (`:258`) → `_register`; SFTP username/remote/port/password (`:438-456`) → `_save`. **Handlers must re-check the same gates the buttons encode** (`on_submit` bypasses a disabled button — e.g. `register_btn`'s `cfg.is_complete()` gate at `:386-395`); extract each gate predicate into a small pure fn if not already one.
- **In-scope standards dimensions:** `product-ux` (keyboard operability, craft), `reliability-resilience` (exit path), `testing` (honesty: manual-Verify surface — stated).
- **Tests to add:** pure icon-path-resolution helper test; gate-predicate re-check tests (Enter with unsatisfied gate is a no-op); render-smoke stays green. Manual checklist (recorded in the slice commit): Exit closes in dev AND frozen exe; window X still closes; Enter submits/refuses correctly on all 7 fields; icon shows on titlebar + taskbar + exe file in the frozen build.
- **Acceptance criteria:** Exit button closes the window in dev run; all three destroy call sites fixed; Enter behaves per field with gates enforced; both build definitions pass `--icon`; `.ico` committed and bundled.

### Slice 3 — Nav integrity (D7 core)
- **In plain English:** the left-nav order is identical every launch (Home, Convert, Run History, Setup, Mapping, Help), and the highlight always follows you — including when a button navigates for you ("Start setup", "Check Run History").
- **Files & changes:**
  - `src/ui_flet/nav.py` — remove state-dependent *reordering*: `ordered_destinations` returns the one fixed order regardless of `needs_setup`; `prominent_initial_id` keeps its semantics (launch lands on Setup while `needs_setup` — re-keyed to `setup_completed` later in Slice 5, a bounded remainder to note). Delete the now-dead group-reorder logic; keep the grouped model only if the rail still renders group affordances, else simplify (KISS).
  - `src/ui_flet/nav_rail.py` — `build_nav` stops discarding the rail: return/expose the inner `ft.NavigationRail` (or a `set_selected(dest_id)` closure over it).
  - `src/ui_flet/shell.py` — hold the rail handle; `select_by_id` sets `rail.selected_index = ordered.index(dest_id)` before `page.update()` — this one change fixes all three programmatic `on_navigate` sites (onboarding "Start setup" `home.py:147`, Home fix-CTAs `home.py:104`, error fallback `home.py:158`).
- **In-scope standards dimensions:** `product-ux` (stable IA, "where am I"), `maintainability-structure` (single selection-sync seam), `testing` (COUNTED nav model).
- **Tests to add:** update the existing COUNTED `nav` tests — fixed order asserted for BOTH `needs_setup` states; `prominent_initial_id` unchanged semantics; render-smoke on the rail; a targeted test that `select_by_id`'s index mapping matches `ordered` (pure mapping fn if extracted).
- **Acceptance criteria:** rail order identical pre/post setup-completion; clicking "Start setup" from Home highlights Setup in the rail; Home fix-CTA highlights Run History; launch still lands on Setup when unconfigured; no reorder logic remains.
