# Handover — plan 0049 (machine-scoped install + gMSA principal), 2026-09-17

You are taking over an APPROVED plan at the start of its build. **Read in this order:** (1) this file; (2) `CLAUDE.md` (authoritative, loaded every session); (3) `.claude/plans/0049-machine-scope-gmsa.md` — the design of record, incl. its *Rebase notes* and *Review record*; (4) `.claude/plans/0046-HANDOVER.md` §1 (gotchas) and §5–§6 (verified facts + the preserved gMSA research — do NOT re-derive); (5) `docs/claugentic-DECISIONS.md` — the two 2026-09-17 entries at the top and the 2026-09-16 block (A8 run_highest · A10 delete-then-create · M1/S0 measurements · the Slice C alarm swap). Everything below is what the plan does not say.

## 1. Owner instructions (verbatim intent, 2026-09-17 — do not re-litigate)
- "yes revise plan 0046 into this shape" → the plan is approved (as v2; rebased to v3.21.0 as plan 0049 — the deltas are in its *Rebase notes* table). "okay lets begin the work."
- gMSA ships **Settings-only** with an on-screen "not yet tested against a live domain" caption. **No feature branch.** The caption flips only when a district reports a green nightly.
- **No CI live smoke.** The live proof is the owner's manual walk on their **domain-joined org laptop** (they will get a domain user and a gMSA from their IT). A read-only `--diagnose` command is the evidence instrument. Windows-only REAL tests (DPAPI LocalMachine, icacls read-back) DO run — S-1a adds the `windows-latest` pytest leg `ci.yml` lacks today.
- Another administrator on a machine-scoped install: **auto-grant on first launch** (one UAC prompt, clear reason). Identity is per-install ("we just match the domain and load that district's mappings; the new person can update the email in the app").
- Order: foundation (S-1a, S-1b) → machine-scope the shipped path (S-2) → engine principal model (S-3) → gMSA UI (S-4). Owner walks after S-2 and S-4.
- Models: **the orchestrator is Opus 5.** Implementers / critics / gates on `opus`; mappers, digests, refuters on `sonnet`; Fable only if the owner switches for a hard judgment. Every `Workflow` `agent()` call sets `model` explicitly. Conserve orchestrator context: never read a large workflow output directly — digest via a Sonnet agent (memory note `workflow-result-digests`); parse `journal.jsonl` results with `ast.literal_eval` and CHECK every mapper returned real findings (one returned `{'summary': 'test'}` today and nobody noticed until the journal was parsed).
- Process: keep the harness **artifacts and gates** (tree gate · DECISIONS · ROADMAP · land gate · DoD); make review depth **proportional** and your call per slice (the owner is weighing retiring the fixed pipeline). Per slice: JIT spec → a short adversarial pass sized to the slice (see §6) → **the owner approves a ~6-line plain-English block before any code** → implement → verify → PR → CI's own `test pass <duration>` line READ AND QUOTED → owner merges. The owner is fatigued by long messages: keep approval asks to those ~6 lines.
- **Refuter lesson (memory `design-review-refuters`):** when verifying findings about PROPOSED code, refuters must attack the reasoning, not check existence — today's panel "refuted" 14/16 design findings for not existing yet and those verdicts were discarded.

## 2. Repo state — FIRST ACTIONS
- `main` is at `8f5761e` (= `origin/main`, v3.21.0, fast-forwarded today). **Uncommitted on `main`:** the new `.claude/plans/0049-machine-scope-gmsa.md` (untracked), this file (untracked), and four edits (`0046-service-account-scheduled-task.md` status line · `0046-HANDOVER.md` SUPERSEDED banner · `docs/claugentic-ROADMAP.md` item ~272 · `docs/claugentic-DECISIONS.md` two top entries). All LF; `scripts/check_no_emails.py` OK on the edited tree.
- **First action:** `git checkout -b claude/0049-s1a-foundation` and commit those six paths as `plan(0049): machine-scoped install + gMSA — approved plan, handover, ledger pointers` (the 0047 pattern: plan commits ride the first slice's branch). The owner merges; never `gh pr merge`. Any PR you did not open is off limits.
- Gotchas that cost real time (full list in `0046-HANDOVER.md` §1): no `make` — copy the `Makefile`'s validate-config command inline · venv python is `C:/Users/shan.peiris/Documents/Integrations/DistrictSync/.venv/Scripts/python.exe` (the system python has pywin32; the venv does NOT — live COM/registry probes need the system one, tests need the venv) · a worktree has no `.venv` · full suite ≈ 15 min, background it (0-byte output = running) · CI ≈ 18 min, background `gh pr checks <N> --watch`, report `${PIPESTATUS[0]}`, `--fail-level` is a FALSE GREEN · `bandit -r src/ -q -c pyproject.toml` (the `-c` is required) · `.gitattributes` enforces LF · **long edits: write a `.py` to the scratchpad with the Write tool and run it — a multi-KB bash heredoc with mixed quotes dies** (memory `large-edits-via-script-file`) · never run the app/CLI without `DISTRICTSYNC_DATA_DIR` · pipeline output to a SHORT temp dir (MAX_PATH) · no email address in any repo file (first names + SD numbers) · a new `src/**/*.py` needs an `ARCHITECTURE_TREE.md` line or the commit is aborted.

## 3. The shipped surface you build on (v3.21.0; file:line verified 2026-09-17)
Engine — `src/scheduler/task_com.py`: `RegisterParams` (:207-224, 8 fields, no kind), `apply_definition` (:227-282, two branches keyed on `password is not None`), `_HRESULT_CANONICAL` (:144-151, six entries incl. `HR_NO_SUCH_LOGON_SESSION=0x80070520`, `HR_NONE_MAPPED=0x80070534`), `TaskFacts` (:328-339, no principal), `read_task` (:464-505, never reads `Definition.Principal`). `windows.py`: `register_task` (:366-378), refusal ladder (:447-467, `_MSG_ACCOUNT_NEEDS_PASSWORD`), elevation predicate `has_password and sys.platform == "win32" and not is_elevated()` (:475), `_register_elevated` payload (:726-736, 9 keys), `ScheduleReadback` (:862-890), `_fail` funnel (every refusal must go through it — `tests/test_scheduler_runas.py`'s AST guard sees only literal `return False, …`). `elevated_apply.py`: ops `register`/`delete` (:144-152), `_REQUIRED_REGISTER_FIELDS` (:56-58), `_do_register` (:168-196), `{ok,message}` result (:79-88). `elevation.py`: handshake files under **`paths.user_data_dir()`** (:242-259 — must be pinned per-user in S-1a), `_dpapi` CurrentUser-only (:132-172, entropy `DistrictSync/elevation/v1`), `_set_owner_only_dacl` icacls argv (:220-227). `src/scheduler/messages.py` = the marker vocabulary; any new canonical must be injective (`test_task_com.py` sweep) and marker-free. `validators.py:52` `_RUN_AS_USER_RE` (no `$`; docstring :135-163 cites N1 — update it).
State — `paths.py:173-221` ladder (override → platform → legacy → create; no ProgramData/HKLM/winreg anywhere in `src/`), `migrate_legacy_data_dir()` (:277, no args — leave it alone), `user_history_db()`/`user_log_file()`/`user_mappings_dir()` seams. `logger.py:28` sink = `user_log_file()`, RotatingFileHandler, 3 backups. `store.py` schema (:68-81, no `run_as`; `SCHEMA_VERSION=1`, `user_version` stamped only on a new DB — an additive `ALTER TABLE … ADD COLUMN run_as TEXT` + version 2). `uploader.py`: `KEYRING_SERVICE` (:51), `store_password`/`_get_password`/`get_stored_password` (:319-343), zip staged in `tempfile.TemporaryDirectory()` (:575). `pipeline._sftp_upload` (:992-1029) trusts `AppConfig.sftp_is_configured()` (`app_config.py:805-811`) — no secret check.
UI — `setup_flow.py`: `RegisteredSchedule(args, unattended, run_as_user)` (:532-559, atomic; principal never on `TaskArgs`), `SCHEDULE_ACCOUNT_FIELD_LABEL` (:526), `schedule_reconcile` via `principal_key`, `downgrade_interrupt` service-account variant, `_FOLDERS_SAVED_BLOCKED_ACCOUNT[_SWITCH]` (:870-885). `setup_gates.py`: `ScheduleAccountFacts`, `RegisterBlock{NONE,INCOMPLETE,RUN_TIME,ACCOUNT_SHAPE,ACCOUNT_NEEDS_PASSWORD,ACCOUNT_SWITCH_NEEDS_REMOVE}` (:108-118), `register_block`, `can_register_schedule`. `screens/setup.py`: `account_field` (:2058-2068, prefilled `cfg.schedule_run_as_user or _keyring_owner_account()`), `password_field` (:2071-2082), `_account_facts` (:2095-2114, strip-only), `_SERVICE_ACCOUNT_DELIVERY_NOTE` (:239-245, the on-screen `--sftp-configure` step), register call (:2341-2353), result branches (:2298-2337), `_keyring_owner_account` (:2758-2774, 8 call sites), the delivery line `"Your delivery password is saved and readable by {…}"` (:2903). `setup_errors.classify_schedule_error`: 14 branches in order (UAC-declined → … → `MSG_NO_LOGON_SESSION` GPO copy :151-167 → `MSG_ACCOUNT_INFO_NOT_SET` → `MSG_LOGON_FAILURE` (account-aware) → `MSG_ACCOUNT_NOT_RECOGNIZED` → `_MSG_ACCOUNT_NEEDS_PASSWORD` → `MSG_COM_UNAVAILABLE` → elevated-denial → access-denied → `_unclassified_copy` — "Try again in a moment" is GONE). `schedule_status.py`: `ScheduleStatus.foreign_account` (:212-231, required kwarg of `derive_schedule_status`), `_is_contradiction` (:397-431, `if foreign_account: return False`), `FOREIGN_RECORDS_NOTE` (:45-48), `run_result_verdict`. `home_status.py`: `_is_missed_run` (:1060-1092), `_foreign_records_elsewhere` (:1008-1034), `FOREIGN_PRINCIPAL_HEADLINE` (:183), `_expects_a_nightly` (:389). `run_history.py`: `_foreign_records_banner`; `sync_window_paused(..., foreign_account)`.
Docs — `docs/partner/headless-sftp-setup.md:275-427` "Running the nightly sync as a service account" (`runas /user:<account> "…DistrictSync-windows.exe --sftp-configure"` at :301-303; Credential Manager scope :292-298; rights :361-369; "why Run History goes quiet" :386-396; seasonal window :398-406; SYSTEM/gMSA out of scope :285-288). `installation.md:163-190` wizard Step 4 (unchanged by 0046). `troubleshooting.md` carries the 0047 copy. CI — `ci.yml` is ONE ubuntu job; `flet-pack.yml` has a `windows-latest` leg (exe smoke only).

## 4. Tests that MUST move deliberately (and what they protect)
- `tests/test_ui_flet_service_account.py` — the delivery note asserts `"@" not in note`; the "readable by" line is pinned VERBATIM (`f"Your delivery password is saved and readable by {_SIGNED_IN}."`); `test_the_old_name_is_gone`. S-2's four-form line replaces the pin with a positive twin per form.
- `tests/test_ui_flet_foreign_principal_wiring.py` — AST-pins every `probe_schedule(...)` call in `screens/home.py`, `screens/run_history.py`, `screens/setup.py`, `screens/mapping.py`, `shell.py` supplies `foreign_account=` via `foreign_task_account`, and `sync_window_paused(..., foreign_account=)`; bans "we pause"/"DistrictSync pauses"/"automatically" in the foreign window note. S-2 adds `shared_records=` beside it (extend the pins; never bypass) and a SEPARATE shared-records window note that may say the pause applies (because on a machine-scoped install it does).
- `tests/test_partner_doc_schedule_copy_parity.py` — 9 literals bidirectionally pinned between `classify_schedule_error` and `docs/partner/troubleshooting.md`; `installation.md`/`faq.md`/help-centre must declare ZERO quotes. S-4's GPO-branch pointer sentence moves both sides.
- `tests/test_task_com.py` injectivity sweep + `hresult_for` inverse; `tests/test_scheduler_runas.py` AST funnel guard; `tests/test_elevated_apply.py` DISPATCH-FIRST rule (nothing in `_cli`'s preamble may run elevated — the `provision` op must not trigger `pin_data_dir` side effects it does not own); `tests/test_scheduler_elevation.py` `WINDOWS_ONLY` real-DPAPI pattern (copy it for LocalMachine); `tests/test_paths.py` `data_dirs` fixture + `real_user_data_dir` marker; `tests/conftest.py` `pytest_configure` pops `DISTRICTSYNC_DATA_DIR` session-wide and `isolated_user_profile` redirects the ladder — add seams for `machine_data_dir`/`is_machine_scope`/the HKLM read, defaulting to OFF so 6,292 tests stay byte-identical.
- **Must NOT change:** `_keyring_owner_account` keeps meaning the KEYRING owner on a per-user install; the delivery line names the keyring owner there, never the task principal (DECISIONS 2026-09-16 A6). `elevation._dpapi` stays CurrentUser. `validate_run_as_user` keeps rejecting `$` (gMSA gets its OWN validator). `TASK_LOGON_S4U` stays undefined. `TaskArgs` shape.

## 5. Per-slice spec skeletons (acceptance criteria; the JIT spec adds the how)
**S-1a — foundation, inert.** `paths.machine_data_dir()`, `is_machine_scope()`, `pin_data_dir()`/`reset_data_dir_pin()`, `handshake_dir()`; HKLM read via `winreg` (frozen-exe safe; a read failure = switch OFF); owner + reparse checks via ctypes; the ladder rule (switch on ∧ invalid dir → `RuntimeError` naming the path; positive twin). `src/sftp/secret_store.py` (`UserSecretStore`, `MachineSecretStore`, `select_store()`, `has_secret(host, username)`, identity-bound blob, verify-or-destroy, `protect_machine_blob`/`unprotect_machine_blob` with a REQUIRED `flags` on the private helper). `uploader`/`pipeline`/`main --sftp-*` route through the store; `sftp_is_configured()` requires `has_secret` on machine scope. Zip staged under `runs/tmp` when machine-scoped. `logger` sink name `runs/etl_tool-<sanitised account>.log` when machine-scoped. `store.py` `run_as` column (additive, `user_version` 2; `write_run_record` gains `run_as`; `run_pipeline`/`convert_job` pass the process account). `ci.yml` gains a `windows-latest` pytest leg. Real Windows tests: LocalMachine round trip + flag assertion. **AC:** with the switch off, every existing test passes untouched and the SD74 snapshot is byte-identical; nothing in the field can turn the switch on.
**S-1b — provisioning.** `elevated_apply` ops `provision` (steps 1–7 of plan D4, fixed step vocabulary + icacls exit code only), `grant_current_user`, `prune_principal`; the migration function (config parse-then-write; `sqlite3.Connection.backup()` + `PRAGMA integrity_check` + row-count; mappings/known_hosts copy; NEVER `copytree` a live db); post-provision session steps (re-pin, reload `AppConfig`, re-point sink, rename per-user `config.json`/`history.db`+sidecars to `*.pre-machine-<ts>`, `MOVED.txt`); the auto-grant screen (`screens/grant_access.py`, one filled primary, `ErrorCard` floor); Remove-time prune leg on the elevated delete; `--diagnose`. **AC:** refuse a pre-existing untrusted dir · non-empty-after-create refusal · rollback before commit removes the dir · non-leak table covers BOTH secrets across result/message/log · `--diagnose` prints no secret value.
**S-2 — machine-scope the shipped path.** `RegisterBlock.DELIVERY_SECRET_UNREADABLE` + `FOLDER_NOT_SHAREABLE` gates; the three-sentence confirm before UAC; provisioning dispatched when the sent account is foreign; `_SERVICE_ACCOUNT_DELIVERY_NOTE` scope-aware; the four-form delivery line from read-back + scope; `shared_records` through `derive_schedule_status`/`_is_missed_run`/`_foreign_records_elsewhere`/both window notes; scope line on Settings + Home; `run_as` in Run History; `headless-sftp-setup.md` service-account section rewritten (the `runas` step retired on a machine-scoped install; the two limitations gone; the posture statement); `installation.md`; CHANGELOG. **AC:** on a per-user install every string and predicate is byte-identical; on a machine-scoped install no note claims records are elsewhere or the pause is off. Then the **owner's walk #1** (domain user, PASSWORD kind).
**S-3 — engine principal model.** `PrincipalKind` + `Principal`; `__post_init__` refusals; `validate_gmsa_account` (trailing `$`, charset, domain optional, refuses `<COMPUTERNAME>$`/`NT AUTHORITY\*`); elevation predicate by kind; payload `kind`; `read_task` reads `Definition.Principal.UserId/LogonType` → `TaskFacts`/`ScheduleReadback.run_as`/`logon_type`; cron adapter refuses MSA loud. **AC:** today's two callers byte-identical; every unrepresentable combination refused; requested-vs-read-back mismatch = WARNING only.
**S-4 — gMSA in Settings.** The disclosure beneath the account field (Settings mount only); caption + prerequisites + partner-doc link; MSA classifier branch keyed on kind (checklist, code on screen, no "try again"); one pointer sentence in the `MSG_NO_LOGON_SESSION` branch (+ `troubleshooting.md` + parity test); CHANGELOG "available, untested". **Kind-aware view paths (added 2026-09-17 from a cross-session gap check):** `ScheduleAccountFacts` gains `kind`; `register_block` must not fire `ACCOUNT_NEEDS_PASSWORD` for an MSA; `schedule_reconcile` and the `downgrade_interrupt` service-account variant need MSA copy without the word "password"; `principal_key` and `foreign_task_account` are name-keyed already (verified) and need no change. Then the **owner's walk #2** (gMSA) and the SD60 hand-off.

## 6. Review sizing (your call; these are the floors)
S-1a: security + resilience critics on the spec (HKLM/ownership/reparse/select-rule/handshake pin), honesty on any copy. S-1b: security + resilience (provisioning races, rollback, migration integrity) — the highest-risk slice. S-2: product + honesty (every note/line true in every scope × read-back state) + the wiring-test extension. S-3: security (the unrepresentable table) + YAGNI. S-4: product + honesty (the caption is the whole honesty story). Use a `Workflow` with explicit `model:` per agent; discard existence-check refutations of proposed code.

## 7. Owner-only items (ask; never do)
- IT ask for the laptop: a domain user with local admin; a gMSA with the laptop in `PrincipalsAllowedToRetrieveManagedPassword`, `Install-ADServiceAccount` run there, "Log on as a batch job"; whether the laptop GPO sets the credential-storage policy.
- The reply to SD60 (Jarrod): draft on request; **never send** from the owner's account.
- Every merge; every UAC click; the manual walks (plan `## Verification`); recording each walk's outcome in DECISIONS with `--diagnose` output as evidence.

## 8. Hard constraints (from today's review; violating any is a defect)
Never `/setowner` or adopt a directory the app did not create · never fall through from a set switch to a per-user profile · never both secret stores, never a fallback chain, never the machine store under `DISTRICTSYNC_DATA_DIR` · never a permissive default on a safety parameter (`flags`, `kind`, `shared_records`, `dry_run` are required keywords) · the principal gets RX at the root and M only on `runs/` · the setup user is granted explicitly (filtered-token rule) · the elevation handshake stays per-user and CurrentUser · no copy, CHANGELOG or doc may say gMSA is "supported" until a district's green nightly · nothing claims SD60's policy is fixable in-app · the app never orchestrates a principal switch (A10: Remove → Schedule) · `run_highest` stays HIGHEST for unattended (A8) · no email address in any repo file.
