# Runtime dependencies — needed to run the ETL pipeline
pandas>=1.4.0,<3.0
PyYAML>=6.0,<7.0
python-dateutil>=2.8.2,<3.0
pydantic>=2.0,<3.0

# Industry-standard per-OS user-data directory resolution (%LOCALAPPDATA% /
# ~/Library/Application Support / $XDG_DATA_HOME). Tiny, pure-Python, zero deps.
# A static `import platformdirs` in src/utils/paths.py, so PyInstaller detects it
# via static analysis — no --hidden-import needed (those are only for the
# dynamically-discovered keyring backends).
platformdirs>=4.0,<5.0

# SFTP upload (--sftp flag) and credential storage in the OS keyring.
paramiko>=3.0
keyring>=24.0

# Native Flet 1.0 desktop UI — the ONLY UI. Bundled into the .exe so launching
# DistrictSync with no args opens the Flet shell (src/ui_flet/launcher.py).
# Pinned EXACTLY — Flet 1.0 is a beta API that can shift; flet-web/flet-cli are
# dev/build-only (requirements-dev.txt) to keep fastapi/uvicorn out of the exe.
# Keep this version in lockstep with docs/FLET_1.0_CONVENTIONS.md (test_flet_pin gates it).
flet==0.86.2
flet-desktop==0.85.3
