# Roadmap (claugentic harness)

The prioritized backlog the claugentic harness works through. `/claugentic-dev-harness:audit` and `/claugentic-dev-harness:product` append their own fenced sections here; items in `## Later` are human-owned.

**Housekeeping 2026-07-16:** every item was re-verified against the code (done-ness evidence) and YAGNI-reviewed with partner distribution imminent. Resolved/stale items were closed (evidence in `claugentic-DECISIONS.md` 2026-07-16); the survivors live below. Landed plan files were deleted per the workflow's "plans are ephemeral" rule — `0032` stays as the detail source for the queued 0035/0036 batches.

## What this app is & does  _(generated by /claugentic-dev-harness:audit · do not edit — re-run to refresh)_

<!-- harness-audit:overview:start -->
_Inferred from project structure, manifests, and docs — not from running the app._

**What it is** — DistrictSync is a desktop/server **data-conversion tool** for school districts in British Columbia. It takes the raw student-information exports a district pulls from MyEducation BC and reshapes them into the specific CSV files that SpacesEDU / myBlueprint+ need to set up classes and rosters. It ships as a single double-clickable program (built with PyInstaller) so non-technical district staff can run it on a school server, often on an automatic daily schedule.

**What it does** — It reads up to a handful of district export files, cleans and reorganizes them, and writes up to seven output spreadsheets (students, staff, families, classes, enrollments, plus two optional course-catalog files). It can preview changes without writing, compare against the previous run, produce a data-quality report, and optionally deliver the finished files to SpacesEDU automatically over a secure (SFTP) connection. Each district's quirks (different file names, columns, email formats, calendar dates) are handled by per-district configuration rather than code changes.

**How it's built** — Python 3.13, using pandas for the data work and pydantic to validate the district configuration files. It's a classic ETL pipeline: **Extract** (read the export files) → **Transform** (entity-by-entity reshaping) → **Load** (write the CSVs). There's also a native desktop app (Flet) for staff who'd rather not use the command line, and built-in scheduling helpers for Windows and Linux.

**How it's organized** — The code lives under `src/`: the pipeline stages in `src/etl/` (with one focused transformer per output type under `transformers/`), configuration handling in `src/config/`, secure file delivery in `src/sftp/`, the scheduling helpers in `src/scheduler/`, shared safety/validation utilities in `src/utils/`, and the native UI in `src/ui_flet/`. Every district's mapping rules are plain YAML files in `config/mappings/`.

**Safety-net signals** — Strong. A large automated test suite (600+ tests) with an 80%-coverage gate, a "golden file" regression test that catches any accidental change to one district's output, configuration validation, and a full continuous-integration pipeline (lint, formatting, type-checking, a security scan, and the tests). The codebase also documents an explicit set of guarantees (atomic file writes, "no orphan" roster integrity, fail-loud error handling) and a clear exit-code contract.

**Confidence & caveats** — High confidence in the structural picture: this is a mature, well-documented, well-tested project with clear conventions. Because this overview is read from the code's structure (not from running the tool against real data), the audit that follows focuses on where the *standards* — especially **student-data privacy, data integrity, and reliable delivery** — could still be strengthened.
<!-- harness-audit:overview:end -->

## Backlog  _(generated by /claugentic-dev-harness:audit · re-run to refresh)_

<!-- harness-audit:backlog:start -->
_Housekeeping 2026-07-16: the 2026-06-25 audit backlog was verified item-by-item against the code — resolved items closed (#3 by plan 0034, #16 by 0029; #6's UI/store halves fixed, its log half is the accepted D2a decision), survivors merged into **Now** / **Later** below. Re-run `/claugentic-dev-harness:audit` to regenerate this section._
<!-- harness-audit:backlog:end -->

## Now — pre-partner batch  _(verified open 2026-07-16 · YAGNI-gated: bugs/PII/data-integrity that bite real partners in the first weeks; everything else deliberately waits)_

1. **Pin the SpacesEDU SSH host keys.** `[security · S]` `src/sftp/uploader.py:118-120` still trusts any server on first connect (`AutoAddPolicy`) — the host *allowlist* checks the name, not the identity, so a first-connect MITM can harvest the SFTP password + a zipped student-data file. Ship/pin the 3 known SpacesEDU host keys, switch to a rejecting policy, drop the `# nosec B507`. _(audit #1)_
2. **Make the anomaly check see a vanishing roster file.** `[bug · M]` An entity that transforms to zero rows never enters `outputs`, so `compute_anomalies` (`src/etl/pipeline.py:214`) can't flag a present→absent / N→0 transition, and an unreadable previous CSV is silently skipped (`:222-224`). Also diff against previous-run CSVs on disk; fail loud on an unreadable baseline; add the pinning test. _(audit #2)_
3. **Broken email-template config must surface as data errors.** `[bug · S]` `base.py:710-712` swallows a template `KeyError` to a log warning and blanks every generated email — invisible to the operator. Route it through the existing `_record_data_error` so it reaches "Completed with N data errors". Live partner path: SD60 generates learn60 emails. _(audit #9)_
4. **Pin the output-CSV contract for every launch district.** `[test · M]` `tests/test_contract.py:191-195` covers only myedbc/sd48/sd74 — sd40 (headerless schedule), sd51, sd54 (field-remapped), and sd60 are exactly where a per-district override could silently change a column SpacesEDU ingests. Extend `_DISTRICT_SETUP`. _(audit #12)_
5. **`convert_job` archives stale entity CSVs like the pipeline does.** `[data-integrity · S]` Only `run_pipeline` calls `archive_stale_outputs` (`pipeline.py:554`); the manual convert path never does — so after a config/tier change, deliver-from-disk would ship a stale CSV from the old config. Call it from `convert_job` too. _(0034 slice-2 residual)_
6. **Verify the `PreRegSchoolCode` → "Next school code" header against a real GDE.** `[data-verify · S]` The mapping silently blanks if the real column is named differently — flagged ⚠️ PENDING in DECISIONS. Needs a real district extract header (owner has them); then clear the flag. _(in-house process item)_
7. **Bounded SFTP retry.** `[resilience · S]` One transient network blip = exit 3 and no delivery until a human notices. Wrap connect+put in ~3 attempts with backoff (`uploader.py:121-127`); date-stamped zip names make retry idempotent. In-scope per the LIGHT "retries/backoff (SFTP only)" standard. _(audit #4 — lowest priority of this batch)_

## Queued feature batches  _(owner-approved program, DECISIONS 2026-07-15 — detail lives in [`.claude/plans/0032-ui-ux-sweep-proposal.md`](../.claude/plans/0032-ui-ux-sweep-proposal.md))_

- **0035 polish batch** — the 0032 items not yet executed by 0031/0033/0034: banner-copy honesty pass (T1 #1) · no-dead-end failure copy (#2) · plain-language vocabulary sweep (#6) · Convert cold-state fixes (#7) · setup-badge re-probe after register (#8) · About/support context + copy buttons + open-log-folder (#9) · interaction-state sweep (#10) · Firefighter fix-routing (T2 #4) · window-geometry persistence (T2 #8).
- **0036 bigger bets (discovery-first)** — startup/installer spike · keyboard + UNC paths · "Check My Setup" self-diagnostic + PII-safe diagnostics export. **NO email alerting** (owner decision).
- **Mapping transparency (read-only)** — re-scoped epic (editor DROPPED): plain-language per-entity detail + a "request a change" support path + internal capability-catalog docs. Build only if prioritized.
- **Code-debt batch** — `student_courses.py` config-driven source columns (+ derive `OUTPUT_COLUMNS` from the field_map); the T1/T2 refactors below.

## Later  _(verified still open 2026-07-16; post-launch)_

**Correctness & resilience (small):**
- Same-second staging/backup dir collision — add a PID/uuid suffix + `exist_ok=False` or an output-dir lock (`loader.py:71-74`). _(audit #7)_
- Hard-kill torn-output reconcile + aged `.tmp_`/`.bak_`/`archive_` sweep on startup. _(audit #8 · M)_
- `_connect` leaks the paramiko client on a failed Test Connection — close before re-raising (`uploader.py:96-129`). _(audit #10)_
- School-year disagreement across sources silently picks the first — collect + warn (`base.py:762-768`). _(audit #14)_
- Surface schedule students absent from the demographic as a quality warning (silently dropped today; large gaps already trip the >20% alarm) — fold into 0035 quality-signal work. _(in-house process item)_

**Config & data (owner input where noted):**
- Config `version` field is never checked and a stale user-dir config shadows the bundled one — validate a supported range in `load_config`. _(audit #11 · M)_
- StudentCourses letter/"Pass" marks score as fails, nulling earned credits — **owner: is legacy-PowerShell parity intended?** Promote if a letter-grade myBlueprint+ partner joins; minimally record a data_error on non-blank non-numeric marks. _(audit #15)_
- Family.csv isn't filtered to the active roster (zero-orphan invariant covers Enrollments/Classes only) — **verify SpacesEDU's tolerance of orphaned Family rows first**, then decide. _(T3.11 · S)_
- StudentCourses active filtering — **owner decision**: should inactive/graduated students' transcripts be emitted? _(T3.12)_

**Test & dev hygiene:**
- SHA-pin all GitHub Actions (every action is a mutable `@vN`; do `softprops/action-gh-release` first). _(security hygiene)_
- Delete the fork branch `ci/flet-xplatform-verify` (owner-only): `git push https://github.com/sh4npeiris/DistrictSync.git --delete ci/flet-xplatform-verify` — the spike dir itself was deleted in the pre-partner batch. _(CUT-1 remainder.)_

**Refactor program (owner-queued after 0035/0036; landing a large refactor pre-launch is risk, not value — YAGNI verdict 2026-07-16):**
- T1.1 typed-config field Strategy (L — kills `classify_field`/`to_raw_dict` round-trip; subsumes T3.10 idempotence nit) · T1.2 debloat `BaseTransformer` + fix the `BlendedClassDetector` LSP smell (L) · T2.1 typed `ClassArtifacts` handoff (M — KISS-check when picked up) · T2.2 single `config.active_entities()` for the 4 duplicated selection sites (M) · T2.3 Scheduler Protocol + factory (M — platform dispatch now at ~17 sites) · T2.4 validate-at-boundary finish (M — `astype(str).str.strip()` grew 22→33 sites; dead `check_required_entities`) · T3.1 `EnrollmentSource` strategies (M — KISS-check) · T3.2 decompose `BlendedClassDetector.detect` (S) · T3.4 hoist the grade→CEDS→homeroom split (S, 4 sites) · T3.5 delete dead `helpers.py` trio + re-home zip helpers (S) · T3.8 `resolve_column()` helper + one shared `DATE_FORMATS` (S) · T3.9 document `_deep_merge` list-replace semantics (S).

**Owner-deferred (explicit decisions, not debt):**
- Email/push alerting on failed **or missed** nightly runs — the pull-cockpit ceiling stands by owner choice (2026-07-15: "ignore email alert for now"; 0036 queue says NO email alerting). Revisit when a real support load justifies it.
- Service-account / machine-scope secret storage (SYSTEM/gMSA task principals; non-keyring secret sources) — enterprise-scope, L.

**Trigger-armed:**
- Upgrade Flet to stable 1.0 when it leaves beta (still `0.85.3` everywhere): bump + re-pin → UI suite + exe smoke + manual graceful-shutdown → update `FLET_1.0_CONVENTIONS.md` → re-verify packaging.
