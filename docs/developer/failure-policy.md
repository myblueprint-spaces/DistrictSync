# DistrictSync ETL failure policy

| | |
|---|---|
| **What this is** | The ONE written rule set for how the ETL fails: at what scope, for which entity, in which direction, with which bounded reason, and what the admin sees. Read it before adding a check, an entity or a config knob. |
| **Plan** | `.claude/plans/0053-etl-failure-policy.md` (slices S0–S15). Decisions: `docs/claugentic-DECISIONS.md` 2026-09-23. Register: one `docs/claugentic-INVARIANTS.md` row per rule P1–P16. |
| **Verified against** | commit `8d33664` (v3.25.0). Every code `file:line` below was read at that commit, except the rows plan 0053 S1–S13a flipped (2026-09-24/25), which cite symbols so they do not drift; where an S10-flipped row still carries a `file:line` beside its symbol, that line is the historical `8d33664` site the row was catalogued at, and the symbol is the current one. Citations into docs this change also edits (`faq.md`, the ROADMAP) are by question heading or item title, never by line, so they do not drift. |
| **Pinned by** | `tests/test_failure_policy_parity.py` (since S2): the §3 criticality table == `outcomes.ENTITY_CRITICALITY` + `DEPENDS_ON`, and the §6 vocabulary table == every member of the enums it lists, each with a non-vacuity check and a doctored-doc twin; since S3, every §6 member also has copy (`failure_copy.FAILED_CATEGORY_COPY` / `OUTCOME_TIER` / `outcome_sentence`). Since S8 the §7 `may-be-empty` table == `outcomes.MAY_BE_EMPTY` and the §7 `outcome-tier` table == `failure_copy.OUTCOME_TIER` over every valid (kind, reason), each with doctored twins. Since S4 it also ties `docs/partner/faq.md` to the code: the FAQ's two criticality bullets name exactly §3's CRITICAL and ISOLATABLE sets, and the isolatable one carries the "pending confirmation" clause exactly while `output-contract.md` says `Q5-status: open`. Since S10 the §5 `require-columns` table == every `columns.require_columns` call under `src/` (site, entity, guard — by AST, both directions), each of those calls carries its `# failure-policy:` tag in the comment block directly above it, and every §5 number the table cites is a catalogue row, each with doctored twins; and (owner ruling 2026-09-25) the §6 vocabulary table lists `OutcomeNote`, every note has a `NOTE_TIER` verdict and copy, and the §7 `note-tier` table == `failure_copy.NOTE_TIER`. Since S11 every `# failure-policy:` tag under `src/` is in a full bijection with the §5 site catalogue (the `tags` and `untagged-sites` tables, both directions, class-checked), every `notes.record_note` call is tagged, and every `OutcomeNote` member has a producer under `src/etl` — each with doctored twins. Since S13a `tests/test_architecture_fitness.py` pins §11's structural rules by AST (each with a detector self-test on synthetic violating source and a declared target registry that fails loudly on a missing path), and §11's `ble-reasons` table == its `BLE_REASONS` vocabulary, with doctored twins. Every other row is held true by review. |
| **Style** | Tables first, graded like `docs/developer/output-contract.md`. Every rule row says what the code does TODAY and carries a Status. |

---

## §0 Status legend

| Status | Meaning |
|---|---|
| `ENFORCED` | Today's code behaves as the row states, at the cited line. |
| `PLANNED (0053 Sn)` | The row states the TARGET. Its *Today* cell states what the code actually does now. Slice `Sn` flips the row to `ENFORCED` **in the same change** that makes it true (P14). |

A row may never say `ENFORCED` for behaviour that does not exist. A slice touches only its own rows.

## §1 The asymmetric-risk rule (P1)

Every posture below derives from this rule. It is the tie-breaker when two rules seem to disagree.

| Rule | Statement | Today | Status |
|---|---|---|---|
| **H1** — never widen delivered PII | A scoping rule that cannot be applied fails **CLOSED** at the smallest scope containing the fault. | Every PII-scope guard fails closed (`base.apply_row_filters`, `grades.filter_to_grade_scope`, and since S10 the homeroom/subject grade splits — all through the one check `columns.require_columns`, §5), and since S4 at the smallest scope §3 allows: an ISOLATABLE entity's guard (Family's guardian `row_filters`) leaves only that entity out — everything else ships — while a CRITICAL entity's (Staff's `row_filters` at SD83, the Students grade scope) still fails the run (`pipeline.run_transform`'s bulkhead). Two fail-OPEN H1 exposures remain: `base.py:295-301` ships every student Active when neither a status nor a withdraw-date column exists, and `filter_to_active` returns the frame unfiltered on an empty roster OR when the frame lacks its student column (`base.py:366-368`) — the first only when the Students output has no `User ID` column (`students.py:46-47`, §5 #27), the second e.g. a contacts export without its student-number column (`family.py:33`), which sends every contact, withdrawn students' included. Both are (e) safety heuristics failing open in the surplus direction; `filter_to_active` never filters to empty by design (`base.py:351`). Since S11 both are RECORDED, never silent: the all-Active default as `all_active_default` — a WARNING-tier note, so the run is amber every night it persists — and the unfiltered frame as `active_roster_unavailable` / `active_roster_column_unresolvable` on the consuming entity (Run History row detail). The likelier roster fault was the OPPOSITE direction: a missing User ID SOURCE column published the roster `{'<NA>'}`, which the empty-roster guard does not catch, so every downstream student row was dropped (an H2 shrink); since S10 that column is required and Students fails typed instead (§5 #27(ii)). | `ENFORCED` scope (S4) · both exposures recorded (S11) · the all-Active default's direction `PLANNED (D10)` |
| **H2** — never silently shrink or omit a deactivating file **because of a detected fault** | The run fails; the shrunken file never ships. | A detected fault in a CRITICAL entity fails the run with nothing written. Since S10 that includes every missing LINKING column §5 gives S10 — a present source that lacks one raises a typed `SourceSchemaError` (`columns.require_columns`) where rows used to be left out in silence (the staff rescue #22a, timetable students #28, the whole roster #27(ii)) — and the one partial ship is gone: `enrollments._homeroom_enrollments`' `except (KeyError, MergeError)` is deleted (#10). A detected fault in an ISOLATABLE entity (S4) never ships a SHRUNKEN file either: that entity's whole file is left out and the run is PARTIAL — an omission the owner accepted per entity in D1 (§3); whether an ABSENT file deactivates anything in SpacesEDU is Q5, open. StudentAttendance's and StudentCourses' keys (§5 #33/#34/#34a, isolatable entities) still ship what they read — since S11 with ONE WARNING and an outcome note, never silent; failing them is a direction decision. **One owner-approved exception, never silent (ruling 2026-09-25):** a present ClassInformation that lacks a co-teacher column (§5 #15) ships `Enrollments.csv` WITHOUT those co-teacher rows — a shrink H2 would otherwise forbid — with one WARNING and the outcome note `coteacher_source_unusable`, which keeps Home and Run History amber every night it persists (§7 `note-tier`). | `ENFORCED` (S10 — every site §5 gives S10; the #15 exception by owner ruling) · #33/#34/#34a recorded (S11), direction open |
| **H3** — surplus or harmless rows | Fail **OPEN**, with a signal RECORDED on the run. | Direction correct at `staff.py:281-299` (keep all staff rather than empty `Staff.csv`), and since S11 the signal reaches the run record: every (e) surplus pass-through records an `OutcomeNote` on its entity's outcome (§5 `tags` table), shown in its Run History row. | `ENFORCED` (S11) |
| **H4** — an optional value is blank | Blank the value; record it. | A mapped column absent from the source is still blanked by the field-map engine (`base.py:753`), and since S6 the source observation (§10) records it: one WARNING per entity naming the column in config spelling, and the column on that entity's outcome as `missing_mapped` — whenever every file the entity reads was observed with a header row. A blank VALUE in a present column is a per-row data fact, not a column posture, and gets no note (S11 decided; the `--quality` report's >50%-missing check is its signal today — ROADMAP). | `ENFORCED` (an absent column, S6) · a blank value: ROADMAP |
| Deliberate exclusions | Departed staff, plan 0052's un-roled staff and withdrawn students are **configured or heuristic** exclusions, not detected faults: H2 does not reverse them. They stay under the existing >20% drop anomaly (`pipeline.py:492`) and the delivery-integrity gate. | As stated. | `ENFORCED` |

## §2 Layer stack and scope ladder

| # | Layer (each owns one question) | Today | Status |
|---|---|---|---|
| 1 | Config parse — is the mapping well-formed? | Pydantic at load; the pipeline records `config` and exits 1 (`pipeline.py:869-881`); Convert raises the typed `ConfigLoadError` and records `config` too (`convert_job`, S5). Since S12 a typo is loud, by origin: an unknown `GlobalConfig`/`EntityConfig` key or `source_columns` role RAISES `loader.UnknownConfigKeyError` for a bundled config and WARNS one line per key for a user-dir one (`loader.unknown_config_keys`); an unknown key inside a field mapping is refused for every origin (§6). | `ENFORCED` (S12) |
| 2 | Extract — could each file be read? | Missing file → empty frame + ERROR log (`extractor.py:131-135`); unparseable (or a case-insensitive name collision) → `ExtractionError`, an `EtlError` whose class category is `input_unreadable` (`extractor.ExtractionError`), which fails the run with that category. | `ENFORCED` |
| 3 | Source observation — does each entity's OWN file carry its mapped columns? | `pipeline.observe_source_columns`, called by BOTH entry points after the input is read and before the transform (AST-pinned): per entity, `preflight.missing_columns_by_entity` compares its `field_map` + `row_filters` columns with its own files' headers; the misses are logged (one `MAPPED COLUMNS MISSING` WARNING per entity) and carried on the outcome as `missing_mapped`. Advisory — see §10. | `ENFORCED` (S6) |
| 4 | Per-entity transform — did this entity build? | Every configured entity gets exactly one outcome per run in an `outcomes.OutcomeLedger` (`pipeline.run_transform`): EMPTY with its reason or BUILT with its row count at the existing branches. A raise is recorded FAILED and decided by §3 at the ONE bulkhead: an ISOLATABLE entity's data errors are rolled back, one `ENTITY NOT BUILT` ERROR line is logged with the traceback, and the loop continues; a CRITICAL entity marks every later one NOT_RUN and re-raises the SAME object. If nothing was BUILT and something FAILED, the first isolated exception is re-raised (its own category, never `no_output`). `BaseException` is never caught. | `ENFORCED` (recording S2, isolation S4) |
| 5 | Delivery-integrity gate — can this output set be vouched for? | `check_delivery_integrity` (`pipeline.py:404-470`): `no_output`, `incomplete_roster`. Unchanged by this plan. | `ENFORCED` |
| 6 | Atomic write | `save_all` backup-and-restore commit; a write `OSError` → `OutputWriteError` (`pipeline.py:1002`). | `ENFORCED` |
| 7 | Deliver | Manifest = files this run wrote (`pipeline.py:1026`, `uploader.py:512-521`). | `ENFORCED` |
| 8 | Run record | Flat per-entity counts + `status` + `error_category` + `entity_outcomes` (`pipeline.build_run_record`; the outcomes key since S2, `None` only when no ledger existed). Exactly one per build attempt on both entry points — Convert's raised attempts since S5 (§7). | `ENFORCED` |
| 9 | Copy mappers | ONE copy source, `src/ui_flet/failure_copy.py`: a failed record's detail on Home (`home_status.failed_detail`) and Run History is its `error_category`'s `FAILED_CATEGORY_COPY` entry, Convert's category-FAILED statuses read the same table, and Convert's `on_error` card is `error_card_copy(exc)` — the exception's category by TYPE. Only `no_input` / `input_unreadable` name the input folder. The closing tail says "nothing was sent" only where the surface can prove it (Convert: delivery was requested; a record: an upload was attempted and failed), else "nothing new was saved". A FAILED entity in a completed run is `LatestReason.PARTIAL` (WARNING) on all three — produced since S4 by the entity bulkhead. | `ENFORCED` |

| Scope (narrow → wide) | Who may choose it | Today | Status |
|---|---|---|---|
| cell | The field-map engine, per row (`base.py:765-800`) — blank that cell, record to `data_errors` | As stated. | `ENFORCED` |
| column | The field-map engine, column-level (`base.py:755-760`) — blank the column, record | As stated. | `ENFORCED` |
| entity | **Only** `run_transform`'s one boundary, and only for an ISOLATABLE entity (§3) | As stated (`pipeline.run_transform`, S4); AST-pinned as the one broad handler there (§11). | `ENFORCED` |
| run | A CRITICAL entity's raise; no usable input; config, extraction, integrity or output fault | As stated; also an isolated failure in a run that built nothing (re-raised as itself). | `ENFORCED` |

Transformers RAISE and never catch-to-continue at entity scope; only the orchestrator widens a fault (P2). No broad handler exists under `src/etl/transformers` outside the field-map engine (AST-pinned since S4; the pin moved, unchanged in strength, to `tests/test_architecture_fitness.py` rule (d) in S13a). The one NARROW catch-to-continue — `enrollments._homeroom_enrollments`' `except (KeyError, MergeError)`, which shipped whatever homeroom rows were built before the error — was deleted in S10; no handler under `src/etl/transformers` catches `KeyError` or pandas `MergeError` except the email template's per-row isolation (cell scope, recorded to data errors), AST-pinned with a doctored twin in `tests/test_require_columns.py`. `ENFORCED` (S10).

## §3 Entity criticality (P3)

**Status of this table: `ENFORCED` — as a declaration since S2 and as behaviour since S4.** It is the code's `outcomes.ENTITY_CRITICALITY` + `outcomes.DEPENDS_ON`, row for row (`tests/test_failure_policy_parity.py`), and `pipeline.run_transform`'s bulkhead is the one place that branches on it: an ISOLATABLE entity's raise leaves that entity out and the run completes (PARTIAL, exit 0); a CRITICAL one fails the run as before (pinned per entity by `tests/test_pipeline_entity_isolation.py`). Decided by the owner 2026-09-23 (D1). `depends_on` lists CODE dependencies on another entity's published context state only.

<!-- failure-policy-table: criticality -->
| entity | criticality | rationale | promotion evidence | depends_on |
|---|---|---|---|---|
| Students | CRITICAL | The roster anchor (`outcomes.ROSTER_ANCHOR_ENTITY`); publishes `context.active_student_ids` (`students.py:47`); a user missing from a delivered `Students.csv` is marked Inactive (`faq.md`, "What happens to students or staff no longer in the file?"). | none — the anchor is never promoted | — |
| Staff | CRITICAL | What happens to a user missing from a delivered `Staff.csv` depends on the district's import settings (`faq.md`, "What happens to students or staff no longer in the file?"); what an ABSENT file does is unknown (Q5a). | would need the Q5a answer + a DECISIONS entry | — |
| Family | ISOLATABLE | Unity 2026-09-22: a report flip under the same filename failed the whole roster. Absence already ships (`tests/test_pipeline_delivery_integrity.py:581-590`; SD51 built no `Family.csv` while Family was enabled on its email-less contacts export — off in its config since 2026-09-25); publishes no context state; no entity reads it. Whether a guardian missing from a delivery is unlinked is open (Q5e). | absence-already-ships evidence + owner decision D1 (2026-09-23) | Students |
| Classes | CRITICAL | Publishes `context.class_artifacts` (`classes.py:49`), which Enrollments requires (`enrollments.py:37-45`). | none while Enrollments is CRITICAL | Students |
| Enrollments | CRITICAL | Whether a user missing from a delivered `Enrollments.csv` is removed from the class depends on the district's import settings (`faq.md`, "What happens to enrollments no longer in the file?"). | would need the Q5a answer + a DECISIONS entry | Classes, Students |
| CourseInfo | ISOLATABLE | isolatable by owner decision 2026-09-23, ahead of partner evidence; Q5d open. A standalone feed (output-contract *Delivery envelope*); publishes no context state; no entity reads its output. It may ship without StudentCourses. | owner decision D1 only — S14 may demote it on the Q5c/Q5d answer | — |
| StudentCourses | ISOLATABLE | isolatable by owner decision 2026-09-23, ahead of partner evidence; Q5d open. Reads the CourseInformation SOURCE file, never the CourseInfo output (`student_courses.py:112-114`); publishes no context state. It may ship without CourseInfo. | owner decision D1 only — S14 may demote it on the Q5c/Q5d answer | Students |
| StudentAttendance | ISOLATABLE | An unmapped absence code raises (`student_attendance.py:349-353`) and today kills rostering — which output-contract.md:558 says a missing attendance drop must never do. Publishes no context state; nothing reads it; absence already ships on nights without absence files. | absence-already-ships evidence + owner decision D1 (2026-09-23); Q5c open | — |

**Rules.** An entity not in this table is CRITICAL (`criticality_of`, S2). An entity that any other entity `depends_on` must be CRITICAL. **Promotion** to ISOLATABLE needs all of: (a) a recorded partner answer OR evidence that its absence already ships; (b) it publishes no `TransformContext` state; (c) no CRITICAL entity reads its output; (d) its dependency is declared here; (e) a dated DECISIONS entry. **Demotion** (S14) is one entity per change, with a contract fixture and a DECISIONS entry.

**Footnote (Q5).** CourseInfo and StudentCourses are ISOLATABLE by OWNER DECISION, not by partner evidence. Whether the two must arrive together is Q5d in `docs/developer/output-contract.md` — open. Until answered, each may ship on a night the other failed.

**Evidence (2026-09-24).** The owner reports that the import follows a hierarchy (users alone; Enrollments need Classes; Family needs users) — consistent with the CRITICAL set and DEPENDS_ON; Unity's association-removal setting is OFF. None of it answers Q5; see "Evidence so far" in `docs/developer/output-contract.md`.

## §4 Never substitute (P4)

| Rule | Today | Status |
|---|---|---|
| A FAILED or EMPTY entity is never replaced: no last-good CSV, no unfiltered rows, no partial frame, no filter-less mapping. | An EMPTY entity is skipped; an ISOLATABLE entity that FAILED is left out of `outputs` whole — nothing substituted, its rows in no output file (S4, the Unity plain-report contract fixture); a CRITICAL raise fails the run with nothing written. The one partial frame that shipped — the homeroom enrollments built before a caught `KeyError` — is gone since S10 (§5 #10). | `ENFORCED` (EMPTY / FAILED, S4; partial frame, S10) |
| Its previous CSV leaves the delivery glob. | Stale CSVs are moved to `archive_<ts>/` (`pipeline.py:1010`); the manifest is this run's files only (`pipeline.py:1026`). | `ENFORCED` |
| Its absence is reported on every run it persists. | A completed run whose own record has a FAILED outcome is PARTIAL / WARNING on Home, Run History and Convert, derived from that record alone (S3), and a delivery-only record inherits its build's outcomes (`home_status.build_record_for`). Since S4 the bulkhead produces that record; the vanished-entity anomaly fires only the first night (the next night has no previous CSV to compare against), and the second consecutive night is still PARTIAL (pinned, `tests/test_pipeline_entity_isolation.py`). | `ENFORCED` |

## §5 Missing-column matrix (P5) — keyed on what the column GUARDS

| Class | The column guards | Required posture | Today | Status |
|---|---|---|---|---|
| (a) `pii_scope` | WHO may be delivered (row_filters, grade scope) | Fail CLOSED; typed `SourceSchemaError(guard=PII_SCOPE)`; entity scope if ISOLATABLE, run if CRITICAL | Fails closed at the entity's declared scope (S4), through ONE check since S10: `columns.require_columns`, which names every missing column in the config's spelling, the source's column COUNT and a `header_looks_like_data` flag — never a header. Sites #1, #4, #5 (the grade at each of the homeroom/subject splits' four callers) and #6 (whose column is the district's own since S9) | `ENFORCED` (entity scope, S4; #6, S9; every site through `require_columns`, S10) |
| (b) `join_key` | An identity or join (merge keys, class artifacts, home school) | Fail CLOSED, typed; never a raw `KeyError`, never a partial result | Every (b) site the catalogue gives S10 raises the typed `SourceSchemaError(guard=JOIN_KEY)` through `columns.require_columns` (the `require-columns` table below): #2, #2a, #3, #8, #9, #10, #13a, #15 (ClassInformation's school only — its co-teacher columns are optional by owner ruling 2026-09-25, see #15), #22a, #27(ii), #28, #29, #30, #36 and #39. No raw `KeyError`, and the one partial ship is deleted. A guard runs only once its source is present and non-empty and the column is about to be read — an absent optional file is not a missing column. Since S11 the (b) reads whose direction is still open are RECORDED, never silent: an identity field the field-map engine blanks (#19, `identity_field_blanked`), StudentAttendance's keys (#33, `attendance_source_column_absent`) and StudentCourses' (#34/#34a, `transcript_source_column_absent`) — one WARNING + one note each; failing them closed is a direction decision | `ENFORCED` (S10) · #19/#33/#34/#34a recorded (S11), direction open |
| (c) `contract_field` | An output field the importer requires | Rows lacking it are excluded and COUNTED; all excluded ⇒ EMPTY with a recorded reason | Family's email exclusion conforms (log count); an all-excluded entity is recorded EMPTY/`no_rows_after_transform` (S2) and, since S8, is a standing WARNING on Home, Run History and Convert (§7, D5); since S11 the exclusion COUNT reaches the record (`contacts_excluded_no_email`, #20) and a mapping with no email output column records `email_output_not_mapped` (Family #20, Students #40) | `ENFORCED` (EMPTY + reason, S2; surfaced, S8; recorded count, S11) |
| (d) `optional_field` | A value that may be blank | Blank + one aggregated WARNING + an outcome note | A mapped column ABSENT from the entity's own file(s): blank + one WARNING per entity + `missing_mapped` on its outcome (S6, the source observation). The ClassInformation co-teacher columns (#15, owner ruling 2026-09-25): the co-teacher rows are left out with ONE WARNING and the outcome note `coteacher_source_unusable` — the first note. Since S11 every other (d) site records ONE WARNING + one note: blended lookups (#16/#31, `blended_lookup_column_absent`), the homeroom teacher name (#35, `homeroom_teacher_name_absent`), the subject class-name columns (#37, `class_name_column_absent`). A blank VALUE in a present column is not a column posture and gets no note (H4, ROADMAP) | `ENFORCED` (absent column: WARNING + `missing_mapped`, S6; #15's note, S10; the other notes, S11) |
| (e) `safety_heuristic` | A fail-open safety filter | Fail OPEN only in the surplus direction; always recorded | Every (e) site records ONE note and at most ONE log line per entity per run (§5 `tags` table): #12, #13, #14/#27(i), #17, #18, #21, #32. Directions unchanged — the all-Active default's (#18) is owner decision D10; its note is the one WARNING-tier (e) note (H1) | `ENFORCED` (S11) |

**Site catalogue.** One row per site. Since S11 it is in a bijection with the `# failure-policy: <class>` tags under `src/` (the `tags` table below, pinned both ways): every tag cites the rows it implements, every row is cited by a tag or listed in `untagged-sites` with its reason. A site found later is added with its class, its tag and its row in the same change.

<!-- failure-policy-table: sites -->
| # | Site | Class | Today | Status |
|---|---|---|---|---|
| 1 | `base.apply_row_filters` | (a) | Checks EVERY filter column first (`columns.require_columns` since S10); one `SourceSchemaError(guard=PII_SCOPE)` names all missing ones in config spelling + the source's column count, never its headers. Entity scope for Family (ISOLATABLE — the Unity plain-report fixture), run scope for Staff (CRITICAL — SD83's `Prefix`) | `ENFORCED` |
| 2 | `StudentTransformer._collapse_cross_enrollment` — the `home_school_column` check | (b) | `SourceSchemaError(guard=JOIN_KEY)`, config spelling + column count; since S10 in ONE `require_columns` with #2a's two columns | `ENFORCED` |
| 2a | `StudentTransformer._collapse_cross_enrollment` — the SchoolCode / User ID source columns (`working[school_col]`, `sort_values`/`drop_duplicates` on `user_id_col`) | (b) | Both resolve through `columns.resolve_source_column` since S9, so a `{column: …}` entry names its column (it used to become the text of a dict); since S10 both are checked with #2's home-school column by ONE `require_columns` before any row is touched — an absent one is a typed `JOIN_KEY` error, no longer a raw pandas `KeyError` | `ENFORCED` (S10; resolution S9) |
| 3 | `StudentTransformer` email `derived_dates` column check | (b) | `SourceSchemaError(guard=JOIN_KEY)`, config spelling + column count; since S10 every derived-date column is checked at once (`require_columns`) | `ENFORCED` |
| 4 | `grades.filter_to_grade_scope` | (a) | `SourceSchemaError(guard=PII_SCOPE)`, the grade column in the config's spelling (`columns.source_column_label`, S9 — so S7's `safe_label` can name it; the default `grade` when the mapping names none) + column count (was a raw `KeyError` listing every header; `require_columns` since S10). Run scope — its only caller is Students, which is CRITICAL (§3) | `ENFORCED` |
| 5 | The grade column `grades.split_by_homeroom_grades` reads (`df[grade_col]`) | (a) | Checked by `require_columns` (`PII_SCOPE`) at each of its four callers before the split reads it: Classes' and Enrollments' homeroom splits (the Students mapping's `Grade`) and subject splits (the Classes mapping's `Grade`, `grades.schedule_grade_label`). The function's own pandas `KeyError` stays as the last line of defence, unreachable from the pipeline | `ENFORCED` (S10) |
| 6 | `StudentTransformer._filter_to_rostered_grades` — the grade column for the student scope | (a) | Resolved by `columns.resolve_source_column`: a bare-string rename is honoured (the retired `resolve_column` ignored it), and an absent column raises #4's typed error | `ENFORCED` (S9) |
| 7 | `enrollments.py:37-45` class artifacts absent | (b) | Raises with an actionable message | `ENFORCED` |
| 8 | `ClassTransformer._merge_course_and_staff` — the course merge `course_df[[SCHOOL_NUMBER, COURSE_CODE, COURSE_TITLE]]` | (b) | With CourseInformation present and non-empty, both sides are required (S10): its school, course code and title, and the schedule's school and course code (after the district-course-code rename). Was a raw pandas `KeyError` | `ENFORCED` (S10) |
| 9 | `ClassTransformer._merge_course_and_staff` — the staff merge `staff_df[[teacher_id_col, LAST_NAME]]` | (b) | With the staff file present and non-empty: its teacher id and last name, and the schedule's teacher id (S10). Was a raw pandas `KeyError` | `ENFORCED` (S10) |
| 10 | `EnrollmentTransformer._homeroom_enrollments` — the homeroom merge | (b) | The `try/except (KeyError, MergeError)` that shipped a partial set is DELETED (S10). Once homeroom students exist every merge column is required up front: the demographic's school, homeroom, student number and teacher id, and the homeroom lookup's teacher id. Verified on pandas 2.3.3: the `MergeError` arm was unreachable (a merge-key dtype mismatch raises a plain `ValueError`); the `KeyError` arm was a missing column — e.g. a demographic without its student number (`filter_to_active` returns the frame, the column selection then raised) lost every homeroom row, student AND teacher | `ENFORCED` (S10) |
| 11 | The homeroom column (Classes homeroom classes, Enrollments homeroom + co-teacher rows); the schedule grade column (Classes' and Enrollments' subject splits, blended detection's two grade maps); the ClassInformation primary-teacher / section-letter columns (Enrollments' co-teacher rows) | (b) homeroom · (a) grade · (b) co-teacher | All configurable since S9, through `columns.resolve_source_column`: `Homeroom` from the Students mapping (read through `context.entity_mappings` — it used to be the dead `global_config["mappings"]` path, and `.lower()` broke on a `{column: …}` entry); the schedule grade from the Classes mapping's `Grade` (`grades.schedule_grade_column` — ONE key for all three readers, so the splits cannot disagree); the co-teacher columns from the Enrollments `source_columns` roles `class_info_primary_teacher` / `class_info_section_letter`. What a MISSING column does is #5 (grade) and #30 (homeroom) — each fails closed, typed, since S10 — and #15 (co-teacher: left out with a standing warning, owner ruling 2026-09-25) | `ENFORCED` (S9 — configurability; the missing-column postures are #5/#15/#30's) |
| 12 | `staff.StaffTransformer.filter_departed_staff` — status column absent | (e) | Every staff row is still kept (direction unchanged). Since S11 it is recorded: ONE WARNING and `staff_status_column_absent` counting the rows kept (`context` required keyword-only) | `ENFORCED` (S11) |
| 13 | `staff._merge_roster` — guard (empty frame, `teacher_id_col` absent from the staff file, `STAFF_SOURCEID` absent from the roster) | (e) | The merge is still skipped. Since S11 a skip over a PRESENT, non-empty staff and roster file — a join column missing — logs ONE WARNING naming the column(s) in config spelling and records `roster_merge_skipped` counting the staff rows shipped without a roster source id; an absent or empty roster file is not a missing column (the extractor reports a missing file). No bundled config declares a roster file | `ENFORCED` (S11) |
| 13a | `staff._merge_roster` — `roster_df[[teacher_id_col, STAFF_SOURCEID]]`; the guard checks `teacher_id_col` on the STAFF file only | (b) | Once #13's guard lets the merge run, the roster's teacher-id column is required (S10): a typed `JOIN_KEY` error on Staff, no longer a raw pandas `KeyError` (category `unknown`) | `ENFORCED` (S10) |
| 14 | `base.filter_to_active` | (e) / H1 | Direction unchanged — the frame is returned unfiltered, never filtered to empty (`base.py:351`); withdrawn students' guardians and enrollments ship. Since S11 the two postures are told apart and recorded on the CALLER's outcome, ONE WARNING each per entity per run: an empty roster → `active_roster_unavailable` (e.g. the `mbponly` tier, whose StudentCourses runs without Students — every night); an absent student column → `active_roster_column_unresolvable` (e.g. a contacts export without its student-number column). Each counts the rows of the first filter reached, kept unfiltered (on Enrollments, the first of its homeroom / subject filters; the second adds nothing — the first count stands) | `ENFORCED` (S11 — recorded; direction open) |
| 15 | `EnrollmentTransformer._classinfo_coteacher_enrollments` co-teacher enrollments — at `8d33664` its `None` returns when the primary-teacher, school or staff-id column was absent, Path 1's skips (no homeroom classes, or no section column; homeroom or school column absent from the homeroom classes) and Path 2's (no Master Timetable ID column) | (d) co-teacher columns · (b) school | **OPTIONAL, never silent — owner ruling 2026-09-25** (reversing Gate A answer 2 for these columns only; DECISIONS 2026-09-25). With ClassInformation present and non-empty: a missing primary-teacher flag or teacher id leaves EVERY co-teacher row out; once primary-teacher rows exist, a missing section column leaves Path 1's rows out (when homeroom classes exist) and a missing Master Timetable ID Path 2's (when blended classes exist) — the other path still runs. Each case logs ONE aggregated WARNING (`CO-TEACHERS LEFT OUT`, config-spelled column names + a row count; `columns.absent_columns`, tag `optional_field`) and records `OutcomeNote.COTEACHER_SOURCE_UNUSABLE` on the Enrollments outcome — a standing WARNING on Home, Run History and Convert every night it persists (§7 `note-tier`). ClassInformation's school stays fail-CLOSED (`require_columns`, `join_key`) once co-teacher rows are being built. The homeroom lookup carries school + homeroom by construction (#30). An ABSENT or EMPTY ClassInformation contributes nothing and records nothing. It used to omit these rows in silence (H2); measured 2026-09-25, SD40 (no Master Timetable ID, blends present) and SD60 (no primary-teacher flag) now carry the note, their delivered CSVs byte-identical | `ENFORCED` (S10, owner ruling 2026-09-25) |
| 16 | `blended._teacher_from_frame`, `_build_teacher_name_map` | (d) | The teacher-name segment of a blended class name is still omitted (`blended.py:565-566`, `:615-616`). Since S11 ONE WARNING — consistent with `_build_grade_map`'s — when blends were named and neither the schedule (name or teacher id) nor the grouped frame carries the name (`BlendedClassDetector._teacher_name_lost`), and #31's note | `ENFORCED` (S11) |
| 17 | `base.BaseTransformer.decide_enroll_status` — withdraw-date fallback (no status column, or the configured one absent) | (e) | Direction unchanged. Since S11 the step records which signal decided, counting the demographic rows: `status_column_absent_date_only` (no status column — the INFO line now carries the row count and how many had no withdraw date) or `configured_status_column_absent` (the configured status column is not in the export — a WARNING naming it); plus `active_without_positive_signal` counting rows kept Active with neither a status value nor a withdraw date (not recorded beside #18's note, whose count covers every row). Measured 2026-09-25: SD74 2026-09-21 and both Unity drops carry the first and the third (their demographic exports have no status column); SD40, SD51 and SD60 carry neither. The EnrollStatus column keys resolve through `columns.resolve_source_column` since S11 (§9) | `ENFORCED` (S11) |
| 18 | `base.BaseTransformer.decide_enroll_status` — the all-Active default (neither a status nor a withdraw-date column) | (e) / H1 | Everyone still ships Active (D10 open). Since S11 the ONE WARNING carries the row count (and a configured status column's name) and `all_active_default` is recorded — a WARNING-tier note (§7), so the run is PARTIAL on Home, Run History and Convert every night it persists. It outranks `configured_status_column_absent` when both apply: a quieter note must never stand in for the H1 exposure. No measured drop reaches it | `ENFORCED` (S11 — recorded, amber) · direction `PLANNED (D10)` |
| 19 | `apply_field_map` direct read `base.py:753`, target an identity/join field (`User ID`, `Student User ID`, `Class ID`, `School ID`) | (b) | Intended blank — the identity column still ships blank (direction open). Since S6 an ABSENT column is observed: one WARNING per entity + `missing_mapped` on its outcome (when every file the entity reads was observed). Since S11 the engine records it itself, whatever the observation could claim: ONE WARNING per entity naming the output fields and their configured columns, and `identity_field_blanked` counting the rows (`notes.note_identity_blanks` over `notes.IDENTITY_OUTPUT_FIELDS`, called by the engine; a bare string, a `column:` transform, an append-year id with append off — a field the caller filled before the engine ran is skipped, as the engine skips it). Students' `User ID` is required since S10 (#27(ii)), so the note never fires there | `ENFORCED` (observation, S6; note, S11) · direction open |
| 20 | `family._exclude_rows_without_email` `family.py:40` | (c) | Excludes + counts once — the POSITIVE exemplar. Since S11 the count reaches the record (`contacts_excluded_no_email`), and a mapping with no `Email` output column records `email_output_not_mapped` (rows pass through, one WARNING, as before). Measured 2026-09-25: SD40, SD60, SD74 and Unity 2026-09-14 carry the exclusion note | `ENFORCED` |
| 21 | `staff.filter_departed_staff` `staff.py:250-307` | (e) | Unrecognised vocabulary / would-empty → WARN + keep all (`:281-299`); since S11 each also records its note (`staff_status_vocabulary_unrecognised` / `staff_filter_would_empty`, counting the rows kept — an EMPTY frame records neither), and the vocabulary line names its values only when every one is an upper-case code (§8) | `ENFORCED` (S11) |
| 22 | `staff.resolve_staff_roles` `staff.py:102-207` (plan 0052) — the rescue-then-drop rule, EXCLUDING its evidence reader (#22a) | (e) | Rescue-then-drop; counts logged | `ENFORCED` |
| 22a | `staff._teacher_of_record_ids` — the plan-0052 rescue's evidence reader | (b) | A teaching-assignment file that is present and non-empty but lacks the teacher-id column is a typed `JOIN_KEY` error on Staff (CRITICAL) since S10 — it used to be skipped in silence, so the rescue found nobody and every un-roled teacher was DROPPED (H2). An absent or empty file is no evidence, not a fault; the reader runs only when some staff row is un-roled. One exception: a file serving ONLY the `student_demographic` role on a config with no `homeroom_grades` — its teacher id is the homeroom teacher, which links nothing there (Classes and Enrollments never read it), so its absence stays no evidence (as before S10) | `ENFORCED` (S10) |
| 23 | `student_attendance._derive_category` unmapped `(Absent Code, Authorized)` | (b)-shaped vocabulary | Raises; since S4 contained at entity scope (StudentAttendance is ISOLATABLE), so rostering still ships — a config whose ONLY entity is attendance still fails, since nothing was built. Its message echoes a value only when it is inside the closed vocabulary — an upper-case code, a `Y`/`N`/blank flag (S4, §8) | `ENFORCED` |
| 24 | `apply_field_map` direct read `base.py:753`, target an importer-required field (e.g. Family `Email`) | (c) | Intended blank; for Family the blank rows are then excluded (#20) — and since S11 COUNTED on the outcome (#20's note). Since S6 an ABSENT column is observed: one WARNING per entity + `missing_mapped` on its outcome (when every file the entity reads was observed), and an entity that then kept no row is EMPTY/`missing_source_column` instead of `no_rows_after_transform` (SD51 2026-09-18) | `ENFORCED` (observation + reason, S6; count, S11) |
| 25 | `apply_field_map` direct read `base.py:753`, any other target | (d) | Intended blank. Since S6 an ABSENT column is observed: one WARNING per entity + `missing_mapped` on its outcome (when every file the entity reads was observed) — that IS the recorded signal, and S11 deliberately adds no second note for the same fact (single source; e.g. SD74's and Unity's Students `Next school code`). Not recorded: an entity the observation cannot claim for (a file observed without a header row) — ROADMAP | `ENFORCED` (observation, S6) · unobserved residual: ROADMAP |
| 26 | Sibling intended-blank reads: `models.FieldTransform` `models.py:191-194`; `models.FieldAppendYear` with append disabled `models.py:273` | classed by target, as #19/#24/#25/#26 | As #19/#24/#25 by target: an identity target records #19's note (S11); every other target's ABSENT column is S6's observation (one WARNING per entity + `missing_mapped`) | `ENFORCED` (observation, S6; identity note, S11) · unobserved residual: ROADMAP |
| 27 | `StudentTransformer.transform` — the published roster, two triggers | (i) (e) / H1 · (ii) (b) / H2 | (i) No `User ID` OUTPUT column ⇒ no roster ⇒ every downstream `filter_to_active` (#14) fails open (surplus). (ii) The realistic one: the User ID SOURCE column is absent ⇒ `apply_field_map` writes scalar `pd.NA` (`base.py:753`) ⇒ the roster is `{'<NA>'}`, which `base.py:366` (empty-roster guard only) does not catch ⇒ every downstream filter failed CLOSED and dropped ALL student rows in Family, homeroom classes, Enrollments and StudentCourses — a silent shrink. Since S10 (ii) is a typed `JOIN_KEY` error: the mapped `User ID` source column is required before the field map runs (`StudentTransformer._require_user_id_source`; only when the mapping names a `User ID`) | (i) `ENFORCED` (S11 — recorded as #14's `active_roster_unavailable` on every consuming entity; direction open) · (ii) `ENFORCED` (S10) |
| 28 | `EnrollmentTransformer._subject_enrollments` student subject rows and non-blended teacher rows, `EnrollmentTransformer._homeroom_enrollments` homeroom teacher rows (at `8d33664` the `_y` column existed only when the staff-ID column was found) — join-key column absent | (b) | Required since S10, where each path reads them: the schedule's student id and teacher id once timetable rows exist, and the demographic's teacher id (and the homeroom lookup's) once homeroom students exist. They used to be left out in silence — a schedule without its student-ID column shipped timetable classes with teachers only, unenrolling every timetable student; a demographic without its teacher id shipped every homeroom with no teacher (SD83, 2026-09-14). A self-service mapping (`_base: myedbc`, whose `student_id_col` is `Student ID`) over a schedule carrying `Student Number` now stops, typed, where it passed shipping no timetable student — KEPT by owner ruling 2026-09-25; the creator cannot set that column yet (ROADMAP) | `ENFORCED` (S10) |
| 29 | `base.assign_class_ids` `base.py:554-567` → `generate_class_id` `row.get(mt_id_col, "")` `:543-547`; `models.FieldAppendYear.apply` `models.py:274-277`; `classes.py:325-326` `merged.get(school_col, "")` | (b) | Required since S10: `assign_class_ids` requires its Class ID column (the caller's entity — Classes and Enrollments), `FieldAppendYear.apply` its column when `append_year_to_id` is on (an ID; with it off it stays a direct read), and Classes' subject rows their Class ID and `School ID` columns — both in ONE check on the schedule, before the course/staff merges. Every subject `Class ID` (or `School ID`) used to ship blank | `ENFORCED` (S10) |
| 30 | `ClassTransformer._create_homeroom_classes` homeroom column absent | (b) | The school and homeroom columns are required WHERE they are read (S10) — after the homeroom split, so a district with no homeroom-grade pupil reads neither; the silent-skip guard after the dedup is gone (D9). Was a raw pandas `KeyError` | `ENFORCED` (S10) |
| 31 | `blended._resolve_working_frame` `blended.py:180-188`; `_build_course_title_map` `:692-697` | (d)/(e) | Directions unchanged: ClassInformation without its teacher id / Master Timetable ID → INFO fallback to the deduplicated schedule (an equivalent frame, logged — not a loss), else WARN + no blended detection; course-title map WARN + `{}`. Since S11 each lookup a missing column cost — no working frame, no section grades, no course titles, no course-code column, no teacher name (#16) — is recorded on Classes as `blended_lookup_column_absent` counting the ClassInformation rows read (`BlendedClassDetector._note_lookup_gaps`), adding no line above DEBUG: each lookup keeps its own ONE WARNING, never one per blend | `ENFORCED` (S11) |
| 32 | `course_codes.filter_excluded_course_codes` `course_codes.py:40-54`; `filter_excluded_course_code_patterns` `:57-79` (column via `resolve_course_code_column` `:33-37`) | (e) | No course-code column ⇒ the configured exclusions are still not applied and the surplus rows ship (e.g. SD40's `ATT--AM/PM` sections as classes; CourseInfo's K/X/ATT patterns). Since S11 recorded: `course_codes.note_unapplied_exclusions` beside each of the seven filter calls (Classes ×3 incl. blended detection, Enrollments, CourseInfo, StudentCourses ×2) — ONE WARNING per entity per run and `course_code_exclusions_not_applied` counting the rows of the first source found without the column | `ENFORCED` (S11) |
| 33 | `student_attendance.py:237-252` (daily band), `:324-333` (period band) — `record.get(col)` on configured columns | (b)/(c) | Directions unchanged, per band. **Daily:** absent code ⇒ every row dropped (EMPTY); absent student, school or date ⇒ rows ship with that key field blank; absent portion ⇒ every full day counted as one row; absent authorized ⇒ every code misses the category map and raises via #23. **Period:** absent category or student ⇒ every row dropped (EMPTY); absent school or date ⇒ blank key fields. Since S11 never silent: `StudentAttendanceTransformer._note_absent_columns` checks each PRESENT band's configured columns first — ONE WARNING naming the absent ones as configured and `attendance_source_column_absent` counting the rows of the band(s) concerned (lost with the entity if #23 then raises) | `ENFORCED` (S11 — recorded) · direction open |
| 34 | `student_courses.py:235-248` (history), `:369-375` (selection) — `record.get(col)` on configured columns | (b)/(c) | Directions unchanged: an absent student-ID or course-code column skips every row (the entity can end EMPTY); `SCHOOL_NUMBER` reads blank. Since S11 never silent: `StudentCoursesTransformer._note_absent_columns` checks each present source for every column it reads — ONE WARNING (per source, resolved config spelling) and `transcript_source_column_absent` counting the rows of the sources concerned | `ENFORCED` (S11 — recorded) · direction open |
| 34a | `student_courses._build_info_lookups` `student_courses.py:193-208` — CourseInformation lookups (`record.get(cols["course_code"])` `:200`, `SCHOOL_NUMBER` `:203`, title/credit `:204`) | (b) key · (d) title/credit | Directions unchanged: an absent course-code column leaves both lookups empty, so every transcript row loses its title and credit value; an absent title or credit column blanks that field. Since S11 recorded by #34's check (the `info` source) | `ENFORCED` (S11 — recorded) · direction open |
| 35 | `classes._homeroom_name` (`row[teacher_name_col]`) and its caller `ClassTransformer._create_homeroom_classes` — homeroom teacher-name column | (d) | Since S10 (owner Gate A answer 3): absent ⇒ every homeroom class name omits the teacher segment and ONE aggregated WARNING says how many (`ClassTransformer._homeroom_teacher_name_column`, tag `optional_field`) — never a run failure. It used to be a raw pandas `KeyError` that failed the run. Since S11 it also records `homeroom_teacher_name_absent` counting those homeroom classes | `ENFORCED` (S10; note, S11) |
| 36 | `EnrollmentTransformer._subject_enrollments` — schedule `SCHOOL_NUMBER` in the subject-enrollment slices (at `8d33664` #28's guards did not check it) | (b) | Required with #28's columns once timetable rows exist (S10). Was a raw pandas `KeyError` | `ENFORCED` (S10) |
| 37 | `naming.generate_class_name` — subject class name | (d) | Names unchanged: no course-title column ⇒ `"Unknown Course"`; no teacher-name or section column ⇒ that segment blank. The fallback title column is `column_names.COURSE_TITLE` since S9 — the structural name `ClassTransformer._merge_course_and_staff` joins the CourseInformation title under; the Name block's four columns resolve through `columns.resolve_source_column`. Since S11 an ABSENT column is recorded: `ClassTransformer._note_class_name_gaps` — ONE WARNING naming the absent course-title (neither the configured nor the joined title) / teacher-name / section columns in config spelling and `class_name_column_absent` counting the subject classes named without them (the primary-teacher flag's absence INCLUDES the teacher, so it loses nothing). A per-row blank (a code missing from CourseInformation) is a value, not a column — H4, ROADMAP | `ENFORCED` (S11; the literal, S9) |
| 38 | `classes._assign_grades` `classes.py:437-448` — subject class `Grade` (`row.get(col, "")`) | (d) | Unreachable as a missing COLUMN since S10: it reads the Classes mapping's `Grade`, the same column (`grades.schedule_grade_column`) #5's `pii_scope` guard requires on the schedule before the subject split, and the course/staff merges only add columns. A blank VALUE maps to `UG` as before (H4, ROADMAP) | `ENFORCED` (S10, via #5) |
| 39 | `blended._add_session_key` over the time-slot components + `SCHOOL_NUMBER` | (b) | The components are configurable since S9: `SESSION_TIME_COMPONENTS` is a Classes `source_columns` role → default map (`session_term`/`session_semester`/`session_day`/`session_period`), resolved once per detection and read by the key and the block label alike. Since S10 `BlendedClassDetector.detect` requires `SCHOOL_NUMBER` and EVERY time-slot component IN FORCE — configured or left to its default (`blended.session_time_labels`, config spelling) — in its working frame: absent, a typed `JOIN_KEY` error on Classes (an absent school let a blend cross schools and ship a blank `School ID`; an absent time column merged distinct sections into one blended class, re-keying their students to `BLENDED_` Class IDs). The components in force are all four, unless the Classes mapping DECLARES fewer in `session_components` (owner ruling 2026-09-25; config format 1.14; `blended.session_time_roles`, validated at load) — strict on that EFFECTIVE set, never narrowed to the configured roles. `sd40myedbc` is the first to declare: its export has no term column, so it declares semester, day and period — the key it effectively used before S10, byte-identical | `ENFORCED` (S10; configurability S9; the declaration, owner ruling 2026-09-25) |
| 40 | `StudentTransformer._warn_rows_without_email` — no `Email Address` output column | (c) | The no-email count cannot be taken; the rows ship with ONE WARNING (unchanged) and, since S11, `email_output_not_mapped` counting them — the Students twin of #20's missing-column branch | `ENFORCED` (S11) |

**Where the fail-closed guards are** (since S10). Every §5 (a)/(b) presence check is ONE call, `columns.require_columns(available, required, *, entity, guard)`: it compares both sides trimmed and lower-cased, names EVERY missing column at once in the config's spelling and order, and raises one `SourceSchemaError` whose message carries the source's column COUNT and `header_looks_like_data` (a quarter or more of the header names look like values — a headerless file read without its `headers:` block) — never an observed name. Each call carries a `# failure-policy: pii_scope|join_key` tag in the comment block directly above it (per call, not per function). This table is every call under `src/` — `site` is `module.Class.function`, `entity` is the literal the call passes or `(caller)` for a shared helper that passes its caller's — pinned both ways by `tests/test_failure_policy_parity.py`:

<!-- failure-policy-table: require-columns -->
| site | entity | guard | §5 |
|---|---|---|---|
| `base.BaseTransformer.apply_row_filters` | (caller) | pii_scope | 1 |
| `base.BaseTransformer.assign_class_ids` | (caller) | join_key | 29 |
| `blended.BlendedClassDetector.detect` | Classes | join_key | 39 |
| `classes.ClassTransformer._create_homeroom_classes` | Classes | pii_scope | 5 |
| `classes.ClassTransformer._create_homeroom_classes` | Classes | join_key | 30 |
| `classes.ClassTransformer._create_subject_classes` | Classes | pii_scope | 5 |
| `classes.ClassTransformer._create_subject_classes` | Classes | join_key | 29 |
| `classes.ClassTransformer._merge_course_and_staff` | Classes | join_key | 8, 9 |
| `enrollments.EnrollmentTransformer._homeroom_enrollments` | Enrollments | pii_scope | 5 |
| `enrollments.EnrollmentTransformer._homeroom_enrollments` | Enrollments | join_key | 10, 28 |
| `enrollments.EnrollmentTransformer._subject_enrollments` | Enrollments | pii_scope | 5 |
| `enrollments.EnrollmentTransformer._subject_enrollments` | Enrollments | join_key | 28, 36 |
| `enrollments.EnrollmentTransformer._classinfo_coteacher_enrollments` | Enrollments | join_key | 15 |
| `grades.filter_to_grade_scope` | (caller) | pii_scope | 4 |
| `models.FieldAppendYear.apply` | (caller) | join_key | 29 |
| `staff.StaffTransformer._merge_roster` | Staff | join_key | 13a |
| `staff.StaffTransformer._teacher_of_record_ids` | Staff | join_key | 22a |
| `students.StudentTransformer._collapse_cross_enrollment` | Students | join_key | 2, 2a |
| `students.StudentTransformer._generate_emails` | Students | join_key | 3 |
| `students.StudentTransformer._require_user_id_source` | Students | join_key | 27(ii) |

The `(caller)` sites: `apply_row_filters` serves Family and Staff; `assign_class_ids` Classes and Enrollments; `filter_to_grade_scope` Students; `FieldAppendYear.apply` whichever entity maps an append-year ID through the field-map engine (no bundled config does).

**Every site tag** (since S11). Each posture a §5 row describes is marked in the code by a `# failure-policy: <class>` comment on its own line — directly above the `require_columns` call for (a)/(b), directly above the `notes.record_note` call (or the branch) for a recorded fail-open posture. The table is every tag under `src/`, grouped by its innermost function (`site`), with how many tags of that class the function carries and the catalogue rows they implement; the class must be one of the five and a class of every row cited. Pinned both ways by `tests/test_failure_policy_parity.py` (with every `record_note` call required to carry a tag):

<!-- failure-policy-table: tags -->
| site | class | tags | §5 |
|---|---|---|---|
| `base.BaseTransformer.apply_row_filters` | pii_scope | 1 | 1 |
| `base.BaseTransformer.assign_class_ids` | join_key | 1 | 29 |
| `base.BaseTransformer.decide_enroll_status` | safety_heuristic | 4 | 17, 18 |
| `base.BaseTransformer.filter_to_active` | safety_heuristic | 2 | 14, 27(i) |
| `blended.BlendedClassDetector._note_lookup_gaps` | optional_field | 1 | 16, 31 |
| `blended.BlendedClassDetector.detect` | join_key | 1 | 39 |
| `classes.ClassTransformer._create_homeroom_classes` | join_key | 1 | 30 |
| `classes.ClassTransformer._create_homeroom_classes` | pii_scope | 1 | 5 |
| `classes.ClassTransformer._create_subject_classes` | join_key | 1 | 29 |
| `classes.ClassTransformer._create_subject_classes` | pii_scope | 1 | 5 |
| `classes.ClassTransformer._homeroom_teacher_name_column` | optional_field | 1 | 35 |
| `classes.ClassTransformer._merge_course_and_staff` | join_key | 4 | 8, 9 |
| `classes.ClassTransformer._note_class_name_gaps` | optional_field | 1 | 37 |
| `course_codes.note_unapplied_exclusions` | safety_heuristic | 1 | 32 |
| `enrollments.EnrollmentTransformer._classinfo_coteacher_enrollments` | join_key | 1 | 15 |
| `enrollments.EnrollmentTransformer._classinfo_coteacher_enrollments` | optional_field | 2 | 15 |
| `enrollments.EnrollmentTransformer._homeroom_enrollments` | join_key | 2 | 10, 28 |
| `enrollments.EnrollmentTransformer._homeroom_enrollments` | pii_scope | 1 | 5 |
| `enrollments.EnrollmentTransformer._subject_enrollments` | join_key | 1 | 28, 36 |
| `enrollments.EnrollmentTransformer._subject_enrollments` | pii_scope | 1 | 5 |
| `enrollments.EnrollmentTransformer.transform` | join_key | 1 | 7 |
| `family.FamilyTransformer._exclude_rows_without_email` | contract_field | 2 | 20 |
| `grades.filter_to_grade_scope` | pii_scope | 1 | 4 |
| `models.FieldAppendYear.apply` | join_key | 1 | 29 |
| `notes.note_identity_blanks` | join_key | 1 | 19 |
| `staff.StaffTransformer._merge_roster` | join_key | 1 | 13a |
| `staff.StaffTransformer._merge_roster` | safety_heuristic | 1 | 13 |
| `staff.StaffTransformer._teacher_of_record_ids` | join_key | 1 | 22a |
| `staff.StaffTransformer.filter_departed_staff` | safety_heuristic | 3 | 12, 21 |
| `student_attendance.StudentAttendanceTransformer._derive_category` | join_key | 1 | 23 |
| `student_attendance.StudentAttendanceTransformer._note_absent_columns` | join_key | 1 | 33 |
| `student_courses.StudentCoursesTransformer._note_absent_columns` | join_key | 1 | 34, 34a |
| `students.StudentTransformer._collapse_cross_enrollment` | join_key | 1 | 2, 2a |
| `students.StudentTransformer._generate_emails` | join_key | 1 | 3 |
| `students.StudentTransformer._require_user_id_source` | join_key | 1 | 27(ii) |
| `students.StudentTransformer._warn_rows_without_email` | contract_field | 1 | 40 |

The catalogue rows that carry no tag of their own, each with the reason (a row is never both tagged and listed here):

<!-- failure-policy-table: untagged-sites -->
| # | why it carries no tag of its own |
|---|---|
| 6 | Resolution only: its presence check is #4's `pii_scope` tag in `grades.filter_to_grade_scope`. |
| 11 | Configurability row: the missing-column postures it lists are #5's, #15's and #30's, each tagged there. |
| 22 | A deliberate exclusion (§1), not a fail-open posture: rescue-then-drop with its counts logged; its evidence reader is #22a, tagged. |
| 24 | Recorded by the source observation (§10, `pipeline.observe_source_columns` → `missing_mapped`), not at a transformer site; the Family count is #20's tag. |
| 25 | Recorded by the source observation (§10) — one WARNING per entity and `missing_mapped`; no transformer site. |
| 26 | As #25 by target; an identity target is #19's tag. |
| 38 | Unreachable as a missing column since S10 — #5's `pii_scope` guard requires the column first. |

## §6 Typed errors and closed vocabularies (P6)

Classification is by TYPE only (`isinstance`), never by message text. Exemplar: the scheduler's HRESULT-keyed `task_com.MSG_*` canonicals and `windows._fail` (`windows.py:217`) — one bounded vocabulary, one log shape.

| Rule | Today | Status |
|---|---|---|
| Every boundary-crossing ETL failure is an `EtlError` with a bounded `category` | `src/etl/errors.py` holds the taxonomy (`RunErrorCategory`, `EtlError`, `SourceSchemaError`, `NoUsableInputError`, `ConfigLoadError`, and S3's `OutputFolderUnsetError`); `DeliveryIntegrityError`, `OutputWriteError` and `ExtractionError` are `EtlError`s carrying enum members; since S10 every §5 (a)/(b) presence guard raises `SourceSchemaError` through `columns.require_columns` (the §5 `require-columns` table), so no catalogued missing-column fault reaches the pipeline as a raw pandas `KeyError`. Untyped by design: §5 #7's missing class artifacts (an ordering fault, `data`) | `ENFORCED` |
| The classifier reads no exception text | `errors.classify_error_category` branches on `isinstance` only; no usable input is the TYPE `NoUsableInputError`. AST-pinned since S1; since S13a in `tests/test_architecture_fitness.py` rule (e), widened to every exception classifier (`errors.classify_error_category`, `outcomes.reason_for`, `failure_copy.error_card_copy`) and to every text-reading shape: no `str(`/`repr(`/`format(` call, no `.args`, no f-string, no string method, no membership test against string constants | `ENFORCED` |
| Every closed-enum member maps to copy, a verdict and a row here | `failure_copy.FAILED_CATEGORY_COPY` is TOTAL over `RunErrorCategory` except `NONE` (which raises), `outcome_sentence` over every valid (kind, reason), `OUTCOME_TIER` over every valid (entity, kind, reason) (a function since S8 — §7), and since S10 `NOTE_TIER` and the note copy over every `OutcomeNote`; failed categories are `Verdict.FAILED` via `LatestReason.FAILED_ETL`. Pinned by `tests/test_ui_flet_failure_copy.py` (derived from the enums) and `tests/test_failure_policy_parity.py` (this table ↔ the copy) | `ENFORCED` |
| Config typos are loud, origin-keyed (P15): a bundled config RAISES at load; a user-dir overlay WARNS at run and is REFUSED at authoring; `Field*` leaf models forbid extras everywhere; the root stays `extra="ignore"` | ONE pure walker, `loader.unknown_config_keys`, runs in the shared resolve → gate → validate path over the resolved raw config and reports every unknown `global_config` key, entity-block key and `source_columns` role (judged against that entity's transformer's `SOURCE_COLUMN_ROLES` — the constants its read sites use), each with its nearest known key (`difflib`). The origin decides (`loader._UNKNOWN_KEY_POLICY_BY_ORIGIN`): bundled (and the single-`config_dir` seam) → `UnknownConfigKeyError` (a `ValueError`, so the pipeline records `config`); user-dir → one WARNING per key, the key ignored, the run continues; `validate_overlay` (hence `write_overlay`) → refused. Every field-mapping variant (`ConfiguredField` and subclasses, `FieldTransform`/`FieldNameConfig` included) forbids extras for every origin, and `classify_field` refuses a typo'd key (`transfrom:` → "did you mean 'transform'?") and a dict with no mapping shape, where it used to log and ship the field blank. `MappingConfig` stays `extra="ignore"`. Pinned by `tests/test_config_unknown_keys.py` (every bundled config: zero findings). | `ENFORCED` (S12) |

<!-- failure-policy-table: vocabularies -->
| enum | member | value | produced today by | Status |
|---|---|---|---|---|
| RunErrorCategory | NONE | `none` | every completed run (`pipeline.py:1072`) | `ENFORCED` |
| RunErrorCategory | NO_INPUT | `no_input` | input folder missing (`run_pipeline`'s early exit); every required file missing/empty — `NoUsableInputError`, by type | `ENFORCED` |
| RunErrorCategory | NO_OUTPUT | `no_output` | `check_delivery_integrity` (`pipeline.py:452-457`) | `ENFORCED` |
| RunErrorCategory | INCOMPLETE_ROSTER | `incomplete_roster` | `check_delivery_integrity` (`pipeline.py:459-468`) | `ENFORCED` |
| RunErrorCategory | CONFIG | `config` | config load (`run_pipeline`'s early exit); any `FileNotFoundError`; `ConfigLoadError` — Convert's config load (`convert_job`, S5) | `ENFORCED` |
| RunErrorCategory | DATA | `data` | any UNTYPED `ValueError` (e.g. `DataLoader.select_ordered`'s missing output column) — no longer the row_filters missing column, which is `source_schema` | `ENFORCED` |
| RunErrorCategory | OUTPUT | `output` | output pre-flight (`pipeline.py:896-909`); `OutputWriteError` (`:378-401`); Convert's unset-output-folder gate error `OutputFolderUnsetError` (S3 — card copy only, never recorded) | `ENFORCED` |
| RunErrorCategory | UNKNOWN | `unknown` | everything else — incl. a raw pandas `KeyError` | `ENFORCED` |
| RunErrorCategory | SOURCE_SCHEMA | `source_schema` | `SourceSchemaError` — every `columns.require_columns` guard (§5, the `require-columns` table) | `ENFORCED` |
| RunErrorCategory | INPUT_UNREADABLE | `input_unreadable` | `ExtractionError` — an unparseable file, or a case-insensitive filename collision | `ENFORCED` |
| OutcomeKind | BUILT | `built` | `run_transform`: the entity's transform produced rows (they are in `outputs`) | `ENFORCED` |
| OutcomeKind | EMPTY | `empty` | `run_transform`: the entity was skipped with nothing to build (always with one of the four EMPTY reasons) | `ENFORCED` |
| OutcomeKind | FAILED | `failed` | `run_transform`: the entity's own transform raised — an ISOLATABLE entity is then left out and the run continues, a CRITICAL one re-raises (S4) | `ENFORCED` |
| OutcomeKind | NOT_RUN | `not_run` | every entity after a raising one, and every entity when the run fails before the loop (`OutcomeLedger.finalize_aborted`) | `ENFORCED` |
| OutcomeReason | NONE | `none` | every BUILT outcome | `ENFORCED` |
| OutcomeReason | NO_SOURCE_FILES_DECLARED | `no_source_files_declared` | `run_transform`: the mapping declares no source file for the entity (incl. an `entity_order` name with no mapping) | `ENFORCED` |
| OutcomeReason | SOURCE_FILES_EMPTY | `source_files_empty` | `run_transform`: every source file of the entity was missing or empty | `ENFORCED` |
| OutcomeReason | NO_ROWS_AFTER_TRANSFORM | `no_rows_after_transform` | `run_transform`: the entity had input and its transform kept no row (e.g. every contact excluded for a blank email) | `ENFORCED` |
| OutcomeReason | MISSING_SOURCE_COLUMN | `missing_source_column` | FAILED: `outcomes.reason_for` — the transform raised a `SourceSchemaError` (a §5 `require-columns` guard), by type. EMPTY (S6): `outcomes.apply_observation` — an EMPTY/`no_rows_after_transform` entity whose source observation found a mapped column absent from its own file(s) | `ENFORCED` |
| OutcomeReason | TRANSFORM_ERROR | `transform_error` | `outcomes.reason_for`: any other raise from the transform; also what `outcomes_from_record` reads an unknown kind/reason as | `ENFORCED` |
| OutcomeReason | RUN_ABORTED | `run_aborted` | every NOT_RUN outcome | `ENFORCED` |
| OutcomeNote | COTEACHER_SOURCE_UNUSABLE | `coteacher_source_unusable` | `EnrollmentTransformer._classinfo_coteacher_enrollments` (§5 #15): a present ClassInformation lacked a co-teacher column, so the co-teacher links needing it were left out; the count is the ClassInformation rows affected — the whole file for an entry column, the primary-teacher rows for a path column, which the other path may still have used (owner ruling 2026-09-25) | `ENFORCED` |
| OutcomeNote | ALL_ACTIVE_DEFAULT | `all_active_default` | `BaseTransformer.decide_enroll_status` (§5 #18): neither a status nor a withdraw-date column — every student shipped Active; counts the demographic rows. WARNING tier (§7) | `ENFORCED` |
| OutcomeNote | CONFIGURED_STATUS_COLUMN_ABSENT | `configured_status_column_absent` | `BaseTransformer.decide_enroll_status` (§5 #17): the configured status column is not in the export, the withdraw date decided; counts the demographic rows | `ENFORCED` |
| OutcomeNote | STATUS_COLUMN_ABSENT_DATE_ONLY | `status_column_absent_date_only` | `BaseTransformer.decide_enroll_status` (§5 #17): no status column, the withdraw date alone decided; counts the demographic rows | `ENFORCED` |
| OutcomeNote | ACTIVE_WITHOUT_POSITIVE_SIGNAL | `active_without_positive_signal` | `BaseTransformer.decide_enroll_status` (§5 #17): counts the rows kept Active with neither a status value nor a withdraw date (not beside `all_active_default`) | `ENFORCED` |
| OutcomeNote | STAFF_STATUS_COLUMN_ABSENT | `staff_status_column_absent` | `StaffTransformer.filter_departed_staff` (§5 #12): no status column; counts the staff rows kept | `ENFORCED` |
| OutcomeNote | STAFF_STATUS_VOCABULARY_UNRECOGNISED | `staff_status_vocabulary_unrecognised` | `StaffTransformer.filter_departed_staff` (§5 #21): values outside Active/Inactive (named in the log only when every one is an upper-case code, else counted — §8); counts the staff rows kept | `ENFORCED` |
| OutcomeNote | STAFF_FILTER_WOULD_EMPTY | `staff_filter_would_empty` | `StaffTransformer.filter_departed_staff` (§5 #21): nobody marked Active; counts the staff rows kept | `ENFORCED` |
| OutcomeNote | ROSTER_MERGE_SKIPPED | `roster_merge_skipped` | `StaffTransformer._merge_roster` (§5 #13): a join column missing from a present staff/roster file; counts the staff rows shipped without a roster source id | `ENFORCED` |
| OutcomeNote | ACTIVE_ROSTER_COLUMN_UNRESOLVABLE | `active_roster_column_unresolvable` | `BaseTransformer.filter_to_active` (§5 #14), on the calling entity: the source lacks the student column; counts the rows of the first filter reached, kept unfiltered (on Enrollments, the first of its two filters) | `ENFORCED` |
| OutcomeNote | ACTIVE_ROSTER_UNAVAILABLE | `active_roster_unavailable` | `BaseTransformer.filter_to_active` (§5 #14, #27(i)), on the calling entity: no roster was published this run; counts the rows of the first filter reached, kept unfiltered (on Enrollments, the first of its two filters) | `ENFORCED` |
| OutcomeNote | BLENDED_LOOKUP_COLUMN_ABSENT | `blended_lookup_column_absent` | `BlendedClassDetector._note_lookup_gaps` (§5 #16/#31), on Classes: a lookup detection reads lacked a column; counts the ClassInformation rows read | `ENFORCED` |
| OutcomeNote | HOMEROOM_TEACHER_NAME_ABSENT | `homeroom_teacher_name_absent` | `ClassTransformer._homeroom_teacher_name_column` (§5 #35): counts the homeroom classes named without the teacher | `ENFORCED` |
| OutcomeNote | CLASS_NAME_COLUMN_ABSENT | `class_name_column_absent` | `ClassTransformer._note_class_name_gaps` (§5 #37): counts the subject classes named without an absent segment | `ENFORCED` |
| OutcomeNote | COURSE_CODE_EXCLUSIONS_NOT_APPLIED | `course_code_exclusions_not_applied` | `course_codes.note_unapplied_exclusions` (§5 #32), on Classes / Enrollments / CourseInfo / StudentCourses: counts the rows of the first source found without a course-code column | `ENFORCED` |
| OutcomeNote | CONTACTS_EXCLUDED_NO_EMAIL | `contacts_excluded_no_email` | `FamilyTransformer._exclude_rows_without_email` (§5 #20): counts the contacts left out for a blank Email | `ENFORCED` |
| OutcomeNote | EMAIL_OUTPUT_NOT_MAPPED | `email_output_not_mapped` | `FamilyTransformer._exclude_rows_without_email` (§5 #20), `StudentTransformer._warn_rows_without_email` (§5 #40): no email output column; counts the rows shipped without it | `ENFORCED` |
| OutcomeNote | IDENTITY_FIELD_BLANKED | `identity_field_blanked` | `notes.note_identity_blanks` (§5 #19), on the mapping's entity: an identity field's source column is absent; counts the rows | `ENFORCED` |
| OutcomeNote | ATTENDANCE_SOURCE_COLUMN_ABSENT | `attendance_source_column_absent` | `StudentAttendanceTransformer._note_absent_columns` (§5 #33): a present band lacks a configured column; counts that band's rows | `ENFORCED` |
| OutcomeNote | TRANSCRIPT_SOURCE_COLUMN_ABSENT | `transcript_source_column_absent` | `StudentCoursesTransformer._note_absent_columns` (§5 #34/#34a): a present source lacks a column it reads; counts the rows of the sources concerned | `ENFORCED` |

Guard kinds (`GuardKind`: `PII_SCOPE`, `JOIN_KEY`) and the leaf errors (`SourceSchemaError`, `NoUsableInputError`, `ConfigLoadError`) are named in the plan's *Naming table* and live in `src/etl/errors.py` (S1); since S10 every guard raises through `columns.require_columns`, whose closing sentence per guard kind (`columns.GUARD_CONSEQUENCE`) is total over `GuardKind` (pinned). `SourceSchemaError`'s `entity`, `columns` (config spelling, never empty) and `guard` are required keyword-only. Since S9 site #4 (`grades.filter_to_grade_scope`) names its column through `columns.source_column_label` too — the config's spelling, or the default `grade` when the mapping names none (which, being undeclared, `safe_label` never prints). The persisted `error_category` is normalised to the enum's plain `.value` in ONE place, `pipeline.build_run_record`, so the store column and the record JSON cannot disagree. `OutcomeKind`, `OutcomeReason`, the legal kind→reason table `VALID_REASONS` and `EntityOutcome` (frozen; an illegal state — BUILT with no rows, a reason invalid for its kind, rows on a non-BUILT outcome, an unusable `missing_mapped` — is REFUSED at construction) live in `src/etl/outcomes.py` (S2; `missing_mapped` and the EMPTY/`missing_source_column` pair S6). `apply_observation` is the ONE place an observation changes a reason, and only EMPTY/`no_rows_after_transform` ever changes — a FAILED reason stays the exception's type. Since S10 (owner ruling 2026-09-25) an outcome may carry `notes` — `(OutcomeNote, count ≥ 1)` pairs its transform recorded through `TransformContext.record_outcome_note` (one per entity and note per run), only on a BUILT or EMPTY outcome (a note on a FAILED or NOT_RUN one is refused). Since S11 `OutcomeNote` carries the whole catalogue — one member per recorded posture, each counting what its row above says — and a transformer records one through `notes.record_note`, which logs the site's ONE line only the first time that (entity, note) is recorded in a run.

## §7 Surfacing, record keys, symmetric sinks (P7, P8, P12)

| Rule | Today | Status |
|---|---|---|
| Every configured entity has exactly one outcome per run, in the record as `entity_outcomes` | `build_run_record` writes `entity_outcomes` (`{entity: {kind, reason, rows}}`, configured order, plus `missing_mapped` — a list of config-spelling column names — only on an entity the source observation found something for, S6; plus `labels` / `file_label` — the config-declared names the copy may print — only on a `missing_source_column` entry that names something, S7; plus `notes` — `{note: count}` — only on an entry whose transform recorded one, S10) on every record the CLI and Convert write, the log line and the store sharing one dict; `None` only where no ledger existed (input-folder check, config load, output pre-flight, a delivery-only record). The ledger is built at the same point on both entry points, and `OutcomeLedger.complete()` refuses a missing outcome. Its `rows` is the rows the TRANSFORM produced — the flat count keys keep their own meaning (`build_run_record`'s docstring). Read by `home_status.classify_latest_reason` and `convert_result.summarize` through the TOTAL `outcomes_from_record` (S3) | `ENFORCED` |
| A FAILED outcome in a successful run is PARTIAL / WARNING on Home, Run History and Convert every run it persists, derived from that run's own record | `LatestReason.PARTIAL` → `Verdict.WARNING` (precedence status → delivery → PARTIAL → anomaly → data warnings; since S10 a PARTIAL made only of notes — every warning outcome BUILT — ranks below anomaly, see the next row), worded by `failure_copy.partial_copy` on all three surfaces; a record without the key classifies exactly as before (snapshot-pinned). Produced since S4 by the bulkhead, on both entry points; Convert's anomaly-ack prompt also names why the file is missing | `ENFORCED` |
| An enabled entity that builds NOTHING is a standing WARNING unless it simply had nothing to send and its absence is a normal night (owner decision D5, 2026-09-24): EMPTY/`no_rows_after_transform` and EMPTY/`missing_source_column` warn on EVERY entity; EMPTY/`source_files_empty` and EMPTY/`no_source_files_declared` are neutral ONLY for an entity in `MAY_BE_EMPTY` (the `may-be-empty` table below), and warn for every other. The remedy for a district that warns is a config change, never a quieter tier | `failure_copy.OUTCOME_TIER(entity, kind, reason)` is the ONE tier rule (the `outcome-tier` table below; BUILT is HEALTHY even with `missing_mapped`; NOT_RUN is FAILED — it exists only inside a failed run) and `failure_copy.warning_outcomes` the ONE PARTIAL predicate: `home_status.left_out_outcomes` (Home, the Run History banner and row) and `convert_result.summarize` all select through it, so a warning EMPTY outcome is the same `LatestReason.PARTIAL` as a FAILED one, at the same precedence (it outranks the anomaly, which is untouched), every run it persists and through a delivery-only record's walk-back. `partial_copy` words a single EMPTY outcome as its `outcome_sentence` (S7 labels included) + the "everything else" sentence + a per-reason next step; Run History reads "N file(s) skipped". `outcomes.MAY_BE_EMPTY` sits beside `ROSTER_ANCHOR_ENTITY`, restrictive by default (an unlisted entity warns). No ETL change: the record and the delivered files are what S7 wrote. Measured before landing (DECISIONS 2026-09-24): of the real drops, SD51 2026-09-18 alone newly turned amber (Family EMPTY/`missing_source_column` — its contact export has no email column), and that was resolved in config, not in the tier: the owner turned SD51's Family off (2026-09-25), so on the bundled configs only Unity's 2026-09-22 plain-report night (a FAILED Family, amber since S4) is amber; no bundled config's contract fixture turns amber. Pinned by `tests/test_standing_empty_warning.py` and `tests/test_failure_policy_parity.py` | `ENFORCED` (S8) |
| A BUILT entity that left out part of itself on a detected fault the owner accepted says so every night it persists (owner ruling 2026-09-25 — the co-teacher rows, §5 #15) | `failure_copy.NOTE_TIER` (the `note-tier` table below) rates each `OutcomeNote`, and `failure_copy.outcome_tier(outcome)` — the WHOLE outcome, so a note cannot be forgotten — is `OUTCOME_TIER` raised to the worst tier of its notes (a note never lowers one). `warning_outcomes` stays the ONE PARTIAL predicate and selects through it, so a BUILT Enrollments carrying `coteacher_source_unusable` is the same `LatestReason.PARTIAL` / WARNING as a left-out file, on Home, the Run History banner and row, and Convert. `partial_copy` never calls that file "left out": alone, the headline names what the note names ("Your roster synced without some co-teachers") and the detail is the note's sentence, the "everything else" sentence and its next step; beside a left-out file, that file's copy leads and the note follows. Run History's suffix is `failure_copy.partial_label` ("co-teachers left out"; "N file(s) skipped" counts left-out files only). **A note never hides an anomaly:** when every warning outcome is BUILT (only notes warn) and the record has anomalies, `classify_latest_reason` returns ANOMALY — the file was delivered, so the "a vanished file's anomaly fires once" reason PARTIAL outranks ANOMALY does not apply, and a note that stands every night would otherwise hide every one-night drop; beside a FAILED or warning-EMPTY file PARTIAL still leads. Pinned by `tests/test_coteacher_standing_warning.py` (two nights, SD60- and SD40-shaped, with CLEAN twins; `TestTheNoteNeverHidesAnAnomaly` for the anomaly rank) and `tests/test_failure_policy_parity.py` | `ENFORCED` (S10) |
| Every build attempt on every entry point writes exactly one record through `build_run_record` | The CLI records every failure (`run_pipeline`'s failure sink; a dry run stores nothing by design). Since S5 Convert does too: its config load raises the typed `ConfigLoadError` and records `config` (`entity_outcomes` `None`) — and records any OTHER load raise by its type, as the pipeline's outer sink does; ONE broad handler in `convert_job` (`# noqa: BLE001 — symmetric failure sink (failure-policy §7); always re-raised`), from `to_raw_dict` through the quality report and closing BEFORE the SFTP leg, records `failed` through `convert._record_failed_attempt` — the category by TYPE (`classify_error_category`) and the completed ledger (`finalize_aborted`) — the same status, `error_category`, counts and completed ledger `run_pipeline`'s sink records for the same fault — then re-raises the SAME object to `on_error`. Recording is suppressed-on-failure, so it can never mask the fault. Four deliberate non-records: an unset output folder, an unusable output folder (pre-flight or the write's `OSError`), `NO_INPUT`, `NEEDS_ANOMALY_ACK` (DECISIONS 2026-09-24). A raise after `save_all` committed records `failed` though the files were written. `entity_outcomes`, `status` and `error_category` are REQUIRED keyword-only with no default on `convert._record_manual_run` (S5; `entity_outcomes` also on `build_run_record`, `run_transform` (`ledger`) and both `PipelineResult` / `ConvertResult`, S2). Pinned by `tests/test_convert_failure_record.py` (one record per attempt, identity, parity with the pipeline's record for injected faults, the non-records beside recording twins, the signature, and an AST pin that the sink ends in a bare `raise` before the SFTP leg). **What a record MEANS to Home is owner decision D14:** a FAILED MANUAL record never sets the verdict (`home_status.verdict_records`, shared by Home, the Run History banner and the probe timestamp the Setup badge reads); Run History's rows list it | `ENFORCED` |
| Partner assumption (P13): a rule that depends on SpacesEDU import behaviour cites a dated confirmation row in `output-contract.md` or the open question; until confirmed the conservative branch applies, and partner docs never claim an omitted file has no side effects | Q5 is open (`Q5-status: open`); `docs/partner/faq.md` states the criticality rule and makes no such claim — its isolatable bullet says the consequence for earlier-linked records "is pending confirmation" | `ENFORCED` (review; since S4 also `tests/test_failure_policy_parity.py`: the FAQ's bullets ↔ §3's sets, the pending clause ↔ `Q5-status`, with doctored twins) |
| Record evolution is additive JSON only — no `runs` DDL, CHECK or `user_version` change until ROADMAP:50 is fixed; readers are TOTAL | No DDL since v1; `store.py` readers are total | `ENFORCED` |
| An unknown kind/reason from a newer build reads as FAILED/TRANSFORM_ERROR (errs toward a warning) | `outcomes.outcomes_from_record` is TOTAL (never raises): a non-mapping → `()`, a blank key dropped, an unknown kind or reason → FAILED/`transform_error`, a known-but-impossible entry dropped (`tests/test_etl_outcomes.py`). An unknown NOTE code (S10) is DROPPED, not read as a warning — most of the catalogue S11 added is Run-History detail only, so erring toward amber would light an older build's Home on facts that are not warnings (DECISIONS 2026-09-25) | `ENFORCED` |
| Exit codes: `0` success (incl. a partial run, D3) · `1` ETL failure · `2` empty stdin / conflicting flags · `3` delivery failed | As stated; a partial run exits `0` with record `status` `success`, `error_category` `none` and the FAILED outcome in `entity_outcomes` (S4) | `ENFORCED` |

The entities whose export may legitimately be absent or empty on a given night (`outcomes.MAY_BE_EMPTY`; pinned — adding one is a DECISIONS entry):

<!-- failure-policy-table: may-be-empty -->
| entity | why its absence is a normal night |
|---|---|
| StudentAttendance | Absence files arrive only on nights with absences; a missing attendance drop must never stop — or darken — rostering (output-contract; its §3 row) |

The tier of one outcome (`failure_copy.OUTCOME_TIER`; pinned over every valid pair). HEALTHY adds nothing to the verdict, WARNING makes a completed run PARTIAL, FAILED only occurs inside a failed run:

<!-- failure-policy-table: outcome-tier -->
| kind | reason | MAY_BE_EMPTY entity | any other entity |
|---|---|---|---|
| BUILT | none | HEALTHY | HEALTHY |
| EMPTY | no_source_files_declared | HEALTHY | WARNING |
| EMPTY | source_files_empty | HEALTHY | WARNING |
| EMPTY | no_rows_after_transform | WARNING | WARNING |
| EMPTY | missing_source_column | WARNING | WARNING |
| FAILED | missing_source_column | WARNING | WARNING |
| FAILED | transform_error | WARNING | WARNING |
| NOT_RUN | run_aborted | FAILED | FAILED |

The tier of one outcome NOTE (`failure_copy.NOTE_TIER`; pinned over every `OutcomeNote`). A WARNING note makes an otherwise HEALTHY outcome warn — the run is PARTIAL every night it carries the note. A HEALTHY note (plan 0053 S11) is shown only as its run's Run History row detail (`failure_copy.detail_note_labels` → `RunRow.notes`, a muted line under the status) — recorded and visible, never amber, never on Home. Raising one to WARNING is an owner decision, measured first against the real drops it would turn amber:

<!-- failure-policy-table: note-tier -->
| note | tier |
|---|---|
| coteacher_source_unusable | WARNING |
| all_active_default | WARNING |
| configured_status_column_absent | HEALTHY |
| status_column_absent_date_only | HEALTHY |
| active_without_positive_signal | HEALTHY |
| staff_status_column_absent | HEALTHY |
| staff_status_vocabulary_unrecognised | HEALTHY |
| staff_filter_would_empty | HEALTHY |
| roster_merge_skipped | HEALTHY |
| active_roster_column_unresolvable | HEALTHY |
| active_roster_unavailable | HEALTHY |
| blended_lookup_column_absent | HEALTHY |
| homeroom_teacher_name_absent | HEALTHY |
| class_name_column_absent | HEALTHY |
| course_code_exclusions_not_applied | HEALTHY |
| contacts_excluded_no_email | HEALTHY |
| email_output_not_mapped | HEALTHY |
| identity_field_blanked | HEALTHY |
| attendance_source_column_absent | HEALTHY |
| transcript_source_column_absent | HEALTHY |

## §8 Privacy and labels (P9)

| Rule | Today | Status |
|---|---|---|
| Records, banners and cards carry closed-set codes humanised through total copy tables — never `str(e)`, cell values or paths | The store carries `error_category` only; Home, Run History and Convert word it through the total tables in `failure_copy` (S3), whose variable text is an authored entity phrase (an unknown entity key reads "one of your files"), counts and, since S7, the validated config-declared labels (row 3) — sentinel-swept in `tests/test_ui_flet_failure_copy.py`; the free-text error is never read | `ENFORCED` |
| No OBSERVED header text in any exception message or log line (a headerless file read without its header makes row 1 a pupil) | The four sites that dumped `sorted(df.columns)` (§5 #1–#4) now carry the COUNT only (`errors.available_columns_note`), and since S10 every guard does, through `columns.require_columns` — plus a `header_looks_like_data` yes/no flag (`columns.header_looks_like_data`), never a name (sentinel-pinned in `tests/test_require_columns.py`); pinned by the sentinel sweep in `tests/test_etl_errors.py` and end to end in `tests/test_pipeline_run_store.py` — since S4 on both the `Pipeline failed` line and the `ENTITY NOT BUILT` traceback. The one ISOLATABLE site that echoed CELL values, the unmapped absence code (§5 #23), shows a value only when it is an upper-case code or a `Y`/`N`/blank flag (S4); since S11 the staff-vocabulary line (§5 #21) follows the same rule through the shared `notes.is_code_shaped` — its unrecognised values are named only when EVERY one is an upper-case code (`A`/`I`), else only their count (a misaligned export's status column can hold names) | `ENFORCED` |
| Config-DECLARED file and column labels may appear in copy and record, membership-validated against the resolved config and sanitised (D4, decided 2026-09-23) | `outcomes.safe_label(text, *, vocabulary)` is the ONE check: exact membership in the RESOLVED config's own vocabulary (`preflight.label_vocabulary_by_entity` — the entity's `field_map` + `row_filters` columns and its `source_files`, config spelling, never lower-cased, never `source_columns`; none for StudentAttendance, whose field_map values are placeholders), then shape: non-blank, trimmed, printable (no newline), ≤ 120 characters, and neither email- nor path-shaped (no `@`, backslash, leading `/`/`~`, drive letter or URL scheme — even a declared one); a FILE label must also be a bare filename (no `/`, no `:`, never `.`/`..` — `source_files` is unvalidated, so a declared relative path is refused, not named). `outcomes.apply_labels` is the ONE producer: a FAILED `missing_source_column` outcome is labelled from the raising `SourceSchemaError`'s own `columns` (only when attributed to that entity; never `str(exc)`), an EMPTY one from `missing_mapped`. **A file is named only beside a surviving column, and only when the entity reads its mapped columns from its own files and has EXACTLY ONE configured source file** (Classes' five, and Enrollments' cross-file reads, never name one); no raise site carries a `source_files` role today, so that second trigger is not implemented. Labels ride the record as `labels` / `file_label`, written only when non-empty; the TOTAL reader re-checks shape, reason and pairing (it cannot re-check membership) and drops junk without dropping the entry. `failure_copy.outcome_sentence` renders them through its label-aware variant (no labels ⇒ the S3 sentence, byte-identical), so Home, Run History and Convert name the file and column. Pinned by `tests/test_etl_outcome_labels.py`, `tests/test_ui_flet_failure_copy.py`, `tests/test_identity_pii_guards.py` (a declared email- or path-shaped name never reaches the labels; a declared relative path never becomes a file label; an observed header never reaches the record) and the raise-isolation twin in `tests/test_pipeline_run_store.py`. S6's `missing_mapped` (record + `MAPPED COLUMNS MISSING` log) is unchanged and never reaches copy | `ENFORCED` (S7) |

## §9 Column resolution (P10)

| Rule | Today | Status |
|---|---|---|
| ONE resolver, `columns.resolve_source_column`, with one shape policy built on `models.classify_field`; presence via `columns.require_columns` | Every transformer source-column read with a default goes through `columns.resolve_source_column` (S9) — since S11 including `BaseTransformer.resolve_active_config`'s EnrollStatus `status_column`/`withdraw_date_column` (the EnrollStatus block is read as a key → column mapping like a `source_columns` block; its values are validated strings, and a WHITESPACE-ONLY value keeps its pre-S11 meaning — a blank `status_column` is configured-and-absent (the date branch), a blank `withdraw_date_column` reads no column — so every valid config, blank values included, reads the column it read before (pinned); the status column's alias detection when none is configured, and its presence check, stay there, recorded as §5 #17/#18): a bare string or a `{column: …}` entry names the column; a blank, `null`, absent key or `DEFAULT_COLUMN_SHAPES` shape (`{value: …}`, an id/role pair, an email `format:`) reads the documented default (one DEBUG line per run); any other shape reads the default with one WARNING; the answer is always `normalize_column_name`d. The same resolution serves config-carrier blocks (`Name`, the id/role pair) and `source_columns`. The three old resolvers (`base.resolve_column`, `family._student_number_col`, `student_courses._field_map_source`) and every inline `.get(...).lower()` are deleted; `context.get_students_config` reads `entity_mappings` first, so the Students mapping reaches the Classes/Enrollments homeroom path. AST-pinned (`tests/test_column_resolution_pin.py`: no `.get(<str>, <str>)` lower-cased, no literal `"column"` read on a field_map entry outside `columns.py`, the retired names stay deleted — each with a doctored twin). TRANSITIONAL for one release: one WARNING per run when a site's answer differs from what the build before S9 read there (ROADMAP removes it). Presence is `columns.require_columns` (S10) — every §5 (a)/(b) guard, in the same pandas-free module; a column a guard names comes from `columns.source_column_label` (config spelling) or a `column_names` constant | `ENFORCED` (the resolver, S9 — EnrollStatus since S11; `require_columns`, S10) |
| New shared behaviour goes in composed modules, not new `BaseTransformer` methods | Review-held; S9 followed it — the resolver is the composed `columns.py`, and it DELETED a `BaseTransformer` method (`resolve_column`) rather than adding one; S11 likewise — `notes.record_note`, `notes.note_identity_blanks` (run by the field-map engine) and `notes.is_code_shaped` are composed, and the one base change (`decide_enroll_status`) refactors an existing method. Since S13a the class body's member set (methods and class attributes) is a frozen registry, `tests/test_architecture_fitness.py` rule (g): a new `BaseTransformer` member is red, and so is a stale entry | `ENFORCED` (fitness pin since S13a) |

## §10 Source observation never enforces (P11)

| Rule | Today | Status |
|---|---|---|
| Missing mapped columns are attributed per entity over that entity's OWN files; the observation never raises, never gates, keeps preflight's soundness rule | `preflight.missing_columns_by_entity` compares each ACTIVE entity's `field_map` + `row_filters` columns (never `source_columns`, the documented cross-file auxiliary reads) with its own `source_files`' headers, and makes no claim for an entity unless every one of those files was observed with a header row. Where each entity's field_map is READ from is declared once, `preflight.OBSERVATION_SCOPE` (completeness-pinned against the registry): OWN_FILES for every entity except Enrollments (ALL_FILES — its id-role pair's teacher column is also read from ClassInformation, a Classes file) and StudentAttendance (NO_CLAIM — its field_map values are placeholders). `pipeline.observe_source_columns` — the one call site on BOTH entry points (AST-pinned) — wraps the derivation, and separately each entity's note + warning, in a broad handler logging at DEBUG (one entity's refused note never costs another entity its observation), so a raise there leaves delivered bytes, status, category and counts identical (raise-isolation twin, `tests/test_pipeline_run_store.py`). The creator's `preflight_report` keeps its own file-agnostic, cross-entity claim | `ENFORCED` (S6) |
| Guards are enforced once, where the column is used (§5) | As §5 states per site | see §5 |

## §11 Layering and justified broad excepts (P16)

Every row is pinned by `tests/test_architecture_fitness.py` (S13a): AST only, no new dependency; each rule has a detector self-test on synthetic violating source, and each rule's targets are a declared registry that fails loudly when a listed path or function is missing, so a rename can never make a rule vacuous. Import rules walk the WHOLE `src` import graph — function-local imports and package `__init__` edges included — so a chain through a helper module counts as an import.

| Rule | Today | Status |
|---|---|---|
| `src/etl`, `src/config`, `src/history`, `src/quality` never import flet or `src.ui_flet`; an explicit `FLET_FREE_MODULES` list never imports flet | True, and pinned (rules (a), (b)). `FLET_FREE_MODULES` names the ETL taxonomy's pure modules (`errors`, `outcomes`, `preflight`, `transformers/columns`, `transformers/notes`) and every `src/ui_flet` decision module measured flet-free; every other non-screen `src/ui_flet` module is declared `FLET_BOUND` (`theme.py` and `job_runner.py` among them — both import flet), so a new UI module must be classified, and a `FLET_BOUND` module that stops importing flet must move lists | `ENFORCED` (S13a) |
| Transformers never import `src.etl.pipeline` | True, and pinned (rule (c)) over every module under `src/etl/transformers`, transitively | `ENFORCED` (S13a) |
| Exactly one entity-scope broad handler, in `run_transform` | The bulkhead's `except Exception` (reasoned `noqa: BLE001`) is the only broad handler in `run_transform`, and none exists under `src/etl/transformers` outside the field-map engine — AST-pinned with doctored twins since S4, moved unchanged in strength to rule (d) in S13a, which also pins that no other handler claims the `entity bulkhead` reason | `ENFORCED` |
| Every in-scope `except Exception` carries a reasoned `# noqa: BLE001 — <reason>` | Every broad handler (`except Exception`, `except BaseException`, a bare `except:`, or a tuple naming one) in the four layers carries the annotation ON its `except` line, with a reason from the closed vocabulary below, optionally followed by `; <detail>` or `, <detail>` (rule (f); every vocabulary reason must also be in use). ruff's `BLE` rule is selected for exactly these layers (`pyproject.toml` `per-file-ignores` exempt the CLI, scheduler, SFTP, UI, utils, tests and scripts; a NEW top-level package under `src/` gets ruff's `BLE` check by default, but not rule (f)'s reason-vocabulary check until it is added to `LAYER_ROOTS` in `tests/test_architecture_fitness.py`) — but ruff skips a handler that re-raises or logs, and never reads the reason, which is why the fitness rule exists. Annotated, never narrowed: S13a changed comments only (`loader._commit_staged`'s rollback stays broad, because it must restore every original whatever failed). Convert's symmetric failure sink (`src/ui_flet`, outside this scope) keeps its own AST pin (`tests/test_convert_failure_record.py`) | `ENFORCED` (S13a) |

The closed reason vocabulary (`BLE_REASONS` in `tests/test_architecture_fitness.py`; this table is pinned to it):

<!-- failure-policy-table: ble-reasons -->
| reason | when it applies |
|---|---|
| `entity bulkhead` | The ONE entity-scope boundary, `pipeline.run_transform` (§2). No other handler may give it. |
| `re-raised` | The handler logs, records or rolls back, then re-raises the ORIGINAL with a bare `raise`. |
| `total by contract` | The function is documented TOTAL; any failure degrades to a stated default (and is logged where the contract says so). |
| `advisory only` | Source observation and labels (§8, §10): a raise there may not change the run's bytes, status or category. |
| `best-effort side effect` | Recording or tidying (run record, store, a settings tidy-up) that must never propagate or mask the failure it follows. |
| `field-map isolation` | The field-map engine's per-cell / per-column isolation, recorded to the run's data errors (§2 scope ladder). |
| `failure returned as a result` | The failure becomes a returned value the caller surfaces (SFTP delivery → logged at ERROR, exit 3). |

## §12 Checklists and DoD delta

| When you… | You must… |
|---|---|
| add an entity | declare it in `outcomes.ENTITY_CRITICALITY` (from S2 — unlisted means CRITICAL) with a §3 row; if it publishes `TransformContext` state it is CRITICAL; classify every column it reads into §5 (a)–(e) and state what happens if the column vanishes |
| add a check on a column | pick its §5 class with the §13 tree; for (a)/(b) call `columns.require_columns` where the column is read, tag the call (the comment line directly above it) `# failure-policy: pii_scope` or `# failure-policy: join_key` and add a `require-columns` row (pinned since S10); never catch-to-continue inside a transformer; add a §5 row. For (c)/(d)/(e) record the posture through `notes.record_note` (an `OutcomeNote` member with its §6 row, `NOTE_TIER` entry and copy — S11), tag the call `# failure-policy: <class>` and add its `tags` row (pinned since S11) |
| add a config knob | say which §5 class a missing referenced column falls into; bundled typos must fail at load (S12) |
| add a district | run its real drop; any column the district lacks must land in a §5 class you can name — see `docs/developer/adding-district.md` |
| add a failure reason or category | add the enum member, its copy row (S3) and its §6 row in the SAME change |
| finish a slice | flip your own rows here, update the parity test (from S2), record DECISIONS |

## §13 Choose your posture — five questions

Ask in order; the first YES decides.

1. **Could this widen delivered PII if the check were skipped?** → (a) `pii_scope`: fail CLOSED, typed, at the smallest scope containing the fault.
2. **Does the column key an identity or a join?** → (b) `join_key`: fail CLOSED, typed; never a raw `KeyError`, never ship what was half-built.
3. **Is the output field one the importer requires?** → (c) `contract_field`: exclude and COUNT the rows lacking it; all excluded ⇒ EMPTY with a reason.
4. **Is there a recoverability argument for failing OPEN** (a surplus row is recoverable, an empty deactivating file is not)? → (e) `safety_heuristic`: fail open in the surplus direction only, and RECORD it. Otherwise → (d) `optional_field`: blank, warn once, note it.
5. **Which entity scope contains the fault?** The transformer raises; `run_transform` alone decides entity vs run from §3. Never decide scope inside a transformer.

## §14 Patterns in use, with in-repo exemplars

| Pattern | Exemplar in this repo |
|---|---|
| Typed errors + canonical, injective messages; one log anchor | `src/scheduler/task_com.py` `MSG_*` + `windows._fail` / `_FAIL_LOG_FORMAT` (`windows.py:192`, `:217`) |
| Totality test over a closed enum (a new member without copy is RED) | `launcher._MACHINE_SCOPE_CAUSES` (`src/ui_flet/launcher.py:43`), pinned in `tests/test_ui_flet_launcher.py` |
| Reader before producer (an inert slice lands first) | plan 0049's inert provisioning slices (S-1b before S-2); here S3 before S4 |
| Origin-keyed severity (bundled RAISE, user-dir WARN — never invert) | `config/loader._apply_user_dir_domains_floor` (`loader.py:358`) |
| Exclude-and-count, never ship a blank the importer rejects | `family._exclude_rows_without_email` (`family.py:40`) |
| Measurement gate, then STOP and escalate | plan 0052 (staff role) — measured on real drops before the default changed |
| Required keyword-only safety parameter (no permissive default) | `upload_csvs(..., *, manifest)`; `_store_run_record(..., *, dry_run)` (`pipeline.py:668`) |
