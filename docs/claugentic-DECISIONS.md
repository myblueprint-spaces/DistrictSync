# Decisions (claugentic harness)

Append the **newest decision at the top**. Consult this log before re-litigating a past choice. Each entry is a dated one-liner: what was decided and why.

---

- **2026-07-16 — Config version gate rejects BOTH major directions (W2c, deviation reconciled toward safety).** `SUPPORTED_CONFIG_MAJOR=1` / minor ceiling 9, checked on the RESOLVED (post-`_base`-merge) version BEFORE Pydantic so out-of-range configs get the actionable message, not schema noise. A NEWER major also rejects (not warn-and-run): a major bump means semantics this build cannot honor, and student-PII output must not be driven by a partially-understood config. Same-major forward minor drift warns. User-dir shadowing stays allowed but is always named in an INFO line (both paths). When bumping the config format, migrate every bundled config in the same release.

- **2026-07-16 — Contract fixtures mirror REAL district GDE header shapes (W2a; headers verified against district extracts, all rows synthetic).** Quirk pins ride the module-scoped per-district pipeline run (indirect parametrization — zero extra cost). W2a also FOUND (not fixed — tests-only slice) the pre-existing **Name-config key mismatch**: `to_raw_dict` emits spaced keys ('primary teacher flag'…) while `classes.py`/`blended.py` read underscore keys — the configured Name columns are dead config, and SD60/SD74 class names silently omit the section letter. Scheduled into the Wave-4 config slice with a DELIBERATE SD74 snapshot update (partner-visible name change, best fixed before partners depend on the names).

- **2026-07-16 — Wave-1 verify fixes.** The `.bak_*` reconcile is age-gated (`_STALE_BAK_MIN_AGE_SECONDS`=1h): a younger backup may be a LIVE concurrent run's in-flight commit and moving it would break that run's rollback — the exact restore-before-cleanup invariant the wave hardened. Non-22 SFTP ports look up as `[host]:port`, which a plain-host pin does NOT cover: `config/known_hosts` documents the bracketed entry form (`ssh-keyscan -p PORT`), and a plain pin bypassed by a port form warns DISTINCTLY (never reads as generically unpinned). Also documented: `archive_stale_outputs` (now UI-reachable via Convert) keys off the broad transformer registry BY DESIGN (a non-destructive move), so if two configs ever share one output dir, running config A archives config B's CSVs out of the next SFTP zip — deliberate asymmetry vs the vanish-anomaly leg's narrow enabled-entities set (which avoids false WARNINGS); single-config-per-dir is the documented deployment.

- **2026-07-16 — Setup/scheduler trust fixes (W2b).** crontab reads are EXIT-CODE-first (`linux._read_crontab_lines`): exit 0 → parse; non-zero + "no crontab" → genuinely empty; anything else → abort loudly WITHOUT rewriting (`crontab -` installs a whole new crontab — a blind rewrite from a failed read would destroy the user's other jobs). `ReconcileOutcome` gained `BLOCKED` — `_register` now reports whether it actually dispatched, closing the 0034 S3 re-gate residual (a gate-refused Save says the schedule wasn't updated and names the run-time fix). SFTP form-port parsing is a pure seam (`sftp_copy.parse_port`; blank→22, typo→None) with fixed PORT_ERROR copy — a port typo can never again fall into the host-allowlist `except ValueError`. The wizard finish delivery-desync guard (`setup_flow.schedule_delivery_desync`) keys off the durable last-registered `schedule_task_args` record (guards resumed wizards too), is one-directional (delivery enabled + task baked without `--sftp` — the only wizard-reachable over-claim), and downgrades the finish copy honestly rather than re-registering from the finish line.

- **2026-07-16 — Dev-hygiene decisions (W2d).** The Setup probe dispatch is a module-level seam (`setup._kick_probe_thread`) purely so tests can stub the kick (the 2026-07-15 flake fix; do not re-inline). New AST ban in the render-smoke suite: `window.destroy/close/center` calls in `src/ui_flet/**` must be awaited or dispatched via `page.run_task` (async on flet 0.85.3 — a bare call silently no-ops); exceptions need an explicit reviewed allowlist entry (empty today). The fallback log handler mirrors `logging.conf`'s rotation limits via module constants (the .conf stays primary). GitHub-Actions SHA-pinning is PREPARED-NOT-APPLIED by the slice (offline) — the orchestrator applies real SHAs at integration where network exists.

- **2026-07-16 — Vanish-anomaly + output-dir hygiene design (W1b).** The anomaly check's expected-entity set derives from the new `pipeline.configured_entity_order(mappings, global_config)` — the exact enabled-entities-filtered list `run_transform` iterates, NEVER raw `mappings.keys()` (a `_base`-inherited-but-disabled entity must not fire a false vanish against another config's CSV). An unreadable previous CSV is a WARNED anomaly-check degradation (enters the anomalies list → ack-gate), not a silent skip and not a run failure — two prior silent-skip test pins deliberately updated. N→0 surfaces through the present→absent leg (zero-row entities never enter `outputs`). `.tmp_*` staging is re-creatable scratch (swept after 7 days); `.bak_*`/`archive_*` are DATA — a stranded `.bak_*` is MOVED to `archive_<ts>_recovered/`, never deleted. Staging/backup names carry a per-call uuid suffix + `exist_ok=False`. Reconcile is best-effort (a hiccup logs ERROR, never blocks the write).

- **2026-07-16 — Zero-orphan invariant extended to Family + StudentCourses (W1c; pre-approved judgment calls, OWNER-VISIBLE).** Family.csv and StudentCourses.csv are filtered to `context.active_student_ids` via the existing `filter_to_active` (source column from the entity field_map; missing roster → unchanged + warning, the Enrollments convention). Consequences the owner should see: live districts with stale emergency-contact extracts may see a one-time Family row-count drop (anomaly may fire once); mbp tiers now exclude transcripts of non-rostered students INCLUDING graduated ones — if myBlueprint+ needs graduate transcripts, add a per-tier opt-out (flagged in the PR, not assumed). Also: `generate_student_email` is now fail-loud (raises; the resilient caller records ONE aggregate data-error + blanks cells); non-numeric StudentCourses marks KEEP legacy not-passing scoring but record data errors (the alpha-marks owner question stays deferred, now measurable per district); `determine_school_year` warns on cross-source disagreement (first value still wins — the warning IS the fix); the quality report gains PII-free Family/StudentCourses orphan-count warnings.

- **2026-07-16 — SSH host-key pinning design (W1a).** Merged known_hosts resolution: user app-data file loads FIRST (paramiko `HostKeys.lookup` returns first-loaded, so user wins per host+keytype), then bundled `config/known_hosts`; per-entry merge keeps bundled pins for other hosts. Ships comment-only, so unpinned hosts stay accept+WARN — pinning can never break delivery before the owner populates keys. `HostKeyVerificationError` subclasses `RuntimeError`, NOT `SSHException` (the retry loop treats SSHException as transient; a changed server identity must never be retried); paramiko's `BadHostKeyException` re-raises as the same canonical error. Retry wraps the whole connect+put unit (fresh connection per attempt, zip built once, date-stamped name = idempotent re-put); `AuthenticationException` never retried (lockout risk); `test_connection` never retries (Setup click must answer fast; 0031 semantics byte-intact). A corrupt known_hosts file is logged ERROR and skipped — local-file corruption is outside the MITM threat model and must not brick nightly delivery.

- **2026-07-16 — `PreRegSchoolCode` → "Next school code" VERIFIED against real GDE headers (PENDING flag cleared).** The column exists verbatim ("Next school code", position ~56) in the repo's enhanced-demographics sample AND in SD74's + SD60's real `StudentDemoEnh`/demographics extracts. The mapping is correct; no code change needed. (Pre-partner Now batch item 6.)

- **2026-07-16 — SpacesEDU host-key acquisition is an OWNER step:** port-22 outbound is blocked from the dev network (`ssh-keyscan` returns nothing though DNS resolves), so the pinning slice ships the mechanism — strict reject on key MISMATCH, warn-on-unknown until keys land, bundled `config/known_hosts` + user-dir override — and the owner populates the keys by running `ssh-keyscan -t ed25519,ecdsa,rsa sftp.ca.spacesedu.com sftp.app.spacesedu.com sftp.myblueprint.ca` from a network that can reach the hosts (office/district server).

- **2026-07-16 — Housekeeping: backlog verified item-by-item vs code + YAGNI-gated; 29 landed/superseded plan files deleted.** Every ROADMAP item and plan file was re-verified against main (evidence-based, no status-line trust) and the survivors YAGNI-reviewed with partner distribution imminent. **Closed as done:** audit #3 early-exit records (0034 S4) · #16 tri-state read-back (0029) · the whole Plan-0012 UX section (realized on Flet or Streamlit-stale) · CUT-1 tkinter picker + windowed-main.py repackage · DRY `--version` onto `app_version()` · harness consolidation (only claugentic tree/hook/script remain) · `.gitattributes` · cross-school duplicate decision (`cross_enrollment.collapse`) · T1.3 shared pipeline core (`run_transform`/`load_from_bytes` + parity test) · T3.6 extractor sniff · scheduler delete/query follow-up (query migration happened via `read_schedule`; `delete_task` on schtasks is documented-deliberate). **Closed as CUT (YAGNI):** audit #6's log half (accepted D2a privacy split — the log is local ops diagnostics on the district's own server) · the missed-run 26/36h banner seam (already adjudicated — "Home owns that") · auto-`--sftp-test` at wizard finish (superseded by the in-step side-effect-free Test connection) · standalone wizard password pre-check (superseded — the in-wizard register IS the validation, with classified error copy). **Plans deleted** per the workflow's "plans are ephemeral (deleted at Land)": 0005–0031, 0033, 0034 + 0013-HANDOFF; kept `0032` (sole detail source for the queued 0035/0036 batches) + `TEMPLATE.md`. Preserved from 0010 before deletion: setuptools-scm was REJECTED because the minimal build-time tag-stamp (`flet-pack.yml` writes `src/_version.py` from `GITHUB_REF_NAME`) achieves exe-version correctness without a new build dependency. The pre-partner "Now" batch (7 verified items) is in the ROADMAP.

- **2026-07-15 — 0034 S3 verify fix: Settings reconcile returns a `ReconcileOutcome` tri-state** (`DISPATCHED`/`INTERRUPTED`/`NONE`, pure copy in `setup_flow.folders_save_note`/`sftp_reconcile_suffix`) so the folders + SFTP Save notes are honest — "updating…" only on a real dispatch, "confirm the schedule choice above" when the downgrade interrupt was shown (no more post-Cancel contradiction between the Save card and the schedule card). The signed-in-only downgrade registers with an explicit `force_blank_password` instead of relying on dialog modality + the transient field value.

- **2026-07-15 — Settings Save trust guards (plan 0034 Slice 3).** (a) The downgrade guard persists a boolean `schedule_unattended` fact at every CONFIRMED register (chosen over extending the fixed read-back script to emit `$task.Principal.LogonType`); consequence: installs that registered unattended BEFORE this build default to False and are unguarded until their next successful register — accepted; revisit the LogonType read-back if field reports show it matters. The choice dialog renders two EQUAL-WEIGHT outlined buttons + a text-tier Cancel — deliberately NO filled default, so the downgrade can never be triggered by reflex-clicking a dominant action. (b) Both trust guards live at the shared reconcile seam (`_ScheduleHandle.trigger_register` is the guarded reconcile entry; `persist_run_time` is the run-time-persistence seam), so the folders Save AND the SFTP Save inherit them; the Register BUTTON deliberately bypasses the interrupt — a blank-password Register there is a legitimate explicit choice. (c) Last-registered TaskArgs persist as `schedule_task_args` (plain JSON dict in config.json, captured at register click time), written on every confirmed register and cleared on confirmed unregister; `task_args_from_persisted` is defensive-TOTAL (absent/hand-garbled record → mount-snapshot fallback, never a Settings crash) because the record lives in a user-editable file. This makes Mapping's "open Settings and Save to update it" promise real: a no-edit Save after a district switch re-registers.

- **2026-07-15 — False-green kill scope + missed-run rule (plan 0034 Slice 4).** (a) The three `SystemExit` sites inside `run_pipeline` (input-dir missing, config missing/invalid) now record a failed run to BOTH sinks before exiting (input-dir reuses the existing `no_input` category — the closed set stays at 5 values; config → `config`); `main.py` argparse-level exits stay UNRECORDED (the `__main__` block is not importable and `tests/test_cli.py` deliberately bypasses argparse — revisit only if the CLI dispatch is refactored). (b) Missed-run precedence: LIVE read-back AND no record in 26h AND store `created_at` older than 26h → Home WARNING, slotted below FAILED_ETL/FAILED_DELIVERY (amber never downgrades red) and above ANOMALY/DATA_WARNINGS/staleness; every fact must positively hold, else silent (a false day-one alarm costs more trust than a late first warning); the 36h staleness proxy remains the fallback when the read-back is not LIVE. Run History's banner still carries NO schedule-attention verdict ("Home owns that"). (c) RunRow's privacy bar amended deliberately: the record's district (`sis_type` → friendly display) is the ONE sanctioned identity fact, surfaced only as the bounded "Different district: <name>" note; the free-text error/path prohibition is unchanged and re-pinned in tests.

- **2026-07-15 — Deliver is deliver-from-disk everywhere (plan 0034 Slice 2).** Every Convert deliver action uploads the already-committed top-level output CSVs via `deliver_job` (never a re-transform), dissolving the deliver-path anomaly auto-ack bypass — the >20% write-gate guards WRITES and a delivery writes nothing. Delivery-only run records carry NO entity counts (flat keys zero + a `delivery_only` rider in the store's JSON blob; no schema change): counts belong to builds — Home tiles fall back to the newest build record (or hide entirely), Run History renders "—" cells with "Delivered saved files" / "Delivery failed" labels, so a delivery never reads as a 0-row build. A successful standalone delivery REFRESHES Home's "last sync" freshness (the roster genuinely reached SpacesEDU); the freshness fact ("Files last built …") derives from the newest on-disk top-level CSV mtime — what's actually on disk, never a run record. Standalone deliver gate (`convert_output.standalone_deliver_state`): HIDDEN when SFTP is unconfigured or no top-level CSVs exist; the calm "Delivery isn't ready on this account" card only when a stored credential is the sole missing piece.

- **2026-07-15 — Mapping post-Apply schedule honesty (plan 0034 Slice 1).** Mapping NEVER re-registers the scheduled task and never collects credentials — a stale-schedule notice routes to Settings via `on_navigate("setup")` (the ONE register flow). Notice decision + copy live pure in `mapping_catalog.post_apply_presentation`: LIVE→assertive ("still uses <old district>"), UNKNOWN+hint→hedged ("may still use", D4 never asserts), MISSING/no-hint→no notice; the healthy detail claims folders only in every branch. Supersedes mapping.py's earlier no-on_navigate (KISS/IA-6) decision. Companion guard: the off-thread probe refine carries an apply/pick generation token, so a stale probe can never resurrect a cleared banner.

- **2026-07-15 — Mapping strategy [J, owner]: vendor-controlled YAML, NO admin-facing mapping editor.** Owner decision after testing v3.6.0, matching the unanimous 5-lens verdict of the 0032 sweep: an in-UI YAML/column editor for a non-technical, twice-a-year admin is the riskiest possible trust surface on a PII pipeline (one bad edit silently corrupts a nightly feed). Mappings are authored by myBlueprint, CI-tested, and baked into the exe; the **urgent hotfix path needs no release** — `config/loader._search_dirs` already searches the user app-data `mappings/` dir FIRST, so a corrected YAML can be dropped per-district. The roadmap "Config-mapping capability epic" (full editor, IA-8b) is RE-SCOPED: the editor is dropped; surviving scope = read-only Mapping transparency (plain-language per-entity detail + a "request a change" support path) as future polish. Also [J, owner]: **email-on-failure alerting deferred** ("ignore email alert for now").

- **2026-07-15 — Post-v3.6.0 program queue approved by owner** (batches, each its own session/branch/PR): **0034 trust & correctness** (Mapping-Apply task reconcile · deliver-from-disk which also dissolves the anomaly auto-ack bypass · Settings-Save honesty · false-green kill) → **0035 polish** (copy/vocabulary sweep · fix-routing · window geometry · About/support context) → **0036 bigger bets, discovery-first** (startup/installer spike · keyboard+UNC paths · Check-My-Setup + diagnostics export; NO email alerting) → **code debt** (student_courses config-driven columns; T1.x refactors planned separately). Detail lives in `.claude/plans/0032-ui-ux-sweep-proposal.md` + `.claude/plans/0034-trust-correctness.md`.

- **2026-07-15 — Plan 0033 Slice 1: DistrictSync DESIGN SYSTEM formalized as code + standard + skill (Direction B "Branded Professional").**
  - **Direction B chosen by the owner** from a 3-direction visual panel ("i like B please proceed"). Thesis: navy owns the nav rail, content stays white on a `#F7F9FC` wash, ONE brand-blue filled action per screen, a toned verdict band under a slim page header, navy 26px numerals on calm tiles. The approved mockup's `:root` custom properties + component shapes are the design contract.
  - **The system lives as CODE + doc + skill (owner: "formalise centralise standardise").** `tokens.py` extended (never broke existing names) with the `space_*`/`radius_*`/`type_*` scales + Direction B roles (rail navy/text, `color_content_wash`, toned status tints/lines + deep on-tint text, `color_action_primary_hover` `#174A96`, `color_action_outline` `#A9C3E8`); `components.py` is now the SINGLE source of control factories (`page_header`, restyled `HealthVerdictBanner` verdict band, `metric_tile`, 3-tier buttons, `card`+1dp shadow, new `district_chip`/`status_pill`). Standard = `docs/DESIGN_SYSTEM.md`; enforcement companion = `.claude/skills/districtsync-design/SKILL.md` (triggers on any `src/ui_flet` change). Screens adopt in Slice 2 — the restyled factories render every existing screen correctly meanwhile (render-smoke green).
  - **Secondary buttons are now GLOBALLY OUTLINED (hierarchy-inversion fix).** The old filled-navy `secondary_button` competed with the single filled primary for visual weight; Direction B makes secondary an `ft.OutlinedButton` (white bg, soft-blue `#A9C3E8` border, MB_PRIMARY text) so the ONE filled primary keeps the emphasis. Signature preserved (`disabled_bgcolor` etc.) — a pure restyle, no caller change. Verified on 0.85.3 that `ft.OutlinedButton` mirrors `FilledButton` (`content`/`icon`/`style`; per-state `side`/`overlay_color` maps) — recorded in `docs/FLET_1.0_CONVENTIONS.md`.
  - **AA is authoring-time enforced for every new painted pair.** Added 8 pairs to `UI_CONTRAST_PAIRS` (deep-on-tint 7.38–8.77:1, rail label 8.01:1, active rail 13.06:1, navy numerals on white 13.06:1, text/muted on wash 16.93/7.18:1); all clear ≥ 4.5. `color_action_outline` is a decorative UI-boundary line (not text) so it is deliberately NOT a contrast pair — the secondary's *text* (MB_PRIMARY on white, 6.53:1) is the gated pair.
  - **myBlueprint-mark-in-titlebar RESOLVED (same day).** Owner pointed at `docs/assets/` — `favicon.ico` there is the official myB "m" mark (transparent 16/32/48). Copied to `assets/myblueprint.ico`; new pure `paths.window_icon_path()` sets `page.window.icon` (running window/title-bar/taskbar = myB), while `app_icon_path()`/`flet pack --icon` keep `districtsync.ico` as the EXE-file icon and the rail keeps the sync glyph. (`docs/assets/myblueprint-logo.png` is mis-named — it is a product-UI screenshot collage, NOT the logo; do not use it as a mark.)
  - **Slice 2 — chrome + screens ADOPTED Direction B.** Navy rail (`nav_rail`: `color_rail_bg`, on-navy `color_rail_text` labels/icons, 12% white active pill, sync-glyph brand block + "DistrictSync"/"Roster sync for SpacesEDU", reassurance + Exit + `v{app_version()}` foot); content area on `color_content_wash` with a ~960px left-anchored reading cap (`shell`); every screen's gradient hero demoted to `components.page_header` (Home verdict-first with a district chip + Refresh in the header and a LIVE-only "Confirmed" schedule row-card; Convert district chip; Setup wizard `_step_header` + Settings header). Gradient reserved to `onboarding.py` alone. New `components.section_label` factory ("Latest roster"); `picker_field` dropped its transient `radius=12`. **Flet 0.85.3 honesty:** the mockup's 3px left accent bar is NOT expressible on a native `NavigationRail` — the active state is the 12% white pill + full-white label/icon (accepted per plan); the version foot uses AA-safe `color_rail_text` (a smaller size carries the "faint" read) rather than the mockup's sub-AA .38 white, keeping the contrast gate green. RESTYLE not rewire — `select_by_id` sync, the D4 badge probe, and all handlers/gates are untouched. No token changes (no new painted pairs). The 5→4 "Finish unnumbered" step count (0032 Tier-1 #10) was deliberately NOT folded in (not trivial — cascades into `setup_flow` + many tests).
  - **Approach (no CHARTER.md → harness grain):** the pure `tokens` contract was extended test-first (new AA pairs + scale-existence guards in `tests/test_ui_flet_tokens.py`, confirmed green before the factories), then the coverage-omitted view factories were restyled and pinned by the existing render-smoke. No independent test-author spawn — this runtime has no subagent tool (per the 2026-07-09 standing note); the token contract was authored first and its enumerated pairs/guards define acceptance.
  - **No-console `creationflags` is now a STANDING RULE.** `helpers.subprocess_no_window_flags()` returns `subprocess.CREATE_NO_WINDOW` on Windows (0 on POSIX — a no-op there), read via `getattr` so it type-checks cross-platform (CI mypy runs on ubuntu). **Every Windows-facing `subprocess.run` in this repo MUST pass `creationflags=subprocess_no_window_flags()`** — the windowed (no-console) PyInstaller exe otherwise flashes a console for every PowerShell/schtasks/icacls child, most visibly the `read_schedule` probe fired on Home/Setup visits + the nav badge (a console flash on every nav click). Applied to all four sites: `windows.register_task`, `windows.delete_task`, `windows.read_schedule`, `elevation._set_owner_only_dacl` (icacls). `elevation._shell_execute_runas` already used `nShow=SW_HIDE` (0), so the elevated child console was already hidden — the UAC consent dialog is OS-owned and still shows (expected). `scheduler/linux.py` (never Windows) + `ui_flet/convert_output.py` (Windows branch is `os.startfile`; subprocess only on darwin/linux) untouched. Regression guards assert the kwarg is present at each mocked site (a dropped flag → KeyError → test fail).
  - **Daily run time gains a `ft.TimePicker` clock affordance.** An adjacent `ft.IconButton(ACCESS_TIME_ROUNDED)` opens `page.show_dialog(ft.TimePicker(value=<datetime.time>, on_change=confirm))` — TimePicker is a `DialogControl` on 0.85.3, `value` is a `datetime.time`, confirm fires `on_change` / cancel `on_dismiss` (recorded in FLET_1.0_CONVENTIONS.md). The `run_time_field` TextField stays the SINGLE SOURCE OF TRUTH (all gating — `can_register_schedule`/`validate_run_time`/`TaskArgs` — reads its `.value`); the picker only writes `HH:MM` back + fires the field's existing refresh handler; typing stays allowed (an affordance, not a replacement).
  - **Setup ordering (user decision): District LEADS.** Wizard `STEP_ORDER` is now District → Folders → Delivery → Schedule → Finish (was Folders → District → …): "pick who you are first, then where your files live." **Delivery STILL precedes Schedule** (F1 unchanged — the nightly task's `--sftp` is baked at registration from `cfg.sftp_enabled`, so delivery must be committed before the Schedule step registers). Order-coupled followers updated in lockstep: `finish_summary_rows`, the onboarding hero step-preview line, and the wizard's one orientation line (moved to the now-first District step). Settings scroll reordered folders/district → schedule → delivery (overrides the earlier "#2a schedule FIRST" Firefighter-landing rationale — the user's what/where → when → destination mental model).
- **2026-07-15 — Plan 0031: SFTP trust gaps — Test/delivery parity, deliver-gate needs a stored credential, fail-loud empty upload. Two user product calls recorded.**
  - **Test parity: AUTH is the test.** Setup's "Test connection" used to require `sftp.listdir(remote_path)` — a capability delivery does NOT need (`sftp.put` never lists). Upload-only accounts (SpacesEDU-style) DENY listing, so Test reported FAILURE while delivery worked — the exact contradiction a district admin hit (Convert said "delivered", Setup said "failed"). Fix: `test_connection` now splits the listdir probe from connect/auth — `PermissionError` on listdir → `(True, LISTING_DENIED_NOTE)` (success-with-note), while `FileNotFoundError`/any other listdir error stays a FAILURE (a missing/wrong remote path breaks `put` too, so it IS a real delivery problem). paramiko maps SFTP_PERMISSION_DENIED → `IOError(EACCES)` ⇒ `PermissionError`, SFTP_NO_SUCH_FILE → `FileNotFoundError` (verified in the vendored paramiko `_convert_status`). `LISTING_DENIED_NOTE` is a FIXED module constant crossing to the UI by EQUALITY (`screens/setup._show_result` compares `msg == LISTING_DENIED_NOTE`); `sftp_copy.sftp_test_copy(listing_denied=...)` appends the note tail LAST (duplicated locally in the pure module — importing the uploader would drag paramiko into its import graph). `humanize._SFTP_REASON_RULES` left untouched (its permission-needle still maps real connect-level failures, only hit on the `ok=False` path).
  - **[J] Deliver gate now requires a stored credential — block + route to Setup (user decision).** Convert's deliver card was gated on `sftp_is_configured()` (config fields only), never checking that a password is stored/readable for THIS Windows account. When configured-but-no-credential, Convert now shows a calm "Delivery isn't ready on this account — add it in Setup → SFTP delivery" card instead of a deliver button that would fail (never blocks the local build above it; any uploader-construction/keyring error is treated as credential-absent, never a view crash). **DECLINED a pre-flight capability check before Deliver** and **DECLINED transient-password entry in Convert** — user chose deliver-then-report (honest exit-3 verdict after the attempt) and a single credential home (Setup), no second password entry point.
  - **`upload_csvs` fail-loud on an empty output dir.** It used to return `[]` silently when the dir had no CSVs → callers marked delivery "ok" (a false "delivered"; unreachable from Convert, reachable from a CLI/scheduled misconfig). Now `raise RuntimeError("No CSV files found to upload in <dir name>")` (name only, no PII path). The pipeline seam (`_sftp_upload`) already catches → `sftp_ok=False` → exit 3; Convert folds it into `BUILT_NOT_DELIVERED` — both the honest outcomes. `--sftp-test` on a listing-denied account now exits 0 with the note printed (delivery will work).
- **2026-07-14 — Plan 0030: SD60 clarification learnings — GENERATE student emails, drop Active No Primary, roster under Home School Number. Honest trade-offs recorded.**
  - **[J] User chose FULL email GENERATION over the evidence-based recommendation to keep file emails as-is.** SD60 emails are now generated as `{legal first}{legal surname}{2-digit admission year}@learn60.ca` for every active student. Measured consequence (real extract, aggregates only): this reproduces **~41%** of existing learn60 logins EXACTLY and **MIGRATES the remaining ~59%** to a new deterministic address. That is a **login-identity change** for SpacesEDU accounts keyed on the old email — accepted by the user as a **one-time standardization, NOT a silent side effect** (it also resolves 1,350 non-whitelisted domains + 736 blanks in one move). Recorded so the choice and its cost are traceable; this is a product judgment, not a correctness win.
  - **NN suffix = admission-date YEAR — a named, known-imperfect join-year proxy.** "Year joined the district" has no column in the extract; admission date (= joined *this* school) matches the existing NN suffix only **~43%** (full reconstruction ~41%). Chosen because it is the ONLY join-year proxy available; labelled as a proxy, not "the join year". Over the ~8,280 active students it yields **0 collisions, 0 empty local parts** (the suffix is necessary for uniqueness — without it 28 students collide); 10 rows lack a parseable admission year → `firstlast` with no suffix (still unique).
  - **Drop "Active No Primary".** SD60 confirmed these are online-course-only attachments, not real active enrolments. The SD60 `EnrollStatus` override was REMOVED → inherits base default `["Active","PreReg"]` (drops 37 ANP rows).
  - **`SchoolCode` → "Home school number".** Roster every student under their home school by reading the authoritative Home-School-Number column directly (100% populated; differs from per-row School Number on 1,617 rows) instead of inferring it from the kept row — judged more correct for SD60's cross-enrolment shape.
  - **New OPT-IN, config-driven email capability (additive, default off → every other district byte-identical): `sanitize` + `derived_dates`.** `sanitize: true` reduces each substituted string to `[a-z0-9]` (strips apostrophes/hyphens/spaces from names); `derived_dates: {pseudo: {column, date_format}}` injects a date part (e.g. 2-digit year) derived from a source date column, REUSING base's existing `friendly_date_format_to_strftime` + a new `derive_date_part` helper (empty on blank OR unparseable — never a garbage suffix). `FieldEmailFormat`/`EmailDerivedDate` carry `extra="forbid"` + `min_length=1` (fail-loud at config load); round-trip conditionally omits both defaults (keeps `test_sd51_custom_email`'s exact `{"format": ...}` equality green). Missing derived column → fail-loud `ValueError` at transformer boundary.
  - **DECLINED an explicit `cross_enrollment.school_column` decoupling (YAGNI, data-backed).** Because `SchoolCode == home_school_column` now, the existing collapse degenerates to keep-first-row-per-User-ID. This is deterministic on the current extract: all 386 cross-enrolled students have identical admission-year AND homeroom across their rows, and the fully-generated email (legal names + admission-year) is identical under first-vs-home selection → **0 differing emails** (other kept-row fields not exhaustively diffed). A future extract with cross-row variance would warrant the decoupling; not built now.
- **2026-07-14 — Exe size (~186MB) is ACCEPTED as-is; the `light` flet-desktop flavor is DECLINED for Windows/macOS.** Measured composition: the embedded `full` Flutter client is **99.8MB (~53% of the exe)**, then pandas(66)/numpy(33) compressed + cryptography/pydantic_core/Python runtime — none removable (Flutter renderer = the offline/no-WebView2 value prop; pandas = the ETL engine). Switching Win/mac from `full`→`light` (only Linux uses `light` today) could save ~30–45MB (~20%), but PLAT-0b only *verified* `light` on Linux, so it carries a real cross-platform build/render-break risk. **Decision: keep `full` on all three OSes** — for a signed, district-distributed PII tool, a proven-good 186MB beats an unproven 150MB. (No UPX either — AV/SmartScreen false-positives + undermines EV signing.) Dev-only deps (playwright 106MB, pytest, hypothesis, flet-web→fastapi/uvicorn) are provably NOT in the exe (the import graph excludes them; the 186MB total confirms it). Do not re-propose the `light` optimization without a green Win+mac UI smoke first.
- **2026-07-14 — Slice 8 open product decisions RESOLVED + implemented (lightweight path).** (a) **No separate welcome screen** — that would add a second entrance and break one-front-door (D10); instead a single calm one-line "what's ahead" preview (the four input steps named + a ~3-minute estimate) sits subdued in the onboarding hero (`screens/onboarding.py`), and the start-card's now-redundant step enumeration was trimmed to a resume reassurance. (b) **No confetti** — the finish moment is an honest *checked-summary* card (calm, not festive — a trust instrument for district admins): one row per input step (Folders / District / Delivery / Schedule, wizard order) showing configured (✓, M3 `CHECK_CIRCLE`) vs deferred (subdued `PENDING`), with the concrete value (friendly district, nightly time, "SpacesEDU"). Derivation lives in the pure COUNTED `setup_flow.finish_summary_rows` / `FinishSummaryRow`, fed the SAME computed facts as `finish_copy` (single source — the card can never contradict the copy); delivery "configured" is deliberately NARROWER than the flow's `_DELIVERY_SATISFIED` (a SKIPPED delivery is *deferred* in the card, matching the honest copy — a stored/tested credential means "configured", never "delivered"). Unit tests in `test_ui_flet_setup_flow.py` (all-configured / all-deferred / mixed / honesty edges).
- **2026-07-09 — Plan 0029 (Flet UI trust & professionalism) LANDED — 12 slices + close-out; harness lessons harvested.** Trust-instrument redesign: config freshness (D1) · chrome/input craft (D2) · nav integrity (D7) · deep-seam test isolation + canary (D3) · SQLite run store (D2) · cross-platform app-data via platformdirs (S11) · schedule-truth tri-state read-back (D4/D4a) · per-op DPAPI UAC elevation (D5) · side-effect-free provenance-honest SFTP test (D6) · wizard→Settings hybrid + one front door + explicit district (D8/D9) · Convert output visibility (D10). Verify dial validated: every fanned-out lens (honesty on trust-copy slices, +security on elevation, product on the wizard) caught real findings, and the whole-feature closing pass caught a cross-slice bug (F1, below) all 12 per-slice gates missed — composition seams now carry their own regression tests. Durable lessons promoted → INVARIANTS (path seam · schedule honesty · elevation boundary), `docs/claugentic-standards/CANDIDATES.md` (honest-status-copy · deep-seam isolation · closing-pass evidence), FLET_1.0_CONVENTIONS (async destroy no-op · on_submit bypasses gates · --icon). Open user decisions → ROADMAP (welcome screen; finish visual treatment). Residual = the manual-Verify checklist (real UAC matrix, frozen-exe chrome, live SFTP, first-launch migration, clean-profile wizard walk).
- **2026-07-09 — Standing note: this runtime has no subagent-spawn tool, so red-first / test-first is single-agent-upheld.** The "pure module + tests-first, tests-as-acceptance" default and any "proven red before green" claim are held by one agent (stated once here; slices reference this rather than re-disclosing each time). A true independent test-author split is unavailable in this environment — the claim is that the pure module was built first and its enumerated tests define the contract, not a separate-author guarantee.
- **2026-07-09 — Plan 0029 closing-verify F1 (cross-slice integration bug, Slice 8 surface): the nightly task's `--sftp` is baked at registration from `cfg.sftp_enabled`, but the wizard registered BEFORE delivery was configured → the natural walk shipped a task with no delivery. Fix: (1) reordered the wizard to Folders→District→**Delivery→Schedule**→Finish so delivery is committed before the task is baked (structural, no re-registration machinery/2nd UAC); (2) routed Settings' SFTP Save through the SAME `task_args_changed` reconcile the folders Save uses (enabling SFTP on a registered schedule now re-registers with `--sftp`) + refreshed the snapshot after any confirmed register (N1); (3) `finish_copy` now keys the delivery-confident claim off PERSISTED delivery (`STORED_CRED_PRESENT`), never transient `TESTED_OK` — a tested-but-unsaved delivery says "click Save", never "will try to deliver" (inversion fixed). Composition regression tests added (wizard-walk register carries `--sftp`; Settings enable-SFTP re-registers; finish claims delivery only when persisted). **Accepted residual (closing verify, non-blocking):** a non-linear backtrack (register Schedule → Back → Save SFTP → Finish without re-registering) can leave the task without `--sftp` while the finish claims delivery — narrow reachability, self-heals on the next Settings edit via the reconcile; guard option recorded on ROADMAP.

- **2026-07-08 — Plan 0029 Slice 9 (Convert output + explicit district, D10/D9-convert-side): `convert_output` pure gate + view-layer output visibility + fail-loud output-dir — judgment calls recorded.**
  - **Approach = pure COUNTED module + tests FIRST, then thin view (recorded default APPLIED).** Same trust-UX-feature work-type as Slices 4b/5/6/7/8 — the harness's recorded default (pure module + tests as the acceptance contract) was APPLIED, not re-judged (no CHARTER.md). `convert_output.py`'s gate/caption/open-folder tests were written first and pass 100%. **No independent test-author spawn — this runtime has no subagent tool** (stated plainly so the claim is falsifiable, consistent with the prior slices' record); the pure module was built first and its tests define the contract, but a true red-first author-then-green split is not available here.
  - **New COUNTED module `src/ui_flet/convert_output.py`, NOT an extension of `convert_result`.** SRP: `ConvertResult`/`summarize` is the PII-free result model and must stay **path-free** (its docstring rule + `test_ui_flet_convert_result` privacy assertions are untouched and still green). The output-folder PATH is app-owned config (never student PII) and belongs at the view layer — so the path-BEARING decisions (`can_run_convert`, `output_dir_is_set`, `resolved_output_caption`, `open_folder`) live in a deliberate counterpart module, keeping the two concerns cleanly apart.
  - **`open_folder` is effectful-but-mockable in a COUNTED module (mirrors `filepicker.check_writable`).** Per-OS dispatch — `os.startfile` (Windows) / `open` (macOS) / `xdg-open` (Linux); list-form args, no `shell=True`, never raises (OSError → WARNING → `False`). Fully mocked in tests (no real file browser opens); `# nosec B606` on `os.startfile` (app-owned dir, no shell, no user interpolation — matches the repo's B603/B607-skipped subprocess posture).
  - **No silent fallbacks (D9/D10 killed both):** the district prefill drops the alphabetical `configs[0]` guess — prefill ONLY from a valid saved `sis_type`, else a "Choose your district" placeholder (`hint_text`, `value=None`) with Run disabled; and `convert_job`'s `AppConfig.load().output_dir or input_dir` fallback is **replaced by a fail-loud `raise ValueError`** (fail-fast, before any I/O), enforced at the view gate AND defensively in the boundary. Convert can no longer write into the *input* folder or run against an implicit district.
  - **Gate consolidated through the one pure predicate.** `can_run_convert(district_chosen, output_dir_set, input_valid)` is the single source the button's `disabled` reads (both `_refresh_convert_gate` and the post-run `_set_running(False)` route through it); runtime single-flight (`JobRunner.can_start`, DONE/ERROR post-run) stays an orthogonal view concern layered on top. District is now part of the gate, so `_on_district_change` re-checks it.
  - **Routed copy points to "Settings" (not "Setup").** The graduated surface titles itself "Settings" (Slice 8) and Convert is a post-setup surface, so the caption ("Files will be written to <dir> — change it in Settings.") and the unset blocked message ("Set your output folder in Settings first…") name where the admin actually edits it; the rail label stays "Setup" for spatial memory.
  - **Auto-select-iff-one deliberately NOT added on the convert side (YAGNI).** `available_configs()` always returns all 10 bundled configs, so the D9 "exactly one config" branch is unreachable here — the explicit deliverable ("prefill ONLY from a valid saved sis_type") is honored without dead code.

- **2026-07-08 — Plan 0029 Slice 8 (Setup wizard→settings + one front door, D8/D9-setup/D10-IA): pure `setup_flow` + wizard/settings hybrid + `sis_type` default flip — judgment calls recorded.**
  - **Approach = pure state machine + tests FIRST (the plan-gate sizing note), then the view; enumerated-tests-as-acceptance (recorded default APPLIED).** Same trust-UX-feature work-type as Slices 4b/5/6/7 — the recorded charter default (pure module + tests first, enumerated tests as the acceptance contract) was APPLIED, not re-judged (no CHARTER.md). `setup_flow.py` (the resume/gate/finish-copy/reconcile-predicate owner) was written with its full 47-case transition table alongside it (100% covered). **No independent test-author spawn — this runtime has no subagent tool** (stated plainly so the claim is falsifiable, consistent with the prior slices' record); the pure module was built first and its tests define the contract, but a true red-first author-then-green split is not available here.
  - **Wizard state is PURE + injected; the view does all I/O.** `derive_flow(FlowInputs) -> FlowState` consumes ONLY injected facts (`folders_valid`, `district_chosen`, `schedule: ScheduleStatus | None`, `schedule_skipped`, `delivery: DeliveryFact`) — resume = the first pre-finish step not truthfully satisfied (NO stored cursor; the view feeds the same tri-state `ScheduleStatus` every surface uses + a synchronous keyring read for the stored-credential reconcile). Schedule satisfaction is LIVE-or-skipped only (UNKNOWN/MISSING/None never satisfy — the D4 honesty invariant carried into the wizard); a `None` schedule resumes ON the Schedule step so its own read-back reconciles ("already scheduled") instead of double-registering. `DeliveryFact.NONE` added beyond the spec's four facts as the genuine "not addressed yet" initial state.
  - **No step flips `setup_completed` — the finish confirmation is the ONLY completion signal.** `setup_flow` has no `setup_completed` concept at all (a test asserts the module + `FlowState` never carry the field); `can_finish` is REACHABILITY, not "set up". The view's Finish button is the sole writer (`cfg.setup_completed = True`), so a mid-wizard abandonment can never read as set-up (D4a). `has_completed_setup()` gates wizard-vs-settings; the launch already lands on the wizard while not completed (Slice 3/5).
  - **Adaptive finish copy = THREE honest, byte-exact variants (honesty register).** `finish_copy` names what was checked and WHEN, never a future guarantee: schedule-skipped ("build <district> when you run a conversion… add a nightly schedule whenever you're ready" — no "tonight" claim), delivery-deferred ("Tonight[ at T] … into your output folder. Set up delivery whenever you're ready."), delivery-tested ("Tonight[ at T] … try to deliver to SpacesEDU — we tested the connection to <host> as <user> just now and it worked."). The next-run time comes ONLY from the OS read-back's `next_run_display` (timeless "Tonight" when absent — never the config `schedule_time` presented as verified, consistent with Slice 5).
  - **ONE register flow + ONE keyring path — the wizard REUSES the Slice 5/6/7 sections verbatim.** `_build_schedule_section` (now returns `(card, handle)` + an optional `on_status` callback) and `_build_sftp_section` (optional `on_delivery` callback) are embedded UNCHANGED by both the wizard's Schedule/Delivery steps AND Settings mode — no fork of `register_task`/`test_connection`/`store_password`. The wizard tracks live-ness/delivery outcome via the callbacks for its resume + finish copy. The existing Slice-5/6/7 view tests (register/unregister crash-nets, side-effect-free SFTP Test, provenance copy) now drive Settings mode (a `setup_completed=True` config) — the shared sections make the behaviour identical in both modes.
  - **Settings reconcile = one Save + a pure `task_args_changed` predicate.** Settings mode's folders/district Save persists, then re-registers via the schedule section's handle ONLY when a task-baked field changed (input_dir/output_dir/sis_type/sftp flag/run_time — SFTP host/user/remote/port are NOT task-baked, so a change to them can't be expressed in `TaskArgs` and never forces a needless re-register) AND a schedule is live. Re-uses the same register flow (incl. elevation) so tonight's action isn't left pointing at stale paths; the register flow surfaces its own confirmed/logged-on-only outcome (an honest downgrade is visible, never silent).
  - **D9 `sis_type` default flip to `""` — blast-radius audit.** `AppConfig.sis_type` default is now `""` (pinned here) so a fresh install has NO pre-selected district; the wizard's District step shows a "Choose your district" placeholder (`hint_text`, `value=None`) and auto-selects ONLY when exactly one config exists (`auto_selected_district`). Audit: the CLI is UNAFFECTED (`--sis` is a required arg, `main.py` errors without it — never defaulted from `AppConfig`); `validate_sis_type("")` raises, but `setup_state`/`is_complete()` gate on a non-blank district so `""` can never reach `run_pipeline` via the UI; `convert.py`'s prefill (Slice 9's territory — untouched here) still falls back to `configs[0]` for display, unchanged. Tests constructing `AppConfig()` and asserting the old `"myedbc"` default fixed to be explicit (`test_app_config` ×3, the mapping-revert render-smoke).
  - **One front door (D10).** The unconfigured Home hero (`onboarding.py`) is the single entrance — its CTA opens the wizard (rail-follow already works). The old three-step "Getting started" preview was removed because it duplicated the wizard's own guided path (a competing set of instructions); the hero is now hero + verdict + one calm line + "Start setup".
  - **MANUAL-Verify (recorded, not automatable here):** a clean-profile first run opens ON the wizard, walks Folders→District→Schedule(skippable)→Delivery(skippable)→finish, and lands in Settings on next visit; keyboard focus lands on each step's first field (autofocus wired on the District dropdown; the reused schedule/SFTP sections keep their existing focus behaviour — a best-effort polish, Flet focus is manual-Verify); real UAC on the wizard's Schedule step; changing the output folder in Settings re-registers the live task.
  - **Verify refinements (product-designer Elevate — 5 honesty/landing fixes + 2 gate items).** (1a) The finish HEADLINE is now variant-adaptive: scheduled → "You're all set."; schedule-skipped → "You're set up — nightly sync not scheduled yet." (no over-signal at the peak). (1b) A completed-but-CONFIRMED-unscheduled install's Home + Run History empty state drops the auto-sync-implying "new syncs will appear" for the honest "Your roster won't sync automatically until you add a nightly schedule — set one up in Settings… Manual conversions from the Convert tab appear here too." — calm WARNING, NO fix CTA/badge (manual-only districts aren't nagged); fires ONLY on a CONFIRMED-MISSING read-back, never an unconfirmed None/UNKNOWN (honesty inverse — never deny a schedule we can't see). (2a) Settings-mode card order is now Daily schedule → Delivery → Folders/district (the operational heart first; the MISSING fix-CTA lands the Firefighter on the schedule card) — wizard step order unchanged. (2b) The MISSING fix CTA reads "Fix the nightly schedule" (action, not destination; still routes to Setup). (3) `_schedule_readout_line` styles by ATTENTION, not state: attention → failed red/error; a calm first-run MISSING → muted/neutral (no false "broken"); the MISSING copy is de-circularized on the Setup surface (new `surface` param on `derive_schedule_status`/`probe_schedule` → "add one below" vs "in Setup" elsewhere). (4) One orientation line on wizard step 1 only. (5) The finish STEP TITLE is "Finish" so the banner owns the peak (no double "You're all set"). Gate: (F4) restored the I1–I6 password-contract pointer comments on the reused register/SFTP handlers; (F5) added a cross-platform render-smoke for the LIVE-schedule finish path (finish body consuming `status.next_run_display`, via a stubbed schedule section firing `on_status`). The fuller welcome-screen + distinct finish visual-treatment questions are flagged to the close-out (Slice 10), not built here.

- **2026-07-08 — Plan 0029 Slice 7 (SFTP truthfulness, D6): side-effect-free Test + provenance-honest copy + Save-only keyring writes — judgment calls recorded.**
  - **Approach = red-first for the bug element, tests-as-acceptance for the pure helpers (no CHARTER.md; same trust-slice work-type as 4a/4b/5/6 — recorded default APPLIED, not re-judged).** The genuine bug (`_test` wrote the keyring BEFORE the network call, so a typo'd Test clobbered a working credential) got a red-first view-level pin (`test_failed_test_leaves_stored_credential_intact`) proven RED against current behaviour (`'typo-pw' == 'working-pw'`) before the fix. The new `password_override` + `sftp_copy` helpers are feature additions with the plan's enumerated tests as acceptance. **No independent test-author spawn — this runtime has no subagent tool** (stated plainly so the claim is falsifiable, consistent with the prior slices' record).
  - **Test is side-effect-free via a transient override, never a keyring write.** `SFTPUploader.test_connection(password_override=...)` threads a typed password to `client.connect()` ONLY (`_connect(password_override)` uses `password_override or self._get_password()` — a blank field falls back to the stored keyring credential, the nightly path). The override never touches the keyring, never a log, never the returned message (host:port core message kept). `screens/setup._test` DELETED its pre-network `store_password` call; the keyring is now written EXCLUSIVELY in `_save` (asserted called-once). Net: a failed/typo'd Test can no longer overwrite a working stored credential (the D6 trust gap).
  - **Provenance-honest success copy in a NEW pure module, not `setup_gates`.** Placed `sftp_test_copy` + `sftp_form_differs_from_saved` in a dedicated COUNTED `src/ui_flet/sftp_copy.py` rather than appending to `setup_gates.py` (SRP > KISS: `setup_gates` is narrowly "submit-gate predicates"; copy composition + an unsaved-edits predicate are a distinct concern). The copy names host + user + credential source: stored → "…using the password saved in this computer's credential manager." / typed → "…using the password you just entered — click Save to keep it." When any of host/user/port/remote in the FORM differ from the saved `AppConfig`, it appends "These settings work — click Save to use them for the nightly sync." and NEVER asserts the nightly sync can deliver for unsaved values (the killed over-claim was the old fixed "Your SFTP credentials work — the nightly sync can deliver."). The only no-Save-prompt state is stored-credential + saved-settings (exactly what the nightly already uses). Failure copy unchanged (bounded `friendly_sftp_reason` categories).
  - **Unsaved-edits = a pure predicate, port-normalized.** `sftp_form_differs_from_saved(cfg, host, username, remote_path, port)` compares stripped strings + an int-normalized port (form ports arrive as strings; an unparseable port reads as a difference → softer copy, never raises); the password is excluded (it never lives in `AppConfig`). The provenance + unsaved decision is computed on the UI thread BEFORE the worker so the off-thread marshalling is unchanged (I6 pattern preserved).
  - **MANUAL-Verify (recorded, not run here — network):** one real Test against the live SpacesEDU host still succeeds using the stored credential on this machine (blank password field → stored-credential path → "…saved in this computer's credential manager").

- **2026-07-08 — Plan 0029 Slice 6 (Per-operation elevation, D5): `elevation.py` DPAPI IPC primitive + self-elevated register/unregister — security judgment calls recorded.**
  - **Approach = security-sensitive feature build; the plan's enumerated tests are the acceptance contract (recorded default APPLIED).** Same work-nature as 4b/5/11 ("durable trust/security contract with an invariant"); no CHARTER.md, so the recorded default (enumerated-tests-as-acceptance) was APPLIED, not re-judged. **No independent test-author spawn — this runtime has no subagent tool** (stated plainly so the claim is falsifiable, consistent with the prior slices' record). The password-lifecycle security assertions ARE the acceptance and were written alongside the code.
  - **SRP split: `src/scheduler/elevation.py` is a GENERIC Windows elevation IPC primitive (no Task-Scheduler logic); `windows.py` consumes it.** elevation owns DPAPI protect/unprotect, the request/result handshake, `run_elevated_powershell` (ShellExecuteExW), and `sweep_orphans`; windows owns the register/delete bootstrap SCRIPTS (task logic) and the outcome→message mapping. Keeps elevation reusable (e.g. a future elevated action) and unit-testable in isolation.
  - **Inbound password channel = DPAPI CurrentUser + app entropy, in a random owner-only-DACL file; NEVER LocalMachine.** The password crosses the elevation boundary ONLY inside a DPAPI-sealed `dsync_elev_*.req` file (`CryptProtectData`, `CRYPTPROTECT_UI_FORBIDDEN`, entropy `DistrictSync/elevation/v1`) — never argv (ShellExecuteEx cannot pass env, so only the encoded bootstrap + base64 PATHS ride the command line), never the parent env, never a log. **CurrentUser scope IS the confidentiality boundary:** an over-the-shoulder UAC consent under a *different* admin SID cannot decrypt → the child's `Unprotect` throws → it writes the `DSYNC_DIFFERENT_ACCOUNT` sentinel and exits (**fail closed**). LocalMachine scope was rejected (would let any account on the box decrypt, downgrading a domain credential's confidentiality). Outbound result = **plaintext** `{ok, message}` (no secret — panel-rejected encrypting it), written atomically (temp + Move-Item); `read_result` degrades missing/partial/non-dict → `None`.
  - **Request-PATH embedded in the FIXED bootstrap as base64 — sanctioned + injection-proof.** ShellExecuteEx can't pass env to the elevated child, so the handshake paths must be embedded in the script text. They are OURS (random `tempfile`-style names under `user_data_dir`, NOT user-controlled), and base64-encoding them (charset `[A-Za-z0-9+/=]`) makes a profile dir containing an apostrophe/space unable to break the surrounding single-quoted PowerShell literal. The register BODY is single-sourced (`_register_body`) between the direct script and the elevated bootstrap — no forked second copy of `Register-ScheduledTask`.
  - **System32 pin + bounded wait.** `run_elevated_powershell` runs the ABSOLUTE `%SystemRoot%\System32\WindowsPowerShell\v1.0\powershell.exe` (elevation raises PATH-hijack stakes — never a bare `powershell`), `SEE_MASK_NOCLOSEPROCESS` + `SW_HIDE`, then `WaitForSingleObject` with a bounded 120s timeout (never `INFINITE`; on timeout `TerminateProcess`), `GetExitCodeProcess`, handle always `CloseHandle`d in `finally`. Outcomes are the bounded `ElevationResult` (LAUNCH_FAILED / DECLINED = `ERROR_CANCELLED` 1223 / TIMEOUT / COMPLETED) — never a raw win32 code.
  - **Success is CONFIRMED via read-back, never assumed from an exit code.** After the child reports `ok`, `register_task` calls Slice 5's `read_schedule`; only `found=True` returns success. An elevated-registered task a filtered token can't read yields `found=None` → honest "couldn't confirm" (`_MSG_ELEVATION_NO_RESULT`), not a false green. Canonical secret-free markers (`_MSG_UAC_DECLINED` / `_ELEVATION_TIMEOUT` / `_ELEVATION_NO_RESULT` / `_DIFFERENT_ACCOUNT` / `_ELEVATION_LAUNCH_FAILED`) are the message contract `setup_errors.classify_schedule_error` keys off (imported, single-sourced). A 5th category `_ELEVATION_LAUNCH_FAILED` was added beyond the spec's 4 (a bounded, honest "prompt couldn't be shown" rather than a raw win32 error). The old un-elevated "run as administrator" copy is superseded on the register path but kept for residual raw access-denied.
  - **DACL is best-effort (icacls), DPAPI is the real boundary.** Owner-only DACL set via `icacls /inheritance:r /grant:r <user>:F` (trusted System32 binary, auditable, no new dep, shell=False) rather than a large `SetNamedSecurityInfo` ctypes dance — a failure is logged, not fatal, because DPAPI CurrentUser already guarantees another account cannot decrypt. `sweep_orphans` (1h TTL) runs at both entry paths so a crash between write and cleanup can't strand a SID-locked request file.
  - **Whole register/unregister flow moved OFF the UI thread (setup.py) with visible states.** The blocking UAC call runs via `page.run_thread` → `page.run_task` marshalling (the SFTP-test pattern) with in-flight copy ("Waiting for the Windows permission prompt…"), then the classified outcome + the Slice-5 read-back refresh. Unregister escalates to `delete_task_elevated` (same UAC path, no DPAPI — a delete carries no secret) when the plain `schtasks` delete returns access-denied on a Highest task.
  - **HARD test-safety guard (RULE: no real UAC in tests).** An autouse conftest fixture (`_no_real_uac`) patches the lowest `_shell_execute_runas` seam to return DECLINED, so a `register_task(run_as_password=...)` call that mocks `subprocess.run` but forgets `is_elevated` on a non-elevated Windows dev host degrades to a fast decline instead of a real UAC dialog. The pre-existing run-as tests are pinned to the already-elevated DIRECT path via `is_elevated -> True` (autouse in `test_scheduler_runas.py`; per-test elsewhere) — the D5 self-elevation path is covered separately in `test_scheduler_elevation.py`. **Discovery flag:** a full-suite run BEFORE this guard landed likely surfaced one real UAC prompt (test host is non-elevated); a read-only `schtasks` query confirmed NO stray `DistrictSync_Daily` task exists (consent was never granted).
  - **Tri-lens verify refinements (gate/security/honesty).** (a) **Timeout is post-consent, so it's resolved by read-back, never asserted as failure:** TIMEOUT (and a missing result) now run the same `read_schedule` confirmation — `found=True` → confirmed success ("it just ran long"); otherwise the HEDGED `_MSG_ELEVATION_TIMEOUT` copy ("may or may not have been created — check the schedule status") replaces the false "before it was answered / nothing was changed". (b) **Elevated delete is read-back-confirmed too** (`delete_task_elevated` → `_confirm_removal`, only `found=False` is a confirmed removal) — the child's self-reported `ok` is never trusted alone (security F4); unconfirmed → `_MSG_ELEVATION_REMOVE_UNCONFIRMED` ("couldn't confirm removed", never assert non-removal). (c) **Error-card headline branches** "Couldn't confirm the schedule" (no-result/timeout) vs "Couldn't register the schedule" (declined/different-account/launch-failed/raw). (d) **Both off-thread workers have a crash-net** (`try/except` → always marshal a calm fallback → spinner+buttons released, never stranded). (e) **Sealed-file cleanup boundary:** `write_request` moved inside the try (`req_path=None` sentinel) so a build/launch error can't strand the DPAPI file until the 1h sweep. (f) **Hardening:** DPAPI entropy is base64-embedded in the child scripts like the paths (uniform injection-proofing); `read_result` caps the read at 64KB (oversized → None). (g) In-flight copy broadened to "Asking Windows for permission and registering the schedule…" (covers the ~120s post-accept child run).

- **2026-07-08 — Plan 0029 Slice 5 (Schedule truth, D4/D4a): tri-state read-back + `setup_completed` split + Setup Unregister/readout — judgment calls recorded.**
  - **Approach = feature build, pure module + tests FIRST (the plan-gate sizing note); test-first for the pure derivation, red-first proven.** `schedule_status.py` (the LIVE/MISSING/UNKNOWN owner) was written test-first: the full precedence-table test ran RED (module absent) before the module existed, then GREEN. Consistent with the prior slices' record: **no independent test-author spawn — this runtime has no subagent tool** (stated plainly so the claim is falsifiable). Same work-type as 4b ("durable trust contract with an honesty invariant") — the recorded default (enumerated-tests-as-acceptance) was APPLIED, not re-judged. (No CHARTER.md; deliberately not introducing one mid-slice — an absent charter defaults to the harness grain, and a new managed doc is out of this slice's scope.)
  - **Tri-state read-back replaces the dead `query_task`.** `read_schedule -> ScheduleReadback` combines `Get-ScheduledTask` (existence + action path) and `Get-ScheduledTaskInfo` (NextRun/LastRun/LastTaskResult) in ONE fixed `-EncodedCommand` script (same injection-free hardening as `register_task`), bounded by a ~10s timeout. **UNKNOWN-vs-MISSING is decided by the cmdlet's OWN error, verified against real PowerShell:** the ObjectNotFound catch (`CategoryInfo.Category -eq 'ObjectNotFound'` / `FullyQualifiedErrorId -like 'CmdletizationQuery_NotFound*'`) emits `DSYNC_ABSENT` → `found=False` (MISSING); any other non-zero exit / timeout / missing PowerShell / non-Windows → `found=None` (UNKNOWN). Live-smoked: a non-existent task returns `found=False`; a real task returns the exact `{found,next_run,last_run,last_result,action_path}` JSON with invariant `'o'` datetimes (offset-bearing) parsed correctly. Only `found=False` may claim "not scheduled"; UNKNOWN never asserts a schedule from the config hint (the Event-141 honesty fix).
  - **Contradiction = the record gap ONLY, with hedged copy (Verify-tightened).** The fired-but-no-record state's SOLE trigger is: a real prior run (`last_run` present — the never-run `LastRunTime` sentinel is nulled in PowerShell via `Year > 1900`) whose time is strictly NEWER than the newest run record. A non-benign `LastTaskResult` is NOT a standalone trigger — an exit-3 run (roster built, SFTP failed) WRITES a record and is a completed "Built, not delivered" row in Run History, so flagging it here would contradict that surface and assert an outcome the evidence doesn't establish. With no records to compare, no gap can be established → no contradiction (a pre-store run must not false-alarm). The copy is HEDGED — headline "Your last scheduled run reported a problem", detail "…didn't report success — open Run History for details. If DistrictSync was moved or deleted…re-register" — never a flat "didn't complete"/"was moved", so it stays truthful for the store-write-failed / in-progress cases the record gap also covers. (Original entry claimed a non-benign `last_result` standalone trigger + a flat "didn't complete" assertion — corrected here.)
  - **No config-hint time ever presented as a verified next-run (Verify-tightened, honesty).** `derive_schedule_status` no longer takes a `hint_time`; a displayed next-run comes ONLY from the OS-reported `NextRunTime`. A found task with no `NextRunTime` reads the timeless "Your nightly schedule is registered with Windows." — the config `schedule_time` is never shown as if verified (the hint-as-truth pattern this slice exists to kill, closed off structurally by dropping the param). `_friendly_schedule_time` was consequently retired rather than relocated. `read_schedule`'s `validate_task_name` is guarded (invalid name → UNKNOWN, never raises — the probe "never raises" contract). Fresh-start empty-state copy is CONDITIONED ("If you used an earlier version, its run history isn't carried over.") since newcomer-vs-upgrader stay indistinguishable — fresh-start is the chosen default, not a verified fact.
  - **`setup_completed` (D4a) = single-source method, not a load-only mutation.** `AppConfig.has_completed_setup()` OR-s the explicit flag with the old finish-line inference (`is_complete() and schedule_registered`); `load()` bakes it via that method (so it persists) AND directly-constructed configs (tests) derive correctly. `nav.needs_setup` re-keys to `not has_completed_setup()` — grep-clean of `schedule_registered`-for-live-ness except this one sanctioned inference. Since the field is now serialized, the inference is NOT gated on key-absence (that would break after the first save); an explicit persisted True is always honored. Resolves the 4b KNOWN-AMBIGUITY: a completed manual-only upgrader now gets the honest fresh-start copy.
  - **`attention` unifies Home WARNING + the nav badge; badge fires on contradiction too (a documented extension of the spec's "badge on MISSING").** `ScheduleStatus.attention` = (MISSING while the config expected a schedule) OR (LIVE contradiction) — both route to Setup, so badging both is consistent. An unexpected MISSING (deliberate manual-only) and every UNKNOWN never badge/warn (no false nag, no unconfirmed assertion). `nav.py` stays subprocess-free: the read-back is fetched OFF-thread per surface (`schedule_probe`, the read→derive→log boundary) and injected into the pure derivations.
  - **New module `schedule_probe.py` keeps `schedule_status.py` pure.** The contradiction WARNING log (durable Event-141 trace, PII-free — task name only) needs I/O + logging, so it lives in a thin tested boundary that bridges `read_schedule` → `derive_schedule_status`, rather than polluting the pure module's "no flet, no I/O" contract. `interpret_unregister` makes Unregister idempotent (an absent task is success-shaped). Save-after-success: `screens/setup.py` writes `schedule_time`/`schedule_registered` ONLY in the register success branch (a failed attempt no longer records a time that isn't real).

- **2026-07-08 — Plan 0029 Slice 4b (Run store, D2/D2a/D2b/D2c): SQLite `history.db` + one-dict-two-sinks + source tags — judgment calls recorded.**
  - **Approach = feature build with the plan's enumerated tests as the acceptance contract** (no CHARTER.md; this "durable run store with a non-fatal-write + reader-`[]`/`None` invariant" work-type is first judged here — recorded as a revisable default). The store's contract tests (reader `[]`-vs-`None` split, quarantine-recreate, forced-write-failure → result/exit/CSVs unchanged + original-exception identity) ARE the acceptance, so they were written alongside the store, migrating the retired `run_log` reader's discipline. No independent test-author spawn — the runtime has no subagent tool (consistent with the Slice 4a/11 record); stated plainly so the claim is falsifiable.
  - **`DSYNC_SOURCE` env vs `--source scheduled` CLI arg — judgment call.** D2c says "inject `DSYNC_SOURCE=scheduled` into the registered task's action env". The Windows ScheduledTasks module has NO per-action environment field — the only way to literally set an action's runtime env var is a `cmd.exe /c "set …"` wrapper, which changes the action's `Execute` off the exe (breaking the exe-path readback Slice 5 depends on) and is a bigger register-flow change than the slice's "small edit / don't touch register flow" allows. Chose instead to carry the source on the action's command line — `_build_action_args` appends `--source scheduled` (the single action builder) — keeping `Execute`=exe and fully testable. `run_pipeline` resolves precedence `explicit arg → DSYNC_SOURCE env → "cli"`, so the env var remains the documented mechanism for container/cron; `main.py` gains a `--source` flag. Net: scheduled runs are labeled from the store's first day, exe path preserved, no cmd wrapper.
  - **One dict, two sinks (D2a) with a privacy split.** `build_run_record` builds the flat record ONCE (the exact shape `home_status`/`run_history` consume + `source`/`sis_type`/`error_category`). `_log_run_record` writes the retained `__DISTRICTSYNC_RUN__` line WITH the rich free-text `error` (ops needs it); the store persists the record WITHOUT `error` — only the bounded `RunErrorCategory` (mirrors `LatestReason`'s closed-set style; defined pipeline-side to avoid an `etl`→`ui_flet` import). `_emit_run_log` is retained as a thin build+log convenience the existing unit tests pin.
  - **Store writes are best-effort, post-commit, and guarded at BOTH sites.** `_store_run_record` swallows everything; at the FAILURE site the whole record/log/store block is additionally wrapped so a recording error can never mask the ETL exception — the bare `raise` re-raises the original (identity asserted in tests). See `docs/claugentic-INVARIANTS.md` for the four store invariants.
  - **Honest fresh-start empty state.** No backfill (the only source is the polluted log). `derive_home_status`/`derive_history_banner` gain a `store_created_at` param (from `store_meta()`); an established install (`schedule_registered` OR a store already exists) with an empty store reads "Run history starts fresh here — earlier runs aren't shown", never the false "No sync has run yet". KNOWN AMBIGUITY, deliberately defaulted: a configured-but-unscheduled empty store cannot distinguish a genuine first run from a manual-only district upgrading — both show the first-run copy (not a regression: pre-4b manual runs never appeared at all); Slice 5's `setup_completed` disambiguates. (Verify-corrected framing — the original entry wrongly equated "unscheduled" with "first run".)
  - **Module/session-scoped fixtures leak the store (found via the canary).** `run_pipeline` now writes the store via `paths.user_data_dir()`; the SD74 (`test_regression_sd74.py`, class-scoped) and contract (`test_contract.py`, module-scoped) fixtures run BEFORE the function-scoped `isolated_user_profile` autouse fixture, so they wrote the REAL `history.db` (the canary caught it). Fixed by redirecting `paths.user_data_dir` inside each fixture's own patch/`MonkeyPatch` block — CSV output (and the SD74 golden snapshot) is byte-identical. A session-scoped backstop was rejected: it would break `test_paths.py`, which drives the real seams via the `real_user_data_dir` marker.
  - **`run_log.py` retired.** Its reader + `[]`/`None` contract move to `history.store.read_run_records`; `home_status`/`run_history` + their screens repoint; `test_ui_flet_run_log.py` → `test_history_store.py`.

- **2026-07-08 — Plan 0029 Slice 11 (Cross-platform app-data relocation): `platformdirs` + failure-safe migration — mechanism + judgment calls recorded.**
  - **Approach = feature build with the plan's enumerated tests as the acceptance contract** (no CHARTER.md; this "cross-platform path relocation with a data-migration invariant" work-type is judged here for the first time — record as a revisable default): implement `paths.py` + the failure-mode tests together, because correctness hinges on the migration's data-safety invariants (legacy stays live, partial state invisible), which ARE the acceptance. No separate independent test-author spawn was used (the runtime has no subagent tool, and the tests pin the plan's stated acceptance criteria); stated plainly so the claim is falsifiable.
  - **`user_data_dir()` resolves via `platformdirs.user_data_dir("DistrictSync", appauthor=False, roaming=False)`** → Windows `%LOCALAPPDATA%\DistrictSync`, macOS `~/Library/Application Support/DistrictSync`, Linux `$XDG_DATA_HOME/DistrictSync` (default `~/.local/share/DistrictSync`). Non-roaming on Windows is correct for the WAL SQLite store (must not sync across machines mid-write). `platformdirs` pinned `>=4.0,<5.0` (house `<major` cap style); static `import` in `paths.py` → PyInstaller detects it (no `--hidden-import`; those are only for dynamically-discovered keyring backends — confirmed in Makefile + flet-pack.yml).
  - **Judgment call — the leaf is `DistrictSync` (capitalized) on ALL THREE OSes, NOT lowercase `districtsync` on Linux as the plan prose said.** Verified `platformdirs` uses the appname VERBATIM (`_append_app_name_and_version` does no case-folding). Honoring the pinned call signature yields a single consistent professional identity (matches Windows/macOS exactly), which is better than a per-OS case split; encoded the REAL per-OS strings in tests.
  - **Migration mechanism = stage-then-atomic-promote (chosen so a failure can neither strand nor lose data).** `migrate_legacy_data_dir()` runs only when legacy `~/.districtsync` exists AND the new dir does not (idempotent; a cheap `exists()` no-op on every already-migrated startup). It COPIES the entire legacy tree (config.json, `etl_tool.log`+rotations, `mappings/`, and `history.db` with its `-wal`/`-shm` sidecars as one unit) into a fresh staging dir under the NEW dir's *parent* (same filesystem → the promote is atomic; the copy tolerates a cross-device home→appdata layout), then promotes it with a single `os.replace`. The new dir becomes "live" only once EVERY file copied; any copy failure discards the staging copy, never creates the new dir, and leaves the legacy dir fully intact and live — so `user_data_dir()` keeps returning legacy and a partial migration is invisible. **Rejected the plan's "file-by-file `os.replace` into the new dir" phrasing** precisely because incremental promotion makes a mid-failure partial state visible (already-moved files stranded under the new dir while legacy still wins) — the exact hazard the slice must design against.
  - **Copy, never move/delete.** Legacy files are left in place with a `MOVED.txt` breadcrumb naming the new location ("safe to delete"), so there is never a window where the only copy of the data is in flight; a human keeps a recoverable original. Simplest + maximally safe (KISS).
  - **`user_data_dir()` never creates the new dir when legacy exists** (deterministic rule: new-if-exists → legacy-if-exists → else create new). This is load-bearing: it lets `migrate_legacy_data_dir()` run BEFORE `get_logger()` at entry without a read prematurely materializing the new dir and defeating the migration guard.
  - **Migration wired at both entries BEFORE the log opens** — `src/main.py` (CLI) before `_configure_cli_logging()`, and `src/ui_flet/launcher.py` before `boot_logging()`. Also resolves the Slice-4a verify flag: `boot_logging()` (and the migrate call) moved INSIDE the launcher's early-failure `try`, so a locked/permission-denied profile at migration/log-open time surfaces the plain-language error dialog instead of dying silently; `_write_traceback`'s independence (it re-resolves the log path itself) is preserved.
  - **Test-isolation extended:** the autouse conftest fixture also redirects `_platform_data_dir`/`_legacy_data_dir` into (non-existent) tmp paths for non-marked tests, so a stray `migrate_legacy_data_dir()` can never touch the real `~/.districtsync`. The real migration on this machine happens only when Shan next runs the app — not during the test run.

- **2026-07-08 — Plan 0029 Slice 4a (Test isolation + seam foundation, D3): AppConfig-through-paths refactor + autouse isolation + `get_logger` deferral — judgment calls recorded.**
  - **Approach = red-first (bug+refactor work-type).** No CHARTER.md exists yet; this is the first time a "test-isolation / seam-hardening correctness fix" is judged, so recording the approach here (the established record-decisions discipline, not a mechanical write): **build the isolation fixture + canary, prove the canary is RED against the un-refactored `AppConfig` (safely, with a redirected HOME so the real profile is never touched), then refactor to GREEN.** Revisable default for this work-type.
  - **`AppConfig` resolves its path through `paths.user_data_dir()` at call time**, via a new `config_file_path()` helper (single source of the `config.json` filename), replacing the import-time `APP_CONFIG_DIR`/`APP_CONFIG_FILE = Path.home()/…` constants (the isolation bypass that let ~365 test fixture records reach the real log and could clobber `config.json`). Same filename, same Unix 0o700/0o600 permissions. Now the ONLY `Path.home()` in `src/` is `paths.py` (single source; also unblocks the Slice 11 relocation).
  - **`get_logger()` deferred out of import time into every entry path** — `src/main.py` `_configure_cli_logging()` (called at CLI dispatch, not module import) + `src/ui_flet/launcher.py` `boot_logging()` (called at UI launch). Each is a tested seam; a fresh-interpreter import of `src.main` now configures NO file sink (regression guard). `logger.py` needed no change — it already resolves the sink through `user_log_file()` → `user_data_dir()` at call time (the reason patching `user_data_dir` is load-bearing where patching `user_log_file` alone is a no-op).
  - **Autouse `isolated_user_profile` conftest fixture** patches the single deep `paths.user_data_dir` seam → per-test tmp dir, swaps a suite-wide in-memory keyring backend (belt-and-suspenders; existing SFTP local mocks that ASSERT specific keyring calls are kept and layer on top), and snapshots/restore+close logging handlers on teardown (a leaked `FileHandler` blocks tmp_path cleanup on Windows). A `real_user_data_dir` marker opts a test OUT of the seam patch so `test_paths.py` still exercises the REAL `user_data_dir` (else that guard would test the fake). Honest register: this is **redirect-the-seams + a canary tripwire** — the guarantee holds only while writes route through the patched seam; NOT a mechanical impossibility.
  - **Canary baseline captured at conftest IMPORT** (before any test/seam-patch runs) via `Path.home()` (which the fixture never patches), so `test_isolation_canary` proves a whole pytest run leaves the real `config.json`/`etl_tool.log`/`history.db` byte-untouched. Extends to a 3-artifact canary (adds a store write) in Slice 4b.
  - **`launcher.resolve_log_path` fallback no longer re-derives `~/.districtsync`** — if the paths seam itself is broken it falls back to a bare `etl_tool.log` (cwd), deliberately NOT duplicating the app-data location (single-source rule).

- **2026-07-08 — Plan 0029 Slice 2 (Chrome & input craft): async exit fix + brand icon + Enter-to-submit — three judgment calls recorded.**
  - **Exit no-op fixed via one shared async helper.** Flet 0.85.3 `Window.destroy()` is a coroutine, so the synchronous calls in `shell.do_exit`/`on_window_event` + `launcher.py`'s boot-error Close were un-awaited no-ops (no exception → the `os._exit` fallback never fired either → Exit did nothing). New shared `shell._close_window(page)` awaits `destroy()` (both Exit + OS-close route through it so they can't drift), `os._exit(0)` fallback kept; handlers are now coroutines (Flet 0.85.3 supports async event handlers). launcher's dialog Close got the same async fix. The zero-orphan `page.on_disconnect` path is untouched.
  - **Brand icon source = `docs/assets/spacesedu-icon.png`, NOT the plan-named `docs/assets/myblueprint-logo.png`.** The named file is a 1412×966 *product screenshot* (verified), which downscaled to 16px is unrecognizable noise — defeating the "brand icon instead of the generic Flet logo" intent. Used the actual square SpacesEDU/myBlueprint brand mark (`spacesedu-icon.png`, 96×96) instead; generated `assets/districtsync.ico` (multi-res 16/24/32/48/64/128/256, LANCZOS-upscaled 256 base) with Pillow (dev-time tool only — NOT added to requirements). `page.window.icon` set via the pure `paths.app_icon_path()` (bundle-relative: dev tree vs `_MEIPASS`); shipped in the bundle via `flet pack --add-data "assets;assets"`.
  - **`--icon` scoped to Windows in `flet-pack.yml` (matrix `icon_arg`), not applied on macOS/Linux.** The runtime icon surface (`page.window.icon`) + Explorer exe icon are Windows-only, and PyInstaller `--icon` expects a `.icns` on macOS — passing a `.ico` risks a mac conversion failure. The `.ico` is still BUNDLED on all three OSes (`--add-data assets`), harmless where unused. Makefile `build-win` (Windows-only) gets both `--icon` + `--add-data assets`.
  - **Enter-to-submit gate predicates extracted to a new COUNTED module `src/ui_flet/setup_gates.py`** (`can_register_schedule` / `can_save_sftp`), single-sourcing the disabled-button gates so `on_submit` (which bypasses a disabled button) re-checks the SAME predicate — Enter can never fire a gated action. setup.py (coverage-omitted view glue) can't host tested pure logic, so a focused COUNTED module (mirroring `filepicker.setup_state`, the folders gate); Slice 8's `setup_flow.py` can absorb/reuse it.
  - **Slice 2 is honestly a manual-Verify slice for the OS-interaction surfaces** (Exit-closes-in-frozen, icon-on-titlebar/taskbar/exe): unit tests cover the pure seams (icon-path resolution, gate predicates, `_close_window` awaits destroy); the OS teardown + rendered icon rest on the runtime-qa checklist per the plan's Test strategy.

- **2026-07-08 — Plan 0029 (Flet UI trust & professionalism redesign) grounding — design decisions D1–D10 + panel rejections + Slice-11 approval recorded.** Combined program driven by a 2026-07-08 field test of the shipped v3.4.0 Flet UI; plan `.claude/plans/0029-flet-trust-professionalism.md`; advisory panel wf_43f08384-433 (10 advisors) + 46-agent investigation wf_84cdd881-3a2. **Product bar:** the UI never asserts a state it didn't check — every success names WHAT it checked and WHEN, never a promise about the future or a self-reported boolean.
  - **D1 — Config freshness via load-per-build:** screens bind `AppConfig.load` (a supplier), not a loaded instance, so every navigation renders current state (the pattern Setup/Convert already use); Mapping's Apply writes through + re-renders, its gate compares the *persisted* value; manual Refresh on Home/Run History. Reactive store rejected (YAGNI).
  - **D2 — Run store = SQLite `history.db` in the per-user app-data dir**, written via one shared `write_run_record()` from BOTH `run_pipeline` and `convert_job`; hybrid schema (promoted typed filter columns + one `record` JSON of the exact flat dict the derivation modules consume → one reader, zero shape change); `PRAGMA user_version=1`, WAL + `busy_timeout`, DELETE-journal fallback, open-use-close per op, quarantine-and-recreate on corruption.
  - **D2a — Log-line parity:** the `__DISTRICTSYNC_RUN__` line stays and gains the same enrichment (source/sis_type/error_category); the log keeps rich free-text error detail for ops, the store carries only bounded `error_category` + counts (privacy split).
  - **D2b — Store writes are best-effort, strictly non-fatal, post-commit:** a store failure logs and never alters `PipelineResult`/CSVs/the exit-code contract, and never masks the original ETL exception; the enriched log line is the durable fallback.
  - **D2c — `source` propagation:** the registered task injects `DSYNC_SOURCE=scheduled`, CLI defaults `cli`, `convert_job` passes `manual` — without the marker every nightly run mislabels the field whose whole purpose is "did the NIGHTLY sync run".
  - **D3 — Test isolation is a correctness fix:** an autouse conftest fixture patches the deep `paths.user_data_dir` seam (patching `user_log_file` alone is a no-op — `logger.py` imported the name by reference) + routes `AppConfig` through `paths` + swaps the keyring backend + restores logging handlers; a canary asserts the real log/config.json/history.db stay untouched; `get_logger` moves out of import time into every entry path.
  - **D4 — Schedule truth via a tri-state ScheduledTasks read-back** (`Get-ScheduledTask` + `Get-ScheduledTaskInfo`): LIVE / MISSING / UNKNOWN, owned by pure `schedule_status.py`; only MISSING may claim "not scheduled", UNKNOWN renders "couldn't confirm right now" and never asserts "scheduled" from the config boolean; all consumers read the one derivation; config writes move after registration success; Unregister added.
  - **D4a — Two facts never conflated:** `setup_completed` (durable: the wizard finish line was reached) vs schedule-live-now (D4 read-back); wizard entry gates on `not setup_completed` only, so a previously-configured install with a broken task lands on Home+WARNING routed to Setup, never back in first-run onboarding (the Event-141 Firefighter is not a newcomer).
  - **D5 — Per-operation elevation isolated in `scheduler/elevation.py`** (`ShellExecuteExW` runas on the absolute System32 powershell path, bounded wait); inbound password channel = DPAPI CurrentUser-scope JSON blob, owner-only ACL, fail-closed on SID mismatch (never widen to LocalMachine); outbound result = atomic plaintext (carries no secret); off-thread with visible in-flight/declined/failed/timeout states; success verified by the D4 read-back, not the exit code.
  - **D6 — SFTP test side-effect-free + provenance-honest:** `test_connection(password_override=…)` threads a typed password only to `connect()` (never keyring/logs/message); keyring written only on explicit Save; success copy names the host, user, and credential source tested.
  - **D7 — Nav fixed order always** (Home, Convert, Run History, Setup, Mapping, Help); newcomer guidance via initial selection + a schedule-driven "needs attention" badge on Setup; `build_nav` exposes the rail and `select_by_id` syncs the highlight (one fix for every programmatic hop); rail label stays "Setup", the graduated surface titles itself "Settings".
  - **D8 — Wizard→Settings hybrid = a pure state machine `setup_flow.py`** (five concrete named steps, no data-driven engine) consuming injected verification results; resume derives the first unsatisfied step from real state (no stored cursor) and reconciles against side effects already performed; no single step flips `setup_completed` (only the finish line does); Settings mode = one reconciling Save that re-registers the task when ANY task-arg changed; finish-line copy is honest + adaptive.
  - **D9 — Explicit district everywhere:** `sis_type` default `""`, "Choose your district" placeholder, auto-select only when exactly one config exists, Convert refuses to run without an explicit district (no `configs[0]` fallback); existing installs unaffected.
  - **D10 — Convert output visibility within the PII rule:** `ConvertResult` stays path-free; the *view* renders the resolved output folder + "Open folder" (`os.startfile`); empty `output_dir` blocks with a routed "set your output folder" instead of silently writing into the input folder.
  - **Rejected — backfill the new store from the old log:** the only source is the already-polluted diagnostic log (365/370 records are test fixtures) — importing it would seed the clean ledger with garbage; instead, honest fresh-start empty states.
  - **Rejected — compaction + a migration framework:** YAGNI (~1.8k rows/~400 KB per year; two exe versions share the DB) — use an additive-only schema rule + a `user_version` stamp + explicit-column statements + never-migrate/downgrade-on-higher-version instead.
  - **Rejected — DPAPI-encrypting the outbound elevation result:** it carries no secret (a sanitized ok/message), so plaintext-atomic is correct; encryption would be ceremony without a threat.
  - **Rejected — nav-reorder for prominence:** reordering a persistent rail destroys spatial memory for a tool opened months apart — guide via highlight/badge + initial selection instead.
  - **Rejected — whole-app `--uac-admin` UAC manifest:** PII posture + mapped-drive access + it breaks the scheduled (non-interactive) launch — elevate per-operation instead.
  - **Slice 11 APPROVED in-scope (pre-adoption window):** relocate `~/.districtsync` → the industry-standard per-OS user-data dir via `platformdirs` (Windows `%LOCALAPPDATA%\DistrictSync` non-roaming — correct for a WAL SQLite store; macOS `~/Library/Application Support/DistrictSync`; Linux `$XDG_DATA_HOME/districtsync`); single change point `paths.user_data_dir()`; transparent, idempotent, failure-safe one-time migration with a `MOVED.txt` breadcrumb; build order immediately after Slice 4a and before 4b so `history.db` is born in the new location.
- **2026-07-07 — Added `sd60myedbc` (SD60 Peace River North) district config + two config-driven capabilities.** Guardians-only Family `row_filters`; opt-in `cross_enrollment.collapse` home-school dedupe for ~386 dual-school students (rows differ only by School Number; same admission date, no withdraw date, disjoint classes — genuine dual-enrolment, so rostered once under Home School, enrolments kept at both); `Active No Primary` retained; `ATT--AM/PM` excluded; emails used as-is (blanks kept) pending SD clarification; declined Students-dedupe-off (chose collapse as safe under either SpacesEDU importer behavior).
- **2026-07-06 — Local E2E of the Flet UI caught + fixed SHIPPING-BLOCKER flet-0.85.3 render crashes on Setup + Mapping (latent since the IA slices).** The manual `DISTRICTSYNC_UI=flet` GUI smoke was deferred as "human-only, headless here" on **every** IA slice, and the screen views are coverage-omitted (`# pragma: no cover - Flet view glue`), so flet-0.85.3 API-drift bugs shipped GREEN through all gates: **`ft.Dropdown(on_change=…)` RAISES `TypeError`** (Dropdown's value-change event is `on_select`; `on_change` doesn't exist — it has `on_select`/`on_text_change`) and **`ft.TextField(helper_text=…)` RAISES** (the field is `helper`). Impact: the **Setup wizard** (the FIRST screen an unconfigured admin opens — district dropdown + 3 schedule/SFTP TextFields) and the **Mapping** switch dropdown **crashed on mount**; the **Convert** + **SFTP-host** dropdowns set `.on_change` post-construction = **dead handlers** (no crash, silently never fired). Masked because the screens' never-crash `ErrorCard` floor swallowed the exception → a naive "returns a Control" check false-passed. **Fixes (view-only, zero core):** 4 Dropdown sites `on_change`→`on_select` (setup/mapping/convert + setup-SFTP-host); 3 TextField `helper_text`→`helper` (setup). **Root cause = a DOC GAP:** `FLET_1.0_CONVENTIONS.md` documented the `FilledButton(text=)` trap but had **no Dropdown/TextField event guidance** — now added (Dropdown=`on_select`, TextField=`helper`/`hint_text`, with the "post-construction `.on_change` is a dead handler" note). **Harness fix (the durable win):** new `tests/test_ui_flet_render_smoke.py` instantiates **every** screen against a stub `Page` and fails if it raises OR falls to its `ErrorCard` floor (precise floor-detection via an `ErrorCard` spy) + a static AST guard banning `ft.Dropdown(on_change=)` — converting the perpetually-deferred human GUI smoke into an automated mount-render gate (9 tests). Also a one-off static kwarg-audit (every `ft.*(...)` kwarg vs `dc.fields`) confirmed **no other invalid constructor kwargs remain** across `src/ui_flet/**`. Gates green (1350 passed/1 skipped, cov 93.67%; ruff/format/mypy/bandit/validate-config/tree-check all clean). **Still human-only:** event-HANDLER interaction paths (on-click behavior) aren't exercised headlessly — the render gate covers mount, not interaction; a real Win11 GUI click-through of Setup register / Convert run / Mapping apply stays a human acceptance step.
- **2026-07-06 — Merged `origin/main` into `feat/flet-ui-rebuild` (Flet wins) + preserved the one non-Streamlit change (real `--version` in built exes).** During the rebuild `main` advanced 5 commits (merge-base `212ac4f`); all conflicts resolved **in favour of this branch**. **Discarded as obsolete:** PR #43 graceful-shutdown (`ff9cabf`) — a Streamlit-only idle-watchdog / Exit / single-instance guard that Flet's native zero-orphan close makes moot — along with all of `src/ui/*` (incl. main's new `lifecycle.py`/`brand.py`), the Streamlit tests `test_ui_lifecycle.py` + `test_version.py` (the latter imports `src.main._resolve_version`, absent here), and the auto-merged Streamlit prose in `requirements-dev.txt` / `INVARIANTS.md` (its idle-watchdog invariant; the **S4U scheduling invariant stays** — non-Streamlit, from Plan 0009 which landed at the merge-base) / `developer/release.md`. The MkDocs/GitHub-Pages docs-site removal stays removed (per the 2026-06-30 decision). **Audit — nothing else non-Streamlit dropped:** `main`'s `pyproject.toml`/`requirements*.txt` changes were purely the `streamlit>=1.54,<1.55` watchdog pin (correctly gone). **The one non-Streamlit change preserved — `536b9a4` "report the real `--version` in built executables" (PR #42):** this branch's importlib-only lookup (both `main.py` and `app_version()`) would report `dev` in a frozen exe, so the fix was **adapted to the Flet packaging**, not lost. Adaptation (DRY, discharging `version.py`'s tracked ROADMAP note): `app_version()` (`src/utils/version.py`) is now the ONE lookup — build-stamped `src/_version.py` → package metadata → `dev` — and `main.py --version` routes through it (dropped its inline `importlib.metadata`); `flet-pack.yml` (THE public/PR builder, shared by `release.yml` + `flet-verify.yml`) gains a tag-aware **Stamp version** step (real version on a tag via `${GITHUB_REF_NAME#v}`, `dev` on a PR/dispatch preview) + `--hidden-import src._version`; `.gitignore` `/src/_version.py` came in free from main's auto-merge; `test_ui_flet_version.py` gains a stamped-precedence test. **Merge commit `70d986b`; version-fix follow-up commit next.** The 8 pre-existing uncommitted noise files (`.claude/settings.json`, `CLAUDE.md`, several `docs/claugentic-*`, `.githooks/`, `PLAN_TEMPLATE.md`) were **not** staged (left `??`/` M` for the user's separate noise-resolution pass). Gates re-run green post-merge; PR #45 updated. The final merge to `main` + release tag remain the user's calls.
- **2026-07-04 — Plan 0027 CUT-1: Flet becomes the ONLY UI — cutover COMPLETE (2 slices — CUT-1a `1644768`, CUT-1b `51d3d3b`). The DistrictSync Flet 1.0 UI rebuild (Plan 0013) is DONE.** Every surface ported (IA-1..IA-9); Streamlit removed. **CUT-1a (the ONE intended `main.py` touch in the whole program):** flipped the no-argv branch Streamlit→Flet — `_default_ui_launcher() -> src.ui_flet.launcher.main` (a window-less-testable seam); **`DISTRICTSYNC_UI` retired** (one UI = dead hatch, YAGNI; `DISTRICTSYNC_UI_DEMO`, a distinct dev flag, untouched); the **CLI branch + `sys.exit(0/1/2/3)` byte-identical** (the diff terminates at the unchanged `try:`; 640 core tests + SD74 + the parity lock stay green). **CUT-1b (delete + de-scaffold + repackage):** `git rm -r src/ui/` (Home + 5 wizard pages + brand/folder_picker/mapping_helpers/launcher — 4306 deletions) + `.streamlit/` + the 3 Streamlit-only tests; dropped `streamlit` from `requirements`/`pyproject` (`ui` extra) + the `ci.yml` mypy `src/ui` exclude + the coverage-omit + the `ui` pytest marker (`addopts` → `-m 'not benchmark'`); **repackaged the release** — `flet-pack.yml` now packs `src/main.py --name DistrictSync` (all ~8 sites) and `release.yml` ships the **Flet-default exe as THE public artifact** (1/OS + `SHA256SUMS.txt`, no Streamlit exe, no "preview" framing). **The gate-caught `release.yml`↔`flet-pack.yml` artifact-name mismatch is FIXED** (upload `DistrictSync-${{matrix.os}}` ↔ downloads `DistrictSync-{windows-latest,ubuntu-22.04,macos-latest}`, byte-identical both sides — a real CI break the plan had glossed); `ci_flet_pack_smoke.py` host-prefix `districtsync-flet`→`districtsync`; `Makefile`/`FLET_1.0_CONVENTIONS.md`/`ARCHITECTURE_TREE`/`README`/developer-docs/`CHANGELOG` de-Streamlit'd (every "until CUT-1" hedge resolved). **Migrate-before-delete: NOTHING LOST** — the parity test's `_run_ui_path` reproduces the adapter INLINE (Streamlit-free, gate-verified); all 7 named capabilities already had tested Flet homes (`classify_schedule_error`→`setup_errors`, run-log parser→`run_log`, brand→`tokens`, Convert adapter→`convert_job`, folder-picker/mapping/help). **No half-cutover** (grep clean: only the intentional `--exclude-module streamlit src.ui` belt-and-braces remains). Gates green (1340 passed/1 skipped, cov 93.58%; parity + SD74 green; ruff/format clean; mypy(`--exclude src/ui_flet`) no new; bandit(-c) 0; validate-config all 10; tree-check OK). **`CLAUDE.md`'s dead Streamlit prose deferred** to the user's noise-resolution (a noise file — never staged). **HUMAN-ONLY acceptance gates remain (the agent can't push to the fork):** (1) the 3-OS `workflow_dispatch` via `flet-verify.yml` (offline-embed + zero-orphan-close on Win/Linux/macOS against the `DistrictSync` exe — also closes PLAT-3's outstanding gate); (2) a real Win11 double-click (→ Flet shell) + `DistrictSync.exe --sis … --input … --output …` (→ CLI runs, exits 0); (3) audit the Release draft (1 exe/OS + `SHA256SUMS.txt`, no Streamlit exe). **The rebuild is COMPLETE on `feat/flet-ui-rebuild`; the final merge to `main` + a release tag are the user's calls.**
- **2026-07-04 — Plan 0026 IA-9: Flet cross-surface humanization + product spec (`ccdafd8`).** Presentation-only, **zero core touch** (verified empty; the SFTP-test raw-string is sanitized VIEW-side — `uploader.py` untouched). **Audit result (honest):** the surfaces were ~90% already clean (each prior slice enforced category-only/PII-free copy + a privacy test); the audit found **2 genuine raw-exception leaks in `screens/setup.py` (IA-4b)** + 2 borderline validator cards + 2 duplicated helpers. **Leaks fixed:** the keyring store-failure card + the SFTP test-connection card (BOTH the returned-`msg` render AND the `except` fallback — the gate corrected the plan's line target: the primary leak was the returned `(False, str(exc))`, not just the `except`) now route through the new bounded **`humanize.friendly_sftp_reason(raw)`** — a category mapper (auth / host-DNS / connectivity / remote-path + a **mandatory catch-all**; case-insensitive; NEVER returns the raw string) that keeps the *diagnostic* value (the admin still learns WHY) without echoing a raw paramiko/socket string; the 2 validator cards → fixed actionable copy. **After IA-9 no `str(e)`/`str(exc)` reaches any admin card.** (Notable: IA-4b's earlier password-focused security fan-out proved no *credential* leak there; this broader humanization pass caught the raw-error-in-copy concern — layered review earning its keep.) **DRY consolidation:** `pluralize` (was duplicated) + `friendly_anomaly_detail(count, *, variant)` (an `AnomalyVariant` HOME/HISTORY/CONVERT enum — CONVERT carries its structural "Review before delivering" CTA difference, not just tense) promoted to COUNTED `humanize.py`; `home_status`/`run_history`/`convert_result` migrated + their local copies deleted — **byte-for-byte** (all existing anomaly-copy assertions stayed green, no reconciliation needed). **Regression lock:** new `tests/test_ui_flet_humanization_sweep.py` — an ADVERSARIAL planted-sentinel sweep over every pure derivation (`derive_home_status`/`derive_history_banner`/`to_run_row(s)`/`convert_result.summarize`/`mapping_catalog.summarize_config`/`list_configs`) × **every reachable branch** (proven by injecting a leak into a fault branch → the sweep FAILS) asserting no `/secret/`/`SECRET_SIS`/raw-ISO/`ANOMALY:`/`Traceback` reaches any emitted string field. **`docs/claugentic-PRODUCT.md`** persisted — the durable narrative product spec (the non-technical BC admin opened 2–3×/yr, the trust promise, per-surface flow+states, a "deliberately out today" section). Gates green (1353 passed/1 skipped, cov 93.55%; `humanize`/`home_status`/`run_history`/`convert_result` 100%; ruff/format clean; mypy(non-UI) no new; bandit(-c) 0; validate-config all 10; tree-check OK). Manual `DISTRICTSYNC_UI=flet` Setup error-copy smoke (bad time / keyring fail / unreachable SFTP → category copy, no raw string) human-only (headless here).
- **2026-07-04 — Plan 0025 IA-8a: Flet Mapping select-a-pre-built-config surface (`2cc2a0a`).** Presentation-only, **zero core touch** (verified empty). The `mapping` destination (Advanced) becomes a **review-and-switch home for the district config** — distinct from Setup's first-run dropdown by showing a read-only **"what this config produces" summary** (friendly name + output-CSV labels + source-file count). NOT the full column-mapping editor — **IA-8b (YAML edit / column wizard / config creation / `mapping_helpers.py`) stays ROADMAP** per the 0013 scope-lock. Pure COUNTED `mapping_catalog.py`: frozen PII-free `ConfigSummary` + `summarize_config` (TOTAL — `output_labels` = the emitted CSVs by the **empty-`enabled_entities`-means-all rule ∩ `mappings.keys()`** [single-sourced with `models.py`; the intersection means an enabled-but-undefined entity never shows a phantom CSV — matches the pipeline's `entity in mappings` gate], labelled via the shared `ENTITY_LABELS` in canonical order, de-duped `source_file_count`; **`except → degraded loaded_ok=False`, never the raw error** — a broken partner YAML degrades calmly, never crashes) + `list_configs`. **DRY (gate #1):** the entity→CSV-label fact was duplicated across `components.run_table` / `screens/home` / `screens/convert`; **single-sourced to `home_status.ENTITY_LABELS`** (one definition, all consume it — byte-identical view output verified). View `screens/mapping.py` (omitted): current-summary card + a switch `ft.Dropdown` (options = `available_configs()` allowlist, no free-text) with the pending config's live summary + an Apply **structurally gated on loadable+different** → writes only `AppConfig.sis_type` (the UI-owned setting Setup already writes; folders/schedule untouched) → verdict-first "Now using {friendly}"; degraded config → Apply disabled; never-crash `ErrorCard` floor. Reuses `available_configs`/`friendly_district_name` (NOT a copy of Setup's `_district_options` — different render seam). Gates green (1292 passed/1 skipped, cov 93.53%; `mapping_catalog.py` 100%; ruff/format clean; mypy(non-UI) no new; bandit(-c) 0; validate-config all 10; tree-check OK). Manual `DISTRICTSYNC_UI=flet` mapping smoke (review current / switch / apply / degraded) human-only (headless here).
- **2026-07-04 — Plan 0024 IA-7: Flet Help surface — link-out to the org KB (`fddb06e`).** Presentation-only, **zero core touch** (verified empty). The smallest slice: `screens/help.py` (view-omitted) — a branded hero + a "Get help" card (`page.launch_url(HELP_CENTRE_URL)` to the canonical SpacesEDU Help Centre article `help.spacesedu.com/en-ca/article/mx56qo` + a `mailto:support@myBlueprint.ca` button, **with both addresses ALSO as `selectable=True` plain text** so an air-gapped/browserless district server gets a copyable address, not a dead click) + a "What DistrictSync does" reassurance card (decouple-the-sync voice) + a never-crash `ErrorCard` floor; all via `components.py`. **Decision (gate-confirmed): LINK OUT, do NOT render bundled `docs/` markdown** — the 0013 scope-lock (block-wins-on-conflict) supersedes the stale "render markdown" IA-7 row; the org KB is the canonical, always-current docs home (per the 2026-06-30 MkDocs-removal decision); a bundled render would need a `flet-pack.yml --add-data docs` + a `_MEIPASS` resolver (disproportionate) + re-entrench the redundancy the program is retiring. `ft.Markdown` DOES exist on 0.85.3 — a scope call, not a gap. **Bundled-docs render DEFERRED to ROADMAP**, now explicitly tied to the offline/air-gapped-district persona + the deferred docs-strategy decision (the copyable selectable-text already covers browserless servers here). URL/email are single-sourced module constants (exact-case drift-guard test — grepped byte-identical across `release.yml`/`README.md`/`Home.py`/`05_Help.py`). `DISTRICTSYNC_UI_DEMO` help→design-demo override **preserved** (the `help` swap precedes the override block, which re-assigns last). Nearly-all-view — no COUNTED module manufactured (honest; the only test is the constant/ordering drift guard, no flet-control instantiation). Gates green (1278 passed/1 skipped, cov 93.25%; ruff/format clean; mypy(non-UI) no new; bandit(-c) 0; validate-config all 10; tree-check OK). Manual `DISTRICTSYNC_UI=flet` Help smoke (opens browser/mailto; both override states) human-only (headless here).
- **2026-07-04 — Plan 0023 IA-6: Flet Run History port — read-only verdict/staleness banner + `ft.DataTable` (`d2b0635`).** Presentation-only, **zero `src/etl|config|sftp|scheduler|main.py` touch** (verified empty). The **2nd consumer of IA-3's `run_log.read_run_records` + `is_stale`** — promotes both per "promote on 2nd use". **DRY single-source (gate #1):** extracted `home_status.classify_latest_reason(record) -> LatestReason` (FAILED_ETL/FAILED_DELIVERY/ANOMALY/DATA_WARNINGS/CLEAN — the status→sftp→anomaly→data-errors precedence, **staleness excluded** as a time-relative axis) + `verdict_for_reason` (via `_REASON_VERDICTS`); **`derive_home_status` refactored behavior-preservingly** to classify through them (all 42 landed `home_status` tests stayed green — the regression net), so Home + the Run-History banner + each per-row label can never drift. Pure COUNTED `run_history.py`: `derive_history_banner` (None→"history unavailable"/[]→"no runs yet"/reason→banner copy + `verdict_for_reason`; CLEAN+`is_stale`→stale WARNING) + `to_run_row`/`to_run_rows` → frozen **`RunRow` with NO `error` field** (total, `.get`+`_as_int`; missing status→"Failed" fail-safe). **Privacy fix vs Streamlit (LIVE/top):** the Streamlit page showed the raw `error` (`str(e)` — path/`sis_type`/column), the log path, and a raw-log expander; IA-6 **DROPS all three** — category-only labels, no `error` attr, the raw `ANOMALY:` string never surfaces (TESTED: a fake path + raw ANOMALY string in NO `RunRow`/banner field; `not hasattr(row, "error")`). New `components.run_table` = the **first `ft.DataTable`** (introspected 0.85.3: `DataTable`/`DataColumn(numeric=)`/`DataRow(color=)`/`DataCell`); conditional myBlueprint+ columns decided **table-wide** (one scan), count cols `numeric`, **no raw Error column**, **text-first Status** (not colour-only), horizontally-scrollable wrapper. View `screens/run_history.py` (verdict-first banner → table; 3 distinct calm degradation states — unavailable/no-runs/never-crash `ErrorCard`; sync read on mount, no worker thread; no `on_navigate` — read-only terminal surface). Deferred to ROADMAP (not debt): pagination/sort/filter/refresh + a "for-support" raw-log affordance. Gates green (1274 passed/1 skipped, cov 93.25%; `run_history.py`+`home_status.py` 100%; ruff/format clean; mypy(non-UI) no new; bandit(-c) 0; validate-config all 10; tree-check OK). Manual `DISTRICTSYNC_UI=flet` run-history smoke (healthy/mixed/stale/empty/unavailable/columns) is human-only (headless here).
- **2026-07-04 — Plan 0022 IA-5: Flet Convert port (SECOND-RISKIEST) — the manual "run it now" surface on a worker thread (2 slices — IA-5a `c96fbd6`, IA-5b `b13b27d`).** Presentation-only, **zero `src/etl|config|sftp|scheduler|main.py` touch** (verified empty; adversarial fan-outs — concurrency + data-integrity (5a), exit-3/privacy + write-guard/concurrency (5b) — ran each slice). **Core entry = the parity-locked `run_transform` + `DataLoader.save_all` ADAPTER, NOT `run_pipeline`** (inside `test_pipeline_parity.py`'s byte-lock; exposes the frames the anomaly-ack + quality report need; lets the ack gate sit between transform and write; keeps SFTP an explicit ack-gated step). `run_pipeline` is the nightly engine — the UI must not re-run it as its manual path. **Reusable `JobRunner` (`job_runner.py`, first real consumer):** COUNTED pure `JobStateMachine` (single-flight — a double-click can't launch two ETLs) + a COUNTED `route()` seam that catches **`SystemExit` FIRST in a separate clause** (it's not an `Exception` subclass; a bare `except Exception` would let it silently kill the worker + write no run-log) → `on_error`; the `page.run_thread`→`page.run_task` `JobRunner.run` glue is `# pragma: no cover`; `_work` mutates NO control (grep-verified). The reusable worker-thread frame IA-6/IA-8 inherit. **Pure PII-free `convert_result.py`** (`ConvertResult` — counts/booleans/quality text, **no DataFrames** — + total `summarize` reusing IA-3's voice; privacy-tested: a fake path/`sis_type`/column never leaks into a headline/detail). **Anomaly-ack write-gate in IA-5a (gate-forced from IA-5b):** `convert_job` computes `compute_anomalies` after transform, BEFORE `save_all`, and returns `NEEDS_ANOMALY_ACK` **WITHOUT writing** on a >20% drop until an explicit "convert anyway" (re-transform-on-ack, `state.reset()` first — no PII frames held across the wait) — a silent 20% roster drop is structurally impossible; Cancel writes nothing. (Streamlit's Convert never wrote to the real output dir — the Flet Convert does, so the write needed the gate; the gate ruling moved it out of IA-5b into the first writing slice.) **IA-5b:** the SFTP leg wraps `upload_csvs` **TIGHTLY** (a build/`save_all` `ValueError` still propagates to `on_error` fail-loud; only the upload failure folds into `BUILT_NOT_DELIVERED` — the **exit-3 shape read from booleans in `on_done`, NEVER `on_error`**, carrying a CATEGORY not the raw exception/host/path); a `page.show_dialog` pre-flight confirm names the destination host (config, not a secret); a `_WRITE_IN_FLIGHT` flag read by `shell._on_leave` is **reassurance-only** (non-blocking — the loader's atomic `save_all` is the real net; the zero-orphan close is byte-identical). Folder-pick input (dir-`PickerField`, not `pick_files`) + `FileChip`s (first consumer); shared `metric_tile` extracted (DRY with Home). **Verify fan-outs caught + fixed 2 REAL defects the mechanical gates couldn't:** (1) [high] `BUILT_NOT_DELIVERED`'s banner promised "try delivering again" but the Deliver action was gated out for that status (only retry was a full redundant rebuild) → `_render_result` now offers redelivery on the failure screen; (2) [medium] a successful SFTP delivery of a run with data-errors silently downgraded to plain `DELIVERED`/HEALTHY, erasing the warning (fail-loud regression vs `home_status`) → new **`DELIVERED_WITH_DATA_ERRORS`** status keeps it a WARNING that still acknowledges delivery. Diff (`_compute_diff`) + management views deferred to ROADMAP (not debt). Gates green (1205 passed/1 skipped, cov 93.00%; `job_runner`/`convert_result` 100%; **`test_pipeline_parity.py` 7 passed**; ruff/format clean; mypy(non-UI) no new; bandit(-c) 0; validate-config all 10; tree-check OK). Manual `DISTRICTSYNC_UI=flet` convert/anomaly-ack/SFTP-preflight/exit-3/write-close smoke is human-only (headless here).
- **2026-07-04 — Plan 0021 IA-4: Flet Setup Wizard port (HIGHEST-CARE) — scheduler + SFTP + keyring on the folders step (2 slices — IA-4a `bfcbf5a`, IA-4b `06e0f89`).** Presentation-only, **zero `src/etl|config|sftp|scheduler|main.py` touch** (verified empty; an adversarial security fan-out — secret-leak + zero-core + marshalling lenses, each finding independently refuted — returned **CLEAN** for both slices). The security-critical slice: two credentials on a live boundary; `register_task`/`SFTPUploader`/`validators`/`is_elevated`/`current_run_as_user`/`register_cron` all CALLED UNCHANGED. **Composition:** a **sectioned single scroll** in `build_setup(page)` (folders → schedule → SFTP, each a `components.card`, verdict-first), NOT a multi-page stepper — a security win (the Streamlit stepper parked the Windows password in `st.session_state` across steps; the single scroll reads it at Register-time and never stashes it — **I3**). No `shell.py` change (the `functools.partial(build_setup, page)` swap already points here). **Classifier relocated (IA-4a):** `_classify_schedule_error` → pure COUNTED `src/ui_flet/setup_errors.py classify_schedule_error(msg, elevated)` (single source), rewritten as **plain prose** (dropped `**markdown**` — `HealthVerdictBanner` renders plain `ft.Text`, would show literal `**`); the Streamlit page keeps a **delegating shim** so `test_wizard_schedule_errors.py` stays green until CUT-1. **Password contract (I1–I5, grep-verified + leak-tested):** the Windows account password flows UI field → `register_task(run_as_password=)` → core child-env `DSYNC_TASK_PW` ONLY (never `AppConfig`/disk/log/argv/message; handler-local, single sink); the SFTP password flows UI field → `SFTPUploader.store_password` → OS keyring ONLY; NEITHER reaches `AppConfig` (no password field) — only the 5 non-sensitive SFTP fields + `schedule_time` persist; the relocated classifier never appends the password (input is core-sanitized; COUNTED leak-test proves a fake secret in `msg` never rides a classified branch). **Scheduler (IA-4a):** run-time (`validate_run_time` raise-gate; raw `"HH:MM"` passed on, NOT the returned tuple) + run-as caption + Windows password field → `register_task`; ok+pw → HEALTHY unattended, ok+blank → WARNING (logged-on-only), fail → `classify_schedule_error(msg, is_elevated())` in an ErrorCard; `schedule_registered=True` flips `nav.needs_setup`. Linux → `register_cron` (no password/classifier). **SFTP (IA-4b):** host `ft.Dropdown` from `sorted(ALLOWED_SFTP_HOSTS)` (allowlist structural + `validate_sftp_host` boundary) + creds → `store_password` (**wrapped** — it re-raises on keyring failure → calm ErrorCard, `{e}` is the keyring error not the password) + `get_stored_password()` round-trip readability check → verdict; **"Test connection"** reuses `SFTPUploader.test_connection()` **marshalled** via `page.run_thread`→`page.run_task` (first real use of the worker pattern; no SystemExit split — returns `(bool,str)`; controls mutated only on the UI thread pre-thread + in the `_show_result` coroutine; 30s timeout bounds a hung connection → no freeze). **Management (edit/disable) views deferred to ROADMAP** (idempotent surface covers the 2–3×/yr need). Gates green (1174 passed/1 skipped, cov 92.78%; `setup_errors.py` 100%; ruff/format clean; mypy(non-UI) no new; bandit(-c) 0; validate-config all 10; tree-check OK). **CUT-1 note:** delete the `01_Setup_Wizard.py` shim + `test_wizard_schedule_errors.py` with the Streamlit page. Manual `DISTRICTSYNC_UI=flet` scheduler/SFTP smoke (register ±pw, classify-on-fail, store+round-trip+test, no-secret-in-banner/config) is human-only (headless here).
- **2026-07-04 — Plan 0020 IA-3: Flet Home three-way health dashboard + pure trust core (2 slices — IA-3a core `03f724e`, IA-3b view `5bb439d`).** Presentation-only, **zero `src/etl|config|sftp|scheduler|main.py` touch** (verified empty across both). The flagship trust surface: a configured admin opening Home gets one plain-language **verdict** (green/amber/red) answering "did the roster sync?" **before** any metric, and the cockpit **never crashes**. **Trust core (IA-3a, pure COUNTED, 100%):** `run_log.read_run_records(log_path=None) -> list[dict] | None` — parses `__DISTRICTSYNC_RUN__` tags from `user_log_file()`, newest-first, malformed lines skipped; **missing file → `[]` (no runs yet, calm) vs unreadable → `None` (status unavailable)** — the load-bearing graceful-degradation split (both TESTED; reusable by IA-6). `home_status.derive_home_status(records, app_config, *, now) -> HomeStatus` — a 9-rule first-match-wins ordered list (None→unavailable · []→empty · failed-ETL→FAILED · SFTP-failed→FAILED · anomaly→WARNING · data_errors→WARNING · stale→WARNING · else HEALTHY+metrics), faithful to `03_Run_History._status_cell`'s precedence + the exit-code contract (1 ETL / 3 SFTP). **TOTAL** — all `.get` defaults, no path raises; unparseable timestamp skips staleness. **Privacy (LIVE/top):** the record's free-text `error` (`str(e)`, can carry a path/`sis_type`) is **NEVER interpolated** into `headline`/`detail` — faults named by CATEGORY from structured fields only (TESTED: a fake-path `error` reaches neither). `is_stale`/`STALE_AFTER_HOURS=36` (nightly + generous skew; KISS, no per-tz math; reused by IA-6). `humanize.friendly_timestamp` (total; unparseable→"recently"). **Single-sourced `nav.needs_setup(app_config)`** (= `not (is_complete() and schedule_registered)`) — `_prominent_group` + the Home dispatcher + the shell swap all delegate (gate #1 fix: rule 1 must gate on *scheduled* too, else a configured-unscheduled install wrongly saw the dashboard). **View (IA-3b, omitted):** `screens/home.py build_home(page, *, app_config, on_navigate)` — three-way dispatch (a=`build_onboarding` reused verbatim when `needs_setup`; b=green verdict + 5-tile metrics — rostering always + myBlueprint+ only-when-nonzero, `StudentAttendance` omitted — + plain last-run time + SFTP ✓; c=amber/red banner NAMING the fault + fix-path CTA), verdict-first, wrapped in a never-crash `ErrorCard` floor. `shell.py` swaps `home` UNCONDITIONALLY to `build_home` (which owns the branch via `needs_setup`; IA-2's unconfigured-only swap deleted). **Sync read on mount, NO loading state** (worker-thread rule is `run_pipeline`-scoped; a kB log ≠ freeze; async = the conventions doc's #1 trap for no gain); the empty state IS built (DS-1's deferred pattern, first consumer). **Verify caught + fixed:** IA-3b's initial `assert fix is not None` (type-narrowing) tripped bandit **B101** under the real CI gate (`bandit -r src/ -q -c pyproject.toml`, which skips the pre-existing B603/B607 subprocess findings via `[tool.bandit] skips`); refactored `_fix_button` to take the `FixAction` directly (no assert, no stripped-under-`-O` risk). Gates green (1162 passed/1 skipped, cov 92.75%; `run_log`/`home_status`/`humanize`/`nav` 100%; ruff/format clean; mypy(non-UI) no new — `classes.py:130` pre-existing; bandit(-c) 0; validate-config all 10; tree-check OK). Rulings (`[]`-vs-`None`, sync-read/no-loading, error-not-verbatim, failed-ETL>failed-SFTP) recorded for IA-5/IA-6 reuse. Manual `DISTRICTSYNC_UI=flet` branch-matrix (healthy/broken/empty/unavailable) is human-only (headless here).
- **2026-07-04 — Plan 0019 IA-2: Flet first-run onboarding hero + decouple-the-sync reassurance (folds DS-2).** Presentation-only, **zero `src/etl|config|sftp|scheduler|main.py` touch** (`git diff aa9150b..HEAD` over those paths empty). New reusable **`build_onboarding(page, *, sis_type, on_start_setup)`** (`screens/onboarding.py`, view-omitted) — a calm branded **verdict-first** welcome for the UNCONFIGURED admin: hero card (greets the friendly district name, never a raw id), `HealthVerdictBanner(Verdict.WARNING, "You're not set up yet")` reusing the DS-1 spine, plain first-run steps + a "Start setup" CTA. **Callback-driven** (`on_start_setup` injected by the shell → `select_by_id("setup")`; owns no nav/lifecycle) so **IA-3's Home branch (a) reuses it verbatim**. Wired via an **unconfigured-only `home`-slot swap** (`functools.partial`, gated on `not (is_complete() and schedule_registered)` — De-Morgan-identical to `nav._prominent_group`) — configured installs keep the placeholder until IA-3; no new nav destination (IA-3 replaces the same swap with its three-way dispatcher — no throwaway). **Decouple-the-sync reassurance = an AMBIENT always-on-screen line** (muted shield icon + "Closing this window won't stop your nightly sync.") in the `nav_rail` trailing directly above Exit. **Chosen over a confirm-on-exit dialog** (gate-forced product call): a 2–3×/yr admin closes the cockpit repeatedly — a per-close dialog is friction and implies risk where there is none; an ambient line is read at the leave decision with zero friction. **OS-✕ coverage:** the line is *always visible whenever the window is open*, so it satisfies `0013:43`'s "every leave point" regardless of leave path (in-app Exit OR title-bar ✕); a per-close cue stays deferred behind a field signal at the `_on_leave`/`do_exit` seam (same YAGNI bar). `_on_leave` stays a no-op (IA-5 still owns the write-guard there). **DS-2 folded** into new pure COUNTED **`humanize.friendly_district_name(sis_type, *, config_dir=None)`** — total (empty→`""`; else `district_name` stripped iff non-empty, else raw id; any load failure→raw id, logged) via `load_config`'s own `config_dir` seam; `screens/setup._district_options` DRY-reuses it (inline lookup + dead `logging` import removed). Kept minimal — only district_name; IA-9 owns the broader humanization sweep. Tests: `tests/test_ui_flet_humanize.py` (8, COUNTED, `humanize.py` 100%) incl. two **required synthetic `tmp_path` fixtures** (empty `district_name`→raw id; malformed YAML→`except`→raw id) — branches no bundled config (all 9 carry a non-empty name) can reach. **Honest note:** `test_ui_flet_setup.py` does NOT cover `_district_options` (view-omitted) — the DRY'd logic is covered at its new COUNTED home + manual dropdown smoke. Gates green (1092 passed/1 skipped, cov 92.37%; ruff/format clean; mypy(non-UI) no new — `classes.py:130` pre-existing; bandit 0 new; validate-config all 10; tree-check OK). Commit `2b3eeee` on `feat/0019-flet-ia2-onboarding` → ff-merged to `feat/flet-ui-rebuild`. Manual `DISTRICTSYNC_UI=flet` GUI pass (onboarding render / district-by-name / reassurance wrap / configured-path placeholder) still human-only (headless here).
- **2026-07-03 — Plan 0018 IA-1: Flet shell assembly + state-aware **reordered** flat `ft.NavigationRail` (option a); F6 shell split.** Presentation-only, **zero `src/etl|config|sftp|scheduler|main.py` touch** (`git diff cbc2af3..HEAD` over those paths is empty). The PLAT-1 nav *prominence model* is now RENDERED: two pure COUNTED helpers in `nav.py` — `ordered_destinations(model)` (flat list, `prominent_group` moved first, remaining groups in canonical `NavGroup` order, **empty groups dropped**, within-group order preserved, total) + `prominent_initial_id(model)` (prominent group's first dest id; **total** — falls back to `ordered_destinations(model)[0].id`, then `""`) — `nav.py` 100% covered. **Design fork resolved by the plan-gate (CHANGES_REQUIRED, folded):** keep ONE flat `ft.NavigationRail` made state-aware by **reordering** its destinations so the prominent group leads — **NOT** a custom `ft.Column` nav with section headers. Why: 0013 mandates the `NavigationRail` *control* + "grouped/prominent," never header *text*; the user job ("lead the unconfigured admin to Setup, the configured one to Everyday") is fully met by state-aware ORDERING; and a custom nav's keyboard-focus/a11y is **unverified on flet 0.85.3** (zero focus/keyboard code in-repo; the conventions doc warns 0.2x focus memory is the wrong API) — `ft.NavigationRail` gives keyboard traversal + selection semantics + a focus indicator **for free**. Section headers / stacked-rails / custom nav deferred behind a future **time-boxed a11y spike** only if headers ever become genuinely required (recorded, not silently cut). **F6 boundary split (flagged at PLAT-1/DS-1) discharged:** the ~60-line rail VIEW (brand mark + destinations + Exit trailing) extracted to the new coverage-omitted **`src/ui_flet/nav_rail.py`** (`build_nav(*, ordered, selected_id, on_select, on_exit)`); `shell.py` slimmed to assembly + lifecycle (−105 net lines). Reconfirmed the module is warranted (not manufactured for a 10-line reorder): the reorder *logic* is the pure `nav.py` helper, `nav_rail.py` is the rail *view* leaving shell. **Lifecycle stays in the shell (gate #3):** `do_exit`/`_on_leave`/`on_window_event`/`on_disconnect` unchanged; `build_nav` receives `on_select`/`on_exit` callbacks and owns no lifecycle/config. **Selection:** id-keyed `render_by_id`/`select_by_id` (the `screens` dict is already id-keyed) decoupled from render order; the **initial** selected screen is `prominent_initial_id` (unconfigured → Setup; configured+scheduled → Home), not index 0. **Highlight is NATIVE (gate #4):** `ft.NavigationRail` self-manages `selected_index` on click; the shell sets only the initial index and holds no rail reference; a11y baseline met by RETAINING the built-in control (no custom focus code). Tests: `tests/test_ui_flet_nav.py` +12 (`ordered_destinations` — each live state leads correctly, permutation-none-dropped, within-group order, empty-groups-dropped; `prominent_initial_id` — both states + empty-prominent fallback + no-destinations→`""`). Gates green (1084 passed/1 skipped, cov 92.33%; ruff/format clean; mypy(non-UI) no NEW errors — the `classes.py:130` pandas-stubs artifact is pre-existing; bandit 0 NEW — its 3 Low-sev issues are all pre-existing in `scheduler/windows.py`; tree-check OK). Commit `9588fe9` on `feat/0018-flet-ia1-shell` → ff-merged to `feat/flet-ui-rebuild`. Remaining: manual `DISTRICTSYNC_UI=flet` GUI pass (headless here) — rail leads Setup unconfigured → Home configured, native highlight, clean close.
- **2026-06-30 — Removed the MkDocs documentation-site generation (site-gen tooling only; `docs/` content kept).** Dropped `mkdocs.yml`, the `mkdocs-material`/`mkdocs-with-pdf` dev deps (`requirements-dev.txt` + `pyproject [dev]`), the `release.yml` `deploy-docs` job (`mkdocs gh-deploy --force` on tag), and the `Makefile` `docs`/`docs-serve` targets. **Why:** the MkDocs GitHub-Pages site was part of the pre-Flet docs approach and is already semi-orphaned — the release body points users to the external **SpacesEDU Help Centre** + the in-app Help page, not the Pages site; with Streamlit (and its `05_Help.py` docs renderer) slated for removal at CUT-1 and the partner-facing docs strategy deferred to the org-KB decision (0013's config-capability epic), maintaining `mkdocs-with-pdf` + a deployed Pages site is dead weight. **Kept (NOT deleted):** all `docs/` markdown content (partner/ + developer/ guides, `index.md`, assets) — still rendered by the Streamlit Help page until CUT-1 and read by the harness; recoverable for the future doc-strategy pass. The deployed `gh-pages` branch is left in place (no longer updated). **Deferred (noise-entangled):** `CLAUDE.md` still has stale MkDocs prose (the Documentation command block, the "single source of truth shared with MkDocs" line, the harness-scope mention) — left untouched because `CLAUDE.md` carries pre-existing uncommitted edits that must not be swept into a slice commit; correct it in a clean pass. Nothing imports mkdocs (CLI-only) → CI/tests/build unaffected.
- **2026-06-30 — Conventions-doc `FilledButton` form corrected (`text=` raises → use `content=`); root cause of a latent PLAT-2 button render-crash, fixed in DS-1.** `docs/FLET_1.0_CONVENTIONS.md` previously documented `ft.FilledButton(text, …)`, but on the pinned `flet==0.85.3` there is **no `text` kwarg** (`dc.fields(ft.FilledButton)` → `content`, not `text`): `ft.FilledButton(text="Save")` **raises `TypeError`**, while positional `ft.FilledButton("Save")` works (sets `content`). PLAT-2's `picker_field.py` Browse + `screens/setup.py` Save used the broken `text=` **keyword** form → they would have **crashed on render** — undetected because Flet views aren't instantiated in unit tests and the manual `DISTRICTSYNC_UI=flet` check was deferred. DS-1's adoption of the component factory (`src/ui_flet/components.py`, which builds via `content=`) fixed every in-tree occurrence; the conventions doc is corrected and now points at that factory as canonical so the trap can't recur. `launcher.py`'s error-dialog button used the (working) positional form. **Lesson:** a wrong API form in the authoritative conventions doc propagated into shipped code the gates couldn't catch (view glue is coverage-omitted + GUI-only) — verify control forms against the installed package, and prefer the shared factory over hand-rolled controls.
- **2026-06-30 — Plan 0017 DS-1: Flet design-system layer — pure `verdict.py` (Verdict→visuals, non-colour-cue tested) + amber-`#B45309` warning (AA-gated) decoupled from the M3 `secondary` role (now `MB_ACCENT`) + `card`/button/`ErrorCard`/`HealthVerdictBanner` primitives adopted in shell/picker_field/setup (Save-gate disabled styling preserved).** Presentation-only, **zero `src/etl|config|sftp|scheduler|main.py` touch** (`git diff --stat` confirms `src/ui_flet/` is the only `src/` change). New COUNTED **`src/ui_flet/verdict.py`** (no flet import, follows `nav.py`): `Verdict` enum + frozen `VerdictVisual(color, icon, headline, tone)` + total `verdict_visuals(v)` — the single source of "what healthy/warning/failed looks and reads like"; **"never colour-alone" is a TESTED invariant** (every visual carries a non-empty `icon` + `tone`, and `color` ∈ the AA-safe verdict tokens). **RC1 (the genuine correctness fix):** `tokens.color_status_warning` was the **sky accent `MB_ACCENT`**, and `theme.py:26` bound the M3 `secondary` role to it — so repointing warning→amber would have silently recoloured `secondary` app-wide. Fix: `color_status_warning` → **amber-700 `#B45309`** (amber-600 `#D97706` FAILS AA — white-on-fill 3.19; amber-700 clears at 5.02) **verdict-only**, and `theme.py` repoints `secondary` directly to **`MB_ACCENT`** so the theme's accent is unchanged (a theme test asserts `secondary == MB_ACCENT != color_status_warning`). New alias `color_on_status_warning` (= white) + the `(color_on_status_warning, color_status_warning)` pair added to `UI_CONTRAST_PAIRS` (gated ≥ AA). New view **`components.py`** (coverage-omitted): `card(gradient=…)`/`hero_gradient`, `primary_button`/`secondary_button(disabled_bgcolor=…)`/`text_button`, `ErrorCard`, `HealthVerdictBanner` (paints colour + resolved icon + headline) — **adopted** in `shell.py` (gradient hero + body card + muted Exit), `picker_field.py` (Browse), `screens/setup.py` (header/form cards + the **security Save-gate** Save button, whose disabled `{DEFAULT: action_primary, DISABLED: border}` fill + `disabled` toggle survive the port byte-for-byte via `disabled_bgcolor`). Dev-only `build_design_demo()` (3 verdict banners + ErrorCard) reachable behind `DISTRICTSYNC_UI_DEMO` (routes Help) — exercises the front-loaded spine, not a user nav entry. **Recorded program deviation:** `0013`'s DS-1 row lists "4 state patterns (empty/loading/error/content)"; this slice ships **error (`ErrorCard`) + content** and **defers empty/loading to their first consumer (IA-3 Home / IA-6 tables)** per build-at-first-consumer (PLAT-1/PLAT-2 precedent) — logged, not a silent cut. Tests: `tests/test_ui_flet_verdict.py` (totality + non-colour-cue + AA-safe colour set) + extended `tests/test_ui_flet_tokens.py` (white-on-amber AA + pair-in-contract) + `tests/test_ui_flet_theme.py` (secondary decoupling); full suite **1075 passed / 1 skipped, coverage 92%** (`verdict.py` COUNTED 100%, `components.py` omitted), tree-check/ruff/mypy(non-UI, no new errors)/bandit/config-validate green.
- **2026-06-30 — Plan 0016 PLAT-2: Flet `ft.FilePicker` async-service wrapper + boundary validation + reusable `PickerField`, landed WITH the Setup folders step.** Presentation-only, **zero `src/etl|config|sftp|scheduler|main.py` touch** (CLI/`run_pipeline`/exit-codes 0/1/2/3 untouched; `git diff --stat` confirms `src/ui_flet/` is the only `src/` change). New COUNTED **`src/ui_flet/filepicker.py`**: the 0.85.3 `ft.FilePicker`-as-async-service wrapper (`pick_directory`/`pick_files` register once on `page.services` via the **non-async, unit-tested idempotent** `_ensure_picker` — RC5; only the `await`-dialog glue is `# pragma: no cover`), boundary validation **split by purity** (RC3): `validate_input_dir` PURE (resolve→exists+is_dir, rejects file-as-path — mirrors `pipeline.py:292`), `validate_output_dir` PURE-STRUCTURAL (parent exists+is_dir, rejects parent-is-file), and a SEPARATE **effectful** `check_writable` (`os.access`, NOT labelled pure, tmp-tested) with a TOCTOU note deferring the real write guarantee to the loader's backup-and-restore atomic `save_all`; UNC/symlink accepted (documented, `resolve()`d). New view glue **`picker_field.py`** (`PickerField` — label · Browse… · path · inline valid/invalid; the one reusable picker every later surface reuses, no tkinter port) + **`screens/setup.py`** (`build_setup(page)` — two `PickerField`s + a SIS/district `ft.Dropdown` sourced from the existing **`loader.available_configs()`** showing `district_name` — RC2/DRY — and a **Save structurally disabled until `setup_state(...).can_save`** so an invalid path can never reach `AppConfig.save()` and flip a false `is_complete()` — RC3 block-not-flag). `shell.build_screens` swaps the `setup` placeholder via **`functools.partial(build_setup, page)`** — dict value type stays `Callable[[], ft.Control]`, the other five placeholders + `render()` untouched (RC4). Coverage: `filepicker.py` (incl. the pure `setup_state`) COUNTED; `picker_field.py`+`screens/setup.py` omitted (views). **Recorded program deviation (RC1b):** per the user's "add the picker where the user picks files", this **deliberately pulls forward** the picker primitive `0013` earmarked for IA-5 and the **folders step** of Setup IA-4 was to own — it mounts via the existing `build_screens` host (no IA-1 rail-prominence / DS-1 library pre-built). Remaining scope stays coherent: **IA-1** still owns shell-assembly + the grouped rail; **IA-4 narrows** to the HIGHEST-CARE scheduler/SFTP/keyring/elevation steps built *on top of* this folders step (risky logic untouched here); **IA-5 reuses** the landed `PickerField`/wrapper. `0013` PLAT-2/IA-4/IA-5 rows re-scoped; `FLET_1.0_CONVENTIONS.md` "picker deferred to IA-5" → "landed in PLAT-2 (`filepicker.py` canonical)". Tests: `tests/test_ui_flet_filepicker.py` (validation + idempotent append) + `tests/test_ui_flet_setup.py` (`setup_state` gate→persist→`is_complete` on a tmp-home `AppConfig`); full suite 1054 passed, coverage 92%, all gates green. Low rework risk (themed via existing tokens; mounts via the existing host).
- **2026-06-29 — Plan 0015 PLAT-3: windowed/no-console/offline Flet exe built + smoked on Win+Linux+macOS via a reusable CI matrix; CI-artifact-only preview.** Build/CI-only, **zero `src/` touch** (the rollback-floor Streamlit exe + CLI/exit-codes 0/1/2/3 untouched). New reusable **`.github/workflows/flet-pack.yml`** (`on: workflow_call`, `permissions: contents: read`, **no `secrets: inherit`** — RC4) packs `flet pack src/ui_flet/launcher.py --name DistrictSync-flet` on `windows-latest`/`ubuntu-22.04`/`macos-latest` (Python 3.13) with per-OS `FLET_DESKTOP_FLAVOR` (full/full/light), a content-bound `~/.flet` cache key (`flet-client-<os>-<ver>-<flavor>`), and the full `src.*`+flet hidden-import set + `--exclude-module streamlit/src.ui`. **Target = `launcher.py` (UI-only preview), NOT `main.py`** — sidesteps the Streamlit-default conflict and keeps `main.py` untouched; CUT-1 repackages `main.py`. **Offline guarantee proven at BUILD time, not just runtime (RC1):** (a) after the warm-up, assert `~/.flet/client/flet-desktop-<flavor>-<ver>` exists+non-empty (the download fallback is silent, so a miss is otherwise invisible); (b) after pack, assert the PyInstaller manifest (`build/DistrictSync-flet/Analysis-00.toc`) references `flet_desktop/app` + a client archive (`scripts/ci_flet_pack_smoke.py --assert-embed`). The **embedded archive (`get_package_bin_dir()`), not the cache, is the source of truth** (anti-poisoning). New **`scripts/ci_flet_pack_smoke.py`** productionizes the throwaway PLAT-0b verifier against the REAL exe: PE `Subsystem==2` no-console (win, `pefile`); runtime offline-embed (move `~/.flet` aside + unreachable `FLET_CLIENT_URL`, **guard `~/.flet` is genuinely absent** so a stale cache can't false-PASS, poll ≥150 s, distinguish alive-but-slow→retry-once from dead→FAIL, print `~/.districtsync/etl_tool.log` on any fail); **zero-orphan close** (baseline-snapshot `flet`/`flet.exe` PIDs before launch, wait for a real top-level window to EXIST, post `WM_CLOSE` via pure `ctypes`/`user32` — no pywin32 — tear the full tree down BEFORE a defensive idempotent `~/.flet` restore that fails loud on corruption, then a baseline-delta orphan sweep). **Close GATES on Windows** (`--require-close`); Linux/macOS **INFO-only/non-gating** (no portable WM_CLOSE; embed still gates on all 3 OS). Embed + close kept as **separate verdict axes**. **`flet pack` gotcha (found at build time):** it has NO native `--paths`/`--exclude-module`/`--collect-submodules`; raw PyInstaller flags go through `--pyinstaller-build-args`, and **each token needs its own `--pyinstaller-build-args=<token>`** (its `nargs="*"` stops at flag-like tokens; PyInstaller needs `--paths` and `.` as separate args). **`release.yml`:** bumped `test`+3 Streamlit jobs+`deploy-docs` to Python 3.13 (was 3.11 — latent mismatch vs `requires-python>=3.13`/`ci.yml`), added a `build-flet` job (`needs: test`, `uses: ./flet-pack.yml`); `publish-release` keeps the **single-exe-per-OS** public Release + adds **SHA-256 checksums** (`SHA256SUMS.txt` — interim integrity anchor; signing is PLAT-4) + a one-line preview note; the Flet exe stays a **CI `upload-artifact`, never a public Release asset** (RC8 — a non-technical admin can't mistake a placeholder-only app for the production download). New **`flet-verify.yml`** (`pull_request` on build/UI/deps paths + `workflow_dispatch` — NEVER `pull_request_target`, RC4) → `uses: ./flet-pack.yml` (anti-rot). `Makefile build-flet-win` parity target. Unit tests (`tests/test_ci_flet_pack_smoke.py`, 20) cover the pure helpers (artifact resolver · baseline-delta orphan set · embed-manifest parse) — scripts ∉ `--cov=src`. **Local proof (Win11, flet 0.85.3, pyinstaller 6.13.0):** real `DistrictSync-flet.exe` built (**189.6 MB** — recorded as the size budget), PE subsystem 2 + offline-embed + zero-orphan close ALL PASS on the real exe; `~/.flet` restored cleanly. 3-OS `workflow_dispatch` green is the remaining acceptance gate. Tree-check scope is `src/**/*.py`+`config/mappings/*.yaml` — `scripts/`+`.github/` are out of scope, so no ARCHITECTURE_TREE entry. Deferred to ROADMAP: delete the throwaway prototype+fork branch, CUT-1 `main.py` repackage+Streamlit drop, repo-wide SHA-pin of actions.

- **2026-06-29 — Plan 0014 PLAT-1: productionized Flet shell + foundation landed (additive `src/ui_flet/`, opt-in via `DISTRICTSYNC_UI=flet`).** New presentation-only package: pure `tokens.py` (8 `MB_*` brand primitives + semantic aliases + a WCAG `contrast_ratio` helper with a frozen `UI_CONTRAST_PAIRS` contract — every fg/bg pair the shell paints is gated >= 4.5:1), `theme.py` (`build_theme()` → Material-3 `ft.ColorScheme`, light-only), `nav.py` (pure state-aware nav model — rail renders FLAT this slice, prominence wiring is IA-1), `shell.py` (themed window + flat `NavigationRail` + a plain `dict[id → placeholder factory]` of calm, branded, in-voice placeholders + zero-orphan close with a documented `_on_leave` leave-point seam), and `launcher.py` (frozen-cwd + early-failure path — full traceback to the ETL log sink, plain-language dialog, non-zero exit). `app_version()` added to `src/utils/version.py` as the single source of truth. The dual-mode entry switch is read at **exactly one site** (`main.py`'s no-argv branch); `DISTRICTSYNC_UI=flet` is the documented **dev default for every subsequent UI slice** so the shell can't rot before CUT-1. Pins LOCKED + gate-enforced (`test_flet_pin`). **Scoping call (per parent 0013's "promote on 2nd use" precedent):** the `JobRunner` worker, `FilePicker` wrapper, screen registry, and write-in-flight close-guard were CUT from PLAT-1 and their knowledge preserved as **contracts in `docs/FLET_1.0_CONVENTIONS.md`** (worker→UI marshal incl. the `SystemExit`/`Exception` `on_error` asymmetry at `pipeline.py:420-426`; FilePicker async-service; `SnackBar`/dialog `show_dialog`; `page.window.*` async forms) — built at first real use (IA-5/IA-1/PLAT-2), not speculatively. **Two AA contrast tweaks** (honest finding): the semantic `color_muted` is slate-600 (not the primitive slate-500, which is 4.38:1 on the page tint) and the healthy-verdict text fill is green-700 (not the bright `MB_GREEN`, 3.30:1 white-on) so every painted pair clears WCAG AA — the brand primitives are preserved unchanged; only the semantic aliases are legibility-tuned. **`shell.py` boundary note (forward-flag):** it carries window + rail + placeholder-host + close-lifecycle (4 concerns) — acceptable at PLAT-1 size, split before IA surfaces grow it (IA-1). Core untouched (`src/etl|config|sftp|scheduler`, `run_pipeline`, CLI flags, exit codes 0/1/2/3); 640 tests + SD74 snapshot stay green; mypy excludes `src/ui_flet`, coverage omits only `shell.py`/`launcher.py`.
- **2026-06-29 — Plan 0013 PLAT-0b: cross-platform packaging verified on real CI (Win + macOS + Linux all GREEN) — the macOS open risk is CLOSED.** Triggered by a "is this compatible with mac and linux, can we make it robust?" check. A throwaway matrix workflow (`flet-xplatform-verify`, branch `ci/flet-xplatform-verify` on the personal fork `sh4npeiris/DistrictSync`, Python 3.13) builds the prototype with `flet pack` and asserts the Flutter client is **embedded offline** via a display-independent signal (with `~/.flet` removed AND `FLET_CLIENT_URL` unreachable, a successful launch can only repopulate `~/.flet/client` by extracting the *embedded* bundle). Result ([run 28370535068](https://github.com/sh4npeiris/DistrictSync/actions/runs/28370535068)): **Windows** `.exe` 62 MB, PE `Subsystem==2` (no console), offline ✅ · **macOS arm64** binary 73 MB, offline ✅ · **Linux** ELF 66 MB, offline ✅ (under `xvfb`). **macOS `flet pack` DOES embed the client** (settled positively on `macos-latest` — supersedes the PLAT-0 "macOS UNVERIFIED" risk; **no `flet build macos` fallback needed**). Also confirmed: the pinned stack `pip install`s clean on all three OSes; Linux defaults to the **`light`** client flavor. **Linux corroborated locally** in WSL2/Ubuntu 24.04 (48 MB ELF, offline-embed ✅). **⚠️ Linux DESKTOP GUI prerequisites** (real finding): the client links **`libsecret-1.so.0`** + GL/GTK (`libgl1`/`libglib2.0-0`/`libgtk-3-0`) and needs a display — a **headless** Linux box can't run the GUI (fails `libsecret-1.so.0`), so a headless Linux server uses the **pure-Python CLI** (zero Flet deps); a Linux *desktop* user needs those libs documented. (User's Linux partner → support **both** paths.) **Clean-close zero-orphan is proven on Windows only** (real `WM_CLOSE`); Linux/macOS clean-close still to smoke on a real desktop (WSLg's software-GL + window-less compositor blocked it). This matrix is the basis for **PLAT-3**'s real `release.yml`. Throwaway (deleted with the prototype after PLAT-1): the fork branch + `docs/reference/flet-prototype-spike/ci_verify_pack.py` + `.github/workflows/flet-xplatform-verify.yml` (only on the fork branch, never landed on `main`). Still no `src/`/core touched.
- **2026-06-29 — Plan 0013 PLAT-0 (throwaway spike) COMPLETE — pin/packaging/lifecycle/CI-preseed all empirically confirmed on a real Win11 target; gate to PLAT-1 cleared.** **(1) Pin LOCKED:** `flet==0.85.3` · `flet-desktop==0.85.3` · `flet-web==0.85.3` (+ `flet-cli==0.85.3` build-only). PyPI confirms `0.85.3` is the **LATEST** (top of the `0.80–0.85.x` 1.0-beta line, `INSTALLED==LATEST`); all wheels install clean on **Python 3.13.2**, `pip check` clean. `pip show` confirms `flet` requires only `httpx/msgpack/oauthlib/repath` — it pulls **neither** `flet-desktop` **nor** `flet-web`, so all three must be pinned explicitly (`flet-web` additionally drags `fastapi`+`uvicorn` → web mode is headless-CI/screenshot only, NOT a runtime dep of the shipped native exe). **(2) Windowed exe:** `flet pack app.py` defaults to **`--noconsole --onefile`** → one windowed, no-console exe; no-console is **deterministic** (built exe's PE Optional-Header `Subsystem==2` WINDOWS_GUI, asserted with `pefile`). **Offline bundling CONFIRMED airtight:** moved `~/.flet` aside **and** pointed `FLET_CLIENT_URL` at an unreachable host, and the packaged exe still opened its window (7.1s) — proving it uses the **embedded** client, not a download. Mechanism (read from installed `flet_desktop.ensure_client_cached` source): resolves **cache → bundled archive (`get_package_bin_dir()`) → download**, and `__download_flet_client()` runs **only** when the bundle is absent; the bundle's presence means the download branch is never reached (the client extracts from the bundle into `~/.flet/client/`, which is why the cache reappears — extraction, not download). **Measured size = ~58 MB** (`60,729,481` B) for the **flet-only spike**; the real app will be larger (adds pandas/pydantic/paramiko/keyring; Streamlit drops at CUT-1) — do NOT quote 58 MB as the shipping size. **(3) Zero-orphan window-close RE-CONFIRMED ON-SCREEN** (the bake-off could only assert via code/API, headless): a real `WM_CLOSE` collapsed the whole tree (`python→python→flet.exe`) in **0.77s from source** and **1.27s for the packaged exe**, **zero orphans** both ways (`psutil` recursive-descendants + stray-`flet.exe` sweep). **(4) CI offline pre-seed DECIDED:** `actions/cache` on `~/.flet` keyed by **`runner.os` + flet-version + flavor**, with a headless **`python -c "import flet_desktop; flet_desktop.ensure_client_cached()"`** warm-up (no-arg on 0.85.3, no `xvfb` needed — it's download+unzip) **before** `flet pack`; cache miss degrades to self-download (fails loud), never to a runtime-download exe. Cache key needs the OS (per-OS client: `flet-windows.zip`/`flet-macos.tar.gz`/`flet-linux-{distro}[-light]-{arch}.tar.gz`) and the Linux runner image must be **pinned** (distro encoded in the filename). `FLET_CLIENT_URL` = air-gap mirror hook; flavor default `light` on Linux / `full` elsewhere (`FLET_DESKTOP_FLAVOR` to override). **(5) ⚠️ OPEN RISK carried to PLAT-3:** macOS `flet pack` client-embedding is **UNVERIFIED** (couldn't test on Win; upstream's 0.85.0 bundle fix named Win+Linux only) → PLAT-3 must smoke-verify on a `macos-latest` runner (fallback `FLET_VIEW_PATH`/`flet build macos`); Linux embedding claimed-fixed but smoke it too. **`docs/FLET_1.0_CONVENTIONS.md` lifted DRAFT → AUTHORITATIVE** for pin/packaging/lifecycle/CI-preseed; the exact 1.0 **API forms** (worker→UI marshalling snippet, `SnackBar`/dialog show API, `page.window.*` async) remain *pending PLAT-1* (harvested while building the real shell). **Recommended gate (PLAT-1):** a CI assertion that the three pins are exact AND match the version named in the conventions doc. **NEXT:** paused for user approval before PLAT-1 (productionized shell). No `src/` touched; 640 tests + SD74 snapshot untouched (spike is isolated to a scratch venv + the reference prototype).
- **2026-06-29 — UI redesign tech LOCKED: Flet 1.0 (native desktop), replacing Streamlit.** After a UX discovery review ([Plan 0012](../.claude/plans/0012-ui-ux-discovery-and-redesign.md)) + an empirical **Flet-vs-NiceGUI bake-off** (identical Home+Convert prototypes built in both; **both passed a real window-close graceful-shutdown test — process tree gone, ZERO orphans**), the DistrictSync UI will be rebuilt as a **native Flet 1.0 desktop app**. Chosen over NiceGUI for staying **pure-Python with no web-UI tech** (no HTML/CSS/JS or web framework to maintain) and a **self-contained renderer** (bundles the Flutter client → works offline, no WebView2 dependency). The **tested ETL/CLI core is REUSED UNCHANGED** (`src/etl`, `src/config`, `src/sftp`, `src/scheduler`, `src/main.py`) — the Flet UI calls it in-process; only the presentation layer (today's Streamlit `src/ui`) is re-implemented. **Pin exact versions** `flet==0.85.3` + `flet-desktop==0.85.3` + `flet-web==0.85.3` (bare `flet` runs neither native nor web). **Beta accepted:** Flet 1.0 ships as the `0.80–0.85.x` beta line (a ground-up rewrite vs the old `0.2x` API) — risk mitigated by version-pinning + a repo **"Flet 1.0 conventions & gotchas" doc** (slice PLAT-1) anchoring future agents to the new typed-dataclass / `ft.run` / FilePicker-async-service API instead of stale `0.2x` training memory, plus UI tests. **Graceful shutdown is now native** (OS window-close ends the process cleanly, ~0.5s, no orphans — retiring the Streamlit idle-watchdog/Exit-page workarounds on `feat/graceful-ui-shutdown`). Packaged windowed (`flet pack` / PyInstaller `--noconsole`) → **no console window** (kills the original console-hangs-around bug) → code-sign (EV Authenticode) to clear Windows SmartScreen. **Flet docs (version-matched — future agents read these, NOT old 0.2x tutorials):** getting started https://flet.dev/docs/getting-started/ · controls https://flet.dev/docs/controls/ · API reference https://flet.dev/docs/reference/ · 1.0 beta notes https://flet.dev/blog/flet-1-0-beta/ · PyPI https://pypi.org/project/flet/ · GitHub https://github.com/flet-dev/flet. Full build plan: `.claude/plans/0013-flet-production-redesign.md` (in progress).
- **2026-06-25 — Plan 0009 Slice 2: scheduler errors de-CLIXML'd + the wizard classifies registration failures by elevation.** A real failed `Register-ScheduledTask` surfaced PowerShell 5.1's CLIXML error serialization — a `#< CLIXML … <Objs>…<S S="Error"> : Access is denied.</S></Objs>` blob that echoes the whole failing script (incl. the `$env:DSYNC_TASK_PW` literal) and buries the human message — because `Write-Error` to a redirected stderr is CLIXML-wrapped. **Fix at the source:** the script's `catch` now emits the bare message via `[Console]::Error.WriteLine($_.Exception.Message); exit 1` (plain text, no CLIXML; success path `Write-Output 'DSYNC_OK'` unchanged). **Defensive Python fallback:** `_clean_ps_stderr(text)` runs on captured stderr BEFORE the canonical-marker match — if `#< CLIXML` is present it extracts ONLY the **first** `<S S="Error">` node (the message line; later nodes echo the script body, never returned), decodes `_x000D_`/`_x000A_`, strips the leading `"<cmd> : "` positional prefix; otherwise returns the text stripped. Bounded regex over `<S S="Error">(.*?)</S>` (no XML parser on untrusted text); a leak test asserts the cleaned message contains "Access is denied" but neither `CLIXML`/`<Objs`/the script body nor `DSYNC_TASK_PW`/the secret. So the canonical markers (`"PowerShell not found"` / `"ScheduledTasks module not available"` / Windows' `"Access is denied"`) still match the cleaned text and `register_task` returns a clean one-liner. **Wizard:** new `is_elevated()` in `windows.py` (win32 `ctypes.windll.shell32.IsUserAnAdmin()` wrapped try/except → False; non-win32 → False; module-level + mypy-clean since UI is mypy-excluded). The wizard's blanket "Run as administrator" failure branch is replaced by a pure, unit-testable `_classify_schedule_error(msg, elevated)`: not-elevated + access-denied → run-as-admin; elevated + access-denied → batch-logon-right / wrong-password (account password not Windows Hello PIN / MSA microsoft.com password) hint; `"PowerShell not found"` / `"ScheduledTasks module not available"` → their own actionable lines; else → the clean `msg`. Kills the misleading run-as-admin loop the already-elevated SD54 admin hit. Slice-1's leak tests still pass against the new `catch`. UX-only — no SD54 dependency; SD74 snapshot byte-identical (scheduler outside the ETL path).
- **2026-06-25 — Plan 0009 Slice 1: Windows scheduled-task registration moved from `schtasks /Create /XML` to PowerShell `Register-ScheduledTask`.** The 2026-06-15 long-path fix paired a `/XML`-declared `<LogonType>Password</LogonType>` with `schtasks /RU /RP` credentials; that handoff fails `ERROR: Access is denied` even when elevated (the SD54 rollout blocker). **Fix:** `register_task` now runs a FIXED PowerShell script (`_build_register_script(has_password)`, references only `$env:DSYNC_*`) by **base64-encoding it as UTF-16LE and passing it via `powershell.exe -EncodedCommand`** (no script file on disk) and passes all dynamic values — incl. the password — via a **fresh-copy child env** (`_build_env()`, `{**os.environ, …}`; `os.environ` never mutated). **`-EncodedCommand`, not stdin (`-Command -`):** piping a multi-line `try/catch` to `-Command -` over stdin **silently no-ops on PowerShell 5.1** (read line-by-line, the block returns exit 0 with no output and registers nothing → reports success while doing nothing); `-EncodedCommand` parses the script as one complete unit so the fail-loud `try/catch` actually runs. Do not revert to stdin. The unattended path forces the stored-credential logon with an **explicit** `New-ScheduledTaskPrincipal -LogonType Password -RunLevel Highest` (not the `-User/-Password/-RunLevel` parameter-set inference, which can silently degrade to `S4U`/`Interactive` on some PS 5.1 builds) → `New-ScheduledTask -Principal` → `Register-ScheduledTask -InputObject $task -User -Password -Force`. **Password-via-env, not argv** is a hardening win over the legacy `/RP <pw>` (which was visible to all users in the process list); the script never echoes `$env:DSYNC_TASK_PW`, and a leak-closure test asserts the value + the `DSYNC_TASK_PW` literal appear in neither the returned message nor `caplog` (success and failure). Settings parity with the old XML preserved (`-StartWhenAvailable:$false` no-catch-up, battery flags, `PT2H`, `IgnoreNew`); run time parsed with `InvariantCulture` (locale-safe). No-password path → `-LogonType Interactive -RunLevel Limited` (matches the prior `InteractiveToken`, never S4U; `run_highest` stays ignored). Fail-loud canonical messages — `"PowerShell not found"` (FileNotFoundError) / `"ScheduledTasks module not available"` (cmdlet-not-found) / else raw PS exception text verbatim — are the contract the Slice-2 wizard classifier keys off. **`delete_task`/`query_task` deliberately stay on `schtasks.exe`** (read-only/name-only, no length/credential surface, fully tested, and `query_task`'s LIST-parse output is consumed by `Home.py`) — consistency migration is a tracked ROADMAP follow-up, not this slice. No new PyInstaller hidden imports (plain subprocess to a system binary). Implementer verified the no-password path live (registers `Interactive`, queried, deleted); the credentialed/unattended path is gated on the user live-verification checklist (`LogonType=Password`/`RunLevel=Highest` + logged-off SFTP-reachable run + `Last Run Result=0x0`). SD74 snapshot byte-identical (scheduler is outside the ETL path).
- **2026-06-23 — Plan 0008 Item 1: `run_transform` skips an entity only when ALL its source frames are empty (was: positional-first frame empty).** The old skip (`primary_df = raw_data.get(source_files[0]); if primary_df.empty: continue`) dropped any entity whose listed-first source was empty — wrong for an entity that resolves its bands **by role** (`StudentAttendanceTransformer`, daily/period order-independent). A period-only `sd51attendance` district lists `daily_absences` first, so an empty daily frame skipped a fully-populated period band → no `StudentAttendance.csv`, run exits 0 (silent). New: `source_frames = [raw_data.get(sf, …) for sf in source_files]; if all(df.empty …): skip`; the positional-first is still passed as the primary (may be empty for a role-resolved entity — transformers tolerate it: Enrollments/Classes return empty on empty schedule, Attendance unions only role-resolved bands). The `transformed.empty` net remains the final backstop, and the 0006 no-usable-input guard (keys off INPUT presence) is independent and unweakened. Reproduce-first: a period-only `run_transform` asserted StudentAttendance ABSENT first, now produced. SD74 byte-identical (none of its entities are role-resolved).
- **2026-06-23 — Plan 0008 Item 2: `append_year_to_id` stays column-level resilient (won't-fix-by-decision).** Considered making the `append_year_to_id` branch of `apply_field_map` per-row resilient (mirroring the `transform:` path). Declined: its helper `generate_class_id` performs no fallible operation (a `row.get` + an f-string — it cannot raise on a single row), so a per-row try/except would defend a failure that cannot occur and add a second near-duplicate resilience loop (DRY/YAGNI). The branch already blanks the whole column **loudly** (recorded as a column-level `data_error`, never silent) on any structural failure. Tightened the docstring to record this; promote to per-row only if `generate_class_id` ever gains a parse/IO step. No code change.
- **2026-06-24 — Plan 0008 Item 3 (revised, post-approval): stale entity CSVs are ARCHIVED after each run, not deleted.** Added `DataLoader.archive_stale_outputs(emitted) -> sorted[str]`: detects stale entity CSVs via the pure `detect_stale_outputs(emitted)` (lists `<EntityName>.csv` whose stem is a registry-known entity and not emitted this run), then **moves** each into a lazily-created `output_dir/archive_<ts>/` via `os.replace` (atomic same-fs); `run_pipeline` calls it after a successful `save_all` and logs an INFO with the archived names (no exit/run-log change). **Non-destructive `os.replace` move sidesteps the cross-config data-loss that blocked an auto-delete:** under `_base` inheritance no *delete* allow-list is cleanly safe — a registry/`mappings.keys()` set would erase a DIFFERENT config's inherited-but-disabled entity CSV sharing the dir (e.g. an `mbp_core`-written `CourseInfo.csv`), and an `enabled_entities` set would BOTH miss a config-dropped entity AND wipe an enabled entity's last-good CSV on an empty-input run — so moving aside (recoverable bytes) is the safe primitive. **Enabler:** the SFTP uploader globs `output_dir.glob("*.csv")` — top-level only, non-recursive (`uploader.py:182`) — so a file inside the `archive_<ts>/` SUBfolder is auto-excluded from delivery (a stale CSV can no longer ship). Best-effort: a per-file move failure is logged at ERROR and skipped (the roster already committed + delivered — an archive hiccup must not fail the run). Supersedes the earlier warn-only landing. **Foot-gun (Stage-9):** any future output-dir *delete*/prune must still derive from the resolved `enabled_entities`, never `mappings.keys()` — `_base` inheritance defines more entities than a config enables, so `mappings.keys()` includes inherited-but-disabled entities and would cause cross-config data loss.
- **2026-06-23 — Plan 0007: `save_all` commit is now true all-or-none (`os.replace` + per-file `.bak_<ts>/` backup/restore).** Replaced the per-file `shutil.move` commit loop — non-atomic **across files** (a move failing on file K left output a torn mix of new 1..K-1 + stale K..N, then the staging dir was deleted so the tear was permanent) **and** (Windows overwrite) **within a file** (`shutil.move`'s `os.rename` raises `FileExistsError` on an existing dest → copy2+unlink fallback can leave a half-written dest). New `_commit_staged(sorted(staged_files), backup_dir)`: per file, move any existing target aside into `.bak_<ts>/` then promote the staged file with `os.replace` (atomic same-fs overwrite); on any failure roll back in reverse `applied` order (remove new dest / restore backup) and re-raise the original — rollback completes **before** `save_all`'s `finally` rmtrees `.bak_<ts>/` (restore-before-cleanup invariant). New-entity path takes no backup and rolls back via `unlink(missing_ok=True)`, never `os.replace(None, …)`. Reproduce-first: 4 role-based `os.replace`-injection tests captured the torn output as a failure first. SD74 byte-identical. (Chose Option A over keep-`shutil.move`+backup, which leaves the within-file tear unfixed.)
- **2026-06-23 — Plan 0006: the "fail loud, don't fake success" trio (one slice).** Three "false green" defects in an unattended PII tool fixed together. **(1) `apply_field_map` is now row-resilient + loud.** Previously `series.apply(func)` aborted the WHOLE column if any one row raised, and the outer `except Exception` silently blanked it to `pd.NA` while the run reported success — and (corrected fact, verified at runtime) the unknown-transform `raise ValueError` was ALSO swallowed by that same handler (returned `[<NA>, <NA>]`, only logged a traceback), so it never actually failed loud. New: transforms apply **per-row** — a row whose `func` raises gets `pd.NA` in **that cell only** while valid rows keep their correct value (so the accurate transform path is untouched → SD74 byte-identical); the unknown-transform / any column-level error blanks **that column** and continues (do NOT raise — config fail-fast validation of `ALLOWED_TRANSFORMS` is the cleaner fix, backlog T2.4). Every failure is recorded to the new `TransformContext.data_errors` ledger, logged at ERROR, and surfaced in the run-log as a `data_errors` summary (`{total, by_field}`) + Run History "Completed with N data errors". **Data errors are a SEPARATE axis** — ETL `status` stays `success` and the run still delivers (never silently swallowed, never fails the run for one bad row). `TransformOutputs` gains a third field `data_errors`; threaded out of `run_transform` via `DataTransformer.data_errors`. **(2) No-usable-input guard** in `run_pipeline` right after `extractor.load_data`: `not raw_data or all(df.empty …)` → ERROR naming the missing/empty files + raise → existing outer handler writes `_emit_run_log("failed")` + re-raises → `main` exit 1. **Keys off INPUT presence (`raw_data`), not `outputs=={}`** — chosen so a supported **period-only `sd51attendance`** run (period file present, daily absent) does NOT false-fail: `outputs=={}` would have, because `run_transform`'s positional-primary skip drops StudentAttendance when the daily file (its listed-first source) is empty (a separate latent bug this guard neither masks nor fixes). A *partial* run with some empty sources stays exit 0 by design. **(3) Run History honest Status** (display-only, additive): the at-a-glance Status cell shows amber "ETL OK · SFTP FAILED" / "Completed with N data errors" derived from the same booleans already in each record (no exit-code or run-log `status` change; the dedicated SFTP ❌ column is unchanged). SD74 snapshot byte-identical (a clean run fires none of the new branches).
- **2026-06-23 — Plan 0005 Slice 3: anomaly compute single-sourced; diff stays per-surface.** Factored the pure row-count-drop computation into `pipeline.compute_anomalies(outputs, output_dir) -> list[str]` (one `ANOMALY_THRESHOLD = 0.20`, plain `"{entity} dropped from {prev} to {new} rows ({pct}% decrease)"` base messages, **no logging/printing**). The CLI's `_check_anomalies` is now a thin wrapper that adds its existing presentation (the `ANOMALY:` prefix + `logger.warning`, preserving today's log output exactly); the Convert page deletes its duplicated `_check_anomalies_ui` + the second `ANOMALY_THRESHOLD` literal and calls the shared `compute_anomalies`, rendering each base message as `st.warning(f"Anomaly detected: {msg}")` + the existing caption. `_compute_diff` left untouched — diff stays per-surface (its documented pyarrow/Arrow string-coercion workaround is irreducibly UI-specific). Single source of truth for the anomaly business logic + threshold; both surfaces keep their user-visible wording. New `TestComputeAnomalies` unit tests assert the shared list both surfaces consume. SD74 snapshot unaffected.
- **2026-06-23 — Plan 0005 Slice 2b: the Convert page is now a thin adapter over the shared engine; CLI↔UI byte-parity locked.** `02_Convert.py` `run_conversion` deletes its inline transform loop / school-year block / `ordered + extra` field-ordering and delegates to `run_transform` (returns `TransformOutputs`); both write paths route through `DataLoader` — SFTP via `save_all`, and the download zip + per-file buttons via the new `DataLoader.csv_encoding(entity)` helper (the **single source of truth for the BOM rule**, extracted from `_write_csv` so the download path stops hardcoding `utf-8-sig`). This is the direct fix for the audit Tier-1 "Browser Convert writes StudentAttendance.csv with a BOM." Two deliberate, desirable behavior changes (now explicit + tested): (a) a missing field-map column makes the UI write **fail loud** (`DataLoader._write_csv` raises `ValueError`, surfaced as `st.error`) instead of writing a partial file; (b) the UI writes **exactly the contract columns** (`df[field_order]`, extras dropped). A fourth honest behavior change: the old Convert page caught load errors **per file** and continued with the remaining uploads, whereas the new adapter wraps the whole upload batch in one `load_from_bytes` `try` — so a single unparseable upload now **aborts the entire conversion** (desirable: matches the CLI's `load_data` abort-on-`ExtractionError`; no partial/divergent run from a bad file). New `tests/test_pipeline_parity.py` runs the same synthetic GDE bytes (sd51myedbc — 5 rostering + StudentAttendance) through both `run_pipeline` and the UI adapter path and asserts per-entity frame-equality AND on-disk `read_bytes()`-equality, explicitly covering StudentAttendance (no BOM) + Students (BOM) and the identical CLI/UI column sets. SD74 snapshot byte-identical (unaffected).
- **2026-06-22 — Attendance `Absence Date` format is now per-district configurable (`global_config.attendance.date_format`, default `yyyy-MM-dd`).** A district may override the StudentAttendance output date shape in its mapping; SD51 uses the default ISO. Uses **friendly tokens** (`yyyy` `yy` `MMMM` `MMM` `MM` `dd` + literal separators — not raw strftime codes) so non-technical config authors read/write it the way they speak (`yyyy-MM-dd`, `dd-MMM-yyyy`). `BaseTransformer.friendly_date_format_to_strftime()` translates **and validates fail-loud**: an unrecognized alphabetic token (e.g. a lowercase `mm` typo) raises at the boundary instead of silently emitting a constant/garbled date. Zero-padded tokens only (Windows strftime lacks portable non-padded directives). Resolved + validated ONCE per run in `StudentAttendanceTransformer.transform()` and reused by both bands. New `BaseTransformer.format_date(value, strftime_fmt)` shares the flexible input-parse grid with `normalize_iso_date` (factored into `_coerce_date`), so input-format recognition is unchanged. No Pydantic change (`attendance` is an open dict, threaded through `to_raw_dict`). The base `StudentAttendance.field_map` "Absence Date" placeholder was reduced to a direct mapping (its value was never executed; the date format's single source of truth is now `attendance.date_format`). SD74 snapshot byte-identical (attendance not enabled there).
- **2026-06-22 — Attendance `Absence Date` now emitted as ISO `yyyy-MM-dd` (was `DD-MMM-YYYY`).** SpacesEDU's standalone attendance importer rejects non-ISO date shapes — the *"Invalid date format"* error that the 2026-06-19 BOM decision attributed to the UTF-8 BOM was only partly the BOM; the `DD-MMM-YYYY` shape itself is also rejected. `StudentAttendanceTransformer` now formats both bands' dates with the existing `normalize_iso_date` (input GDE dates in any of the 4 recognized formats — `dd-MMM-yyyy` / ISO / `m/d/yyyy` / `d/m/yyyy` — still normalize to ISO, so input-format intelligence is unchanged). Reverses **only the OUTPUT shape**; rostering dates are untouched (only the standalone attendance feed was failing). Removed the now-dead `format_dd_mmm_yyyy` helper — a near-clone of `normalize_iso_date` differing only in its `strftime` (DRY / no dead code) — and swapped it for `normalize_iso_date` in `ALLOWED_TRANSFORMS` and the base `StudentAttendance.field_map` transform placeholder (that field_map's values are decorative: the transformer builds rows directly and never calls `apply_field_map`). ISO is a fixed SpacesEDU contract (not per-district), so it's hardcoded, not configurable (KISS/YAGNI). SD74 snapshot byte-identical (attendance not enabled there).
- **2026-06-19 — Plan 0004: `StudentAttendance.csv` written WITHOUT the UTF-8 BOM.** SpacesEDU's standalone attendance import rejected the file — *"Unexpected file: StudentAttendance.csv"* + cascading *"Invalid date format"* — even though the data matched the spec (valid `DD-MMM-YYYY` dates, 4 case-sensitive columns). Root cause: `DataLoader._write_csv` wrote every CSV with `utf-8-sig` (BOM), and SpacesEDU's BOM-strict, case-sensitive header check read the first header as `﻿School Number` ≠ `School Number`, so the file wasn't recognized and column mapping broke (mis-validating the date column). Fix: a `DataLoader._NO_BOM_ENTITIES = {"StudentAttendance"}` allowlist — those entities are written **plain UTF-8** (no BOM); all other CSVs keep the `utf-8-sig` BOM so districts can still open them in Excel without mojibake. The rostering CSVs were unaffected because they reach SpacesEDU via the zip/OneRoster path, which tolerates the BOM; only the standalone attendance feed is strict. Regression test asserts StudentAttendance has no BOM and a rostering entity still does. (CRLF line endings kept — RFC-4180 standard; revisit only if SpacesEDU rejects them next.)
- **2026-06-19 — Plan 0004: StudentAttendance output reduced from 28 columns to the 4 required SpacesEDU fields.** The SpacesEDU docx explicitly permits dropping every optional field after Student Number ("Only required fields are needed in the file, all other fields can be removed if preferred"), so `StudentAttendance.csv` now emits ONLY the 4 populated columns in exact case-sensitive order: `School Number, Absence Date, Absence Category, Student Number`. Dropped the 24 always-blank optional columns. `OUTPUT_COLUMNS`/`POPULATED_COLUMNS` consolidated to one constant; `_frame()` no longer reindexes to a 28-col superset with `fill_value=""` (rows already carry exactly the 4 keys). The base `myedbc` `StudentAttendance.field_map` was reduced to the 4 keys (field_map is used ONLY for output column ORDER here — the transformer builds rows directly and never calls `apply_field_map`, so the values stay placeholders). All band logic preserved (role-based resolution, fail-loud derivation, no `drop_duplicates`, blank-code/blank-student drops, `format_dd_mmm_yyyy`). SD51 row count unchanged (79,303); SD74 snapshot byte-identical (not enabled).
- **2026-06-19 — Plan 0004: StudentAttendance band selection moved to per-district `source_files` (role-based, order-independent).** The base `myedbc` entity previously forced BOTH bands by declaring `daily_absences` + `period_absences` in `StudentAttendance.source_files`; deep-merge can't *remove* an inherited `source_files` key, so a single-band district couldn't run only daily or only period. **Fix:** the base now declares **no** `source_files` for StudentAttendance (empty `{}`; it never enables the entity, so it never runs there) and keeps only the shared defaults all districts inherit — the two headerless `headers:` blocks, the 28-col `field_map`, and `global_config.attendance` with both `daily` and `period` sub-blocks. Each **district** declares the role(s) for the band(s) it runs (`daily_absences` K-7 and/or `period_absences` 8-12); SD51 declares both (daily first → stays the pipeline primary). The transformer now resolves **each band by its ROLE** from `context.raw_data` (via `get_source_file`) instead of treating the positional primary `df` as the daily frame — so a **period-only** district (whose period file is primary) is no longer mis-processed as K-7 daily. Each band is optional/graceful and each band's config block is required **only when that band's data is present** (`attendance.daily` gated on the daily frame, `attendance.period` on the period frame), so a single-band district need not configure the other. **SD51 output byte-identical** (it declares both, daily first → `get_source_file(daily_absences)` returns the same frame the pipeline passed as `df`); **SD74 snapshot byte-identical** (not enabled). `sd51attendance` (attendance-only tier, `_base: sd51myedbc`) inherits both bands. No Pydantic change (`EntityConfig.source_files` already accepts an empty dict).
- **2026-06-17 — claugentic-dev-harness installed alongside the project's pre-existing in-house harness.** DistrictSync already maintains its own `docs/WORKFLOW.md`, `docs/ENGINEERING_STANDARDS.md`, `docs/DECISIONS.md`, `docs/ROADMAP.md`, `docs/ARCHITECTURE_TREE.md`, `.claude/agents/` roles, and a `scripts/check_architecture_tree.py` gate. The claugentic harness installs under the `claugentic-` namespace (its own tree + a second tree-gate hook, per the adopter's init choice) so the two coexist without collision. The harness **composes with** the project's existing CI gates (ruff / mypy / bandit / pytest 80% / SD74 snapshot / config-validation), it does not replace them. _(init harvest of `docs/WORKFLOW.md`.)_
- **2026-06-17 — Load-bearing project invariants the harness must respect** (harvested from `docs/WORKFLOW.md` + `docs/DECISIONS.md`, recorded so claugentic agents don't violate them): (1) **Config-driven columns** — GDE source column names come from the district `field_map`, never hardcoded in transformers; (2) **Zero-orphan invariant** — enrollment/class rows are filtered to `context.active_student_ids` so nothing references a student absent from `Students.csv`; (3) **`_base` config inheritance** — all 7 entity definitions live in base `myedbc_mapping.yaml`; configs select via `enabled_entities`, never redefine entities. See the project's own `docs/DECISIONS.md` for full detail.

- **2026-06-19 — Plan 0004 Slice 3: StudentAttendance 8-12 Student Period Absences band (per-period pass-through).** Added the 8-12 band as **per-period PASS-THROUGH** unioned with the existing K-7 daily band into the single 28-column `StudentAttendance.csv`. **One output row per period-absence row — no AM/PM collapse, no Period Id, no derivation** — because SpacesEDU aggregates per-day itself via per-entry weights (8-12 absence = 0.25/entry → 4 entries = 1 day, capped at 1/day); the standard rotating-block timetable export has no AM/PM signal anyway, so collapsing would be guesswork. **Category passed through AS-IS** (the GDE `Absence Category` already holds the final codes `A`/`A-E`/`L`/`AD`/`AL`/`OffSite`/`ISS`…; SpacesEDU ignores non-accepted ones, so no filtering). Rows with a blank category OR blank student number are dropped; **no dedup** (per-period multiplicity is intentional). Source = the **standard headerless** `StudentPeriodAbsences.txt` (MyEd Data Elements §2.45, 17 cols) — NOT the `Enhanced` variant the original plan assumed (Enhanced is headed + adds a Period Id the pass-through model doesn't need; standard is consistent with the headerless `StudentDailyAbsences.txt`). `daily_absences` stays FIRST in `source_files` so it remains the pipeline's primary (skip-on-empty-primary guard). New `global_config.attendance.period` block (4 source column names, no derivation knobs); reuses the open-dict `attendance` model from Slice 2 (no Pydantic change). Both bands optional/graceful (daily empty → period only; period absent → daily only). SD51 inherits the base source filenames unchanged (no per-district override). **SD74 snapshot byte-identical**; K-7 daily behavior unchanged. This supersedes the original Slice-3 plan (Enhanced-file half-day collapse), which was parked pending an Enhanced re-export.
- **2026-06-18 — Plan 0004 Slice 2: StudentAttendance entity scaffold + K-7 Daily band (SD51).** New opt-in `StudentAttendanceTransformer` emits the 28-column SpacesEDU `StudentAttendance.csv` (4 columns populated, 24 blank, **no `drop_duplicates`** — a full-day absence is intentionally two identical rows). **Derivation is fully config-resident** (Configurable-Columns + fail-loud): the K-7 `(Absent Code, Authorized) → Absence Category` map, the `Portion Absent → row-count` rule, and every source column name live in a new `global_config.attendance.daily` block read at runtime — so SpacesEDU's review tunes config, never code. An unmapped non-blank `(code, authorized)` pair **raises** rather than silently dropping/mis-bucketing. `attendance` was added to the `GlobalConfig` Pydantic model as an **open dict** (not a nested model) and threaded through `to_raw_dict()` so it reaches the transformer and so Slice 3's 8-12 Enhanced-Period keys need no model change. New `format_dd_mmm_yyyy` transform (added to `ALLOWED_TRANSFORMS`) produces the contract's `DD-MMM-YYYY` shape (mirrors `normalize_iso_date`). SD51 enables it via a **full** `enabled_entities` list (base-5 rostering + StudentAttendance) because deep-merge **replaces** lists. Quality report gets an explicit `StudentAttendance: []` skip (duplicates legitimate). **SD74 snapshot byte-identical** (not enabled for SD74); no other district affected. The 8-12 Enhanced Period Absences band is deferred to Slice 3 (pending SD51's `StudentPeriodAbsencesEnhanced.txt` re-export).

---

## Project history (harvested from the in-house harness, 2026-06-17)

## 2026-06-15 — Windows scheduled-task registration via Task Scheduler XML

> **SUPERSEDED-BY-0009 (2026-06-25).** The `schtasks /Create /XML` + `<LogonType>Password</LogonType>` + `/RU /RP` credential handoff below regressed: it fails `ERROR: Access is denied` on the unattended (password) path even when elevated — the very "Verified LIVE … no iteration needed" claim that broke. Registration is now PowerShell `Register-ScheduledTask` (see the 2026-06-25 entry at the top). The `/TR` 261-char insight and the no-catch-up `StartWhenAvailable=false` rationale below still hold and are preserved in the new mechanism; the `schtasks /XML` + `/RP` mechanism is dead. `delete_task`/`query_task` remain on `schtasks.exe`. Retained for history.

- **Register via `schtasks /Create /XML`, not inline `/TR`.** `schtasks` caps the `/TR` value at 261 chars; the source-mode command (`cmd /c "cd /d "<repo>" && "<python>" -m src.main --sis … --input "…" --output "…" --sftp"`) hit 458 chars on this dev box and failed. XML has no length cap, sets `<WorkingDirectory>` natively (kills the brittle nested-quote `cmd /c "cd /d …"` wrapper), and carries the schedule/principal/run-level. Public API of `register_task`/`query_task`/`delete_task`/`current_run_as_user` unchanged; validation-before-OS-call and `/RP` password redaction preserved.
- **Password stays out of the XML; run-level moves into it.** The run-as password is still passed via `schtasks /RP` on the command line (never written to the temp XML); `/RU /RP /XML` is a valid documented combination. The old `/RL HIGHEST` flag is replaced by `<RunLevel>HighestAvailable</RunLevel>` (password mode) / `LeastPrivilege` (no-password → `<LogonType>InteractiveToken</LogonType>`, matching the prior logged-on-only default).
- **XML written as UTF-16-with-BOM** (`encoding="utf-16"` + `<?xml … encoding="UTF-16"?>`) — `schtasks /Create /XML` accepted it first try on Windows 11 (no iteration needed); UTF-16 chosen for cross-version compatibility. Temp file is always removed in a `finally`.
- **Hand-built XML string with `xml.sax.saxutils.escape` on every interpolated value** (paths/user/args) rather than ElementTree — deterministic, testable output with explicit escaping at the boundary (validate-at-boundary). Daily trigger uses a fixed past `StartBoundary` date (`2024-01-01`) so the emitted XML is deterministic and never triggers a catch-up run (`<StartWhenAvailable>false</StartWhenAvailable>`).
- **Verified LIVE on this Windows 11 box** end-to-end (register→query→delete) with a deliberately long input/output path: old `/TR` would have been 458 chars (>261) and failed; XML registered `SUCCESS`, queried `Ready` with the full command line intact, deleted cleanly.
## 2026-06-19 — PreReg included by default (reverts v3.2.0 PreReg-excluded default)

- **PreReg included by default.** Default `active_values = ["Active", "PreReg"]`, matching the SpacesEDU / myBlueprint+ Advanced CSV spec's expected `EnrollStatus` values (exactly `Active` and `PreReg`). The v3.2.0 default (`["Active"]` only — see the superseded 2026-06-05 entry below) was a breaking change that silently dropped pre-registered students from Students/Classes/Enrollments. Reverted because the spec lists `PreReg` as an expected, includable status.
- **Fix lives in code, not per-district mapping.** Change is to `BaseTransformer.DEFAULT_ACTIVE_VALUES` (`src/etl/transformers/base.py`), so every config gets the corrected default without editing each `config/mappings/*.yaml`.
- **Districts can still opt out or extend.** A district can EXCLUDE PreReg again, or ADD statuses (e.g. `"Active No Primary"`), via `EnrollStatus.active_values` in its config — the per-config override is the single point of customization.
- **Withdraw-date logic unchanged.** Status still wins when present (active iff in `active_values`; withdraw date ignored); only rows with *no* status value fall back to the withdraw date (past/unparseable → Inactive). `"Active No Primary"` and other non-listed statuses remain Inactive by default.

## 2026-06-05 — Plan 0003 verification + import model (full-snapshot, upsert)

- **Verified end-to-end on real SD48 GDE data** (after rebasing onto the post-mbponly/extractor main): roster 7,922 → 7,543 (−379 = 50 inactive-without-withdraw-date leak + 329 PreReg, all verifiably non-active); orphaned student enrollments 405 → 0 on a full `--sis sd48myedbc` run (not just fixtures), stable under the rewritten extractor. SD74 snapshot byte-identical.
- **GDEs are full snapshots, not deltas.** Of 6,278 students in the SD48 schedule, all but 1 are present in the demographic extract — so the orphans were withdrawn/inactive students *in* the extract (removed by the active rule), not active students missing from a delta. The 1 exception (`2644905` — in the schedule, absent from the demographic) is a source-data gap → ROADMAP.
- **SpacesEDU import is upsert/unenroll-style** (`docs/partner/faq.md`): it preserves existing classes, adds new enrollments, and *unenrolls* students who drop out of `Enrollments.csv`. So dropping a withdrawn student's enrollment cleanly **unenrolls** them — which is *why* the zero-orphan filter is correct (the intended outcome), not merely "avoids import errors".

## 2026-06-05 — Zero-orphan enrollments (plan 0003, Slice 2)

- **Zero-orphan invariant via a published active roster.** `StudentTransformer` publishes its filtered output's `User ID` set to `context.active_student_ids` (so `active_student_ids == set(Students.User ID)` by construction); `classes.py` (homeroom) and `enrollments.py` (homeroom + subject) then filter their *student* rows to that set. Result: no enrollment or homeroom class references a student absent from `Students.csv`. Verified on an SD48-shaped fixture: 3 orphans (2 homeroom + 1 subject) → 0. The single filter helper is `BaseTransformer.filter_to_active(df, student_col, context, caller)` — both consumers call it (DRY); chosen over re-deriving "is active" at each read site (would drift from the roster). Demographic `Student Number` and schedule `Student ID` share the pupil-number value space; both sides normalized with `astype(str).str.strip()`.
- **Students-only filter; teacher/co-teacher rows untouched.** Only the student-row derivations are filtered; teacher rows in `_homeroom_enrollments`/`_subject_enrollments` derive from the *unfiltered* frames, and `_blended_teacher_enrollments`/`_classinfo_coteacher_enrollments` are not touched — so teacher rows are byte-identical to before (regression-tested by holding the homeroom class set constant and comparing a mixed roster vs an all-active control). The one intended teacher-row change: a homeroom with *only* inactive students is dropped (no empty homeroom class) along with its teacher enrollment — the no-empty-homeroom goal, not a leak.
- **Never filter-to-empty (guard).** When `active_student_ids` is empty (Students disabled / ran later, e.g. `mbp_core`) or the student column is absent, `filter_to_active` logs a WARNING and returns the frame unchanged rather than dropping every row. `filter_to_active` returns a `.copy()` so callers can mutate columns without a `SettingWithCopyWarning` (matches the other `filter_*` helpers).
- **SD74 snapshot byte-identical.** SD74 has no inactive students, so its active set = all students → the filter is a no-op; the frozen snapshot stays green (the regression guard).

## 2026-06-05 — Active-student predicate (plan 0003, Slice 1)

- **Config-driven active predicate (single source of truth).** Active-student detection now lives in `BaseTransformer` (`resolve_active_config` / `compute_enroll_status` / `is_active_mask`), resolving the status / withdraw-date columns and `active_values` from the Students `EnrollStatus` config (Configurable Columns). `students.py` delegates to it. Replaces the hardcoded-column logic in `students.py._determine_enrollment_status`. (Slice 2 will reuse the same mask to drop orphan enrollments.)
- **Corrected status-column alias.** The default status column resolves from the alias `["enrollment status", "enrolment status"]` — first present in the (normalized) frame. Real MyEd BC exports use the two-L `"Enrollment status"`; the repo fixtures / SD40's injected headers use the one-L `"Enrolment Status"`. The old code only matched one-L, so real two-L data silently fell through to withdraw-date-only detection (the SD48 −50 leak). A district can override `status_column` explicitly.
- **Status wins; withdraw date is the fallback.** When a row has a status value, the status decides (active iff in `active_values`) and the withdraw date is **ignored** — so a re-enrolled student (status `Active` with a lingering past withdraw date) stays Active. Only rows with *no* status value fall back to the withdraw date (past/unparseable → Inactive + one aggregated warning; 4 formats `%d-%b-%Y`, `%Y-%m-%d`, `%m/%d/%Y`, `%d/%m/%Y`). This matches the original status-XOR-date intent — corrected from a briefly-shipped "withdraw-date hard override" that wrongly dropped re-enrolled students (SD48: 3 such, roster 7,543 → 7,548).
- **PreReg excluded by default.** (SUPERSEDED — see 2026-06-19.) Default `active_values = ["Active"]`, matching `docs/partner/faq.md` ("Students with status PreReg … are excluded"). The active mask is `label != "Inactive"` (no implicit union with `"Active"`), so a district can opt PreReg (or any status) back in via `EnrollStatus.active_values` — e.g. for upcoming-year rosters. (An earlier draft of this plan retained PreReg; corrected after confirming the documented partner behavior against the SD48 data.)
- **`EnrollStatus` config fails loudly.** New strict `FieldEnrollStatus` (`extra="forbid"`) validates the dict form; a recognizable EnrollStatus override carrying an unknown/typo'd key now RAISES at config load (was a silent warn-passthrough). Bare `EnrollStatus: null` stays the auto-detect sentinel. SD48 roster correction: −50 verifiably-inactive students (Withdrawn/Inactive/Graduate without a withdraw date). SD74 snapshot stays byte-identical (its demographic has no status column + all-blank withdraw dates → date-only back-compat).
- **Deferred (ROADMAP):** `classify_field` is not idempotent — re-validating an `EntityConfig` that already holds typed field values (e.g. nested in a freshly-constructed `MappingConfig`) stringifies them. Pre-existing; out of scope for this slice. The real `load_config` path (raw dict → validation) is unaffected.

## 2026-06-05 — Plan 0002 Slice 4 (unattended SFTP + schedule hardening)

- **Scheduled task runs as the interactive setup user with stored Windows password** (`/RU /RP /RL HIGHEST`) so it survives reboots; SYSTEM rejected — SYSTEM cannot read the per-user OS keyring where the SFTP password is stored.
- **SFTP secrets stay in the per-user OS keyring; the task's run-as account must equal the setup account** — the Setup Wizard enforces this by resolving `current_run_as_user()` and registering the task as that account.
- **A requested SFTP upload that fails exits code 3** (ETL output CSVs still written), so delivery failures surface visibly in Task Scheduler's "Last Run Result" and in Run History, not silently as a green exit 0.

## 2026-06-04 — Harness / standards

- **Output targeting via `enabled_entities`.** All 7 entity definitions live in base `myedbc_mapping.yaml`; configs select which to emit via `global_config.enabled_entities`. Tiers `mbp_all` (7) and `mbp_core` (Students + 2 course CSVs) are thin overrides. Rationale: defines entity templates once (DRY) and lets a per-district myBlueprint+ config (`_base: <district>` + `enabled_entities`) inherit both the district's columns and the base entity defs. Chosen over "the new config carries the new sections" (would duplicate defs and hit YAML's single-`_base` limit). Ratified after PR #12 review.
- **`ARCHITECTURE_TREE.md` + presence enforcement.** Added `docs/ARCHITECTURE_TREE.md` (one line per source file). `scripts/check_architecture_tree.py` is a deterministic gate (no LLM) checking presence + staleness for `src/**/*.py` and `config/mappings/*.yaml`; descriptions are authored by humans/agents. Intended wiring: `make check-tree` + CI (authoritative) + optional local pre-commit.
- **Configurable-columns standard reaffirmed.** GDE source columns must be resolved from `field_map`, never hardcoded in transformer code. `student_courses.py` (PR #12) violates this (hardcodes ~10 columns, bypasses `field_map` for input) — logged as debt to migrate.
- **Line-ending hygiene (proposed).** Working tree shows widespread CRLF↔LF churn; recommend a committed `.gitattributes` (`* text=auto eol=lf`) + one normalization commit to stop the noise.
- **Adopted the staged agent Development Workflow** (`docs/WORKFLOW.md`): triage → discuss → plan (`.claude/plans/`) → adversarial plan-review → spec → user approval → implement (isolated) → verify → land/archive → retrospect. Roles are a *library* in `.claude/agents/` the orchestrator selects from per task (starter: `plan-reviewer`, `implementer-architect`). Hard rule: slice work into units one specialist finishes in a single ≤1M-context session, landing complete with **no new tech debt**. Stage 9 feeds learnings back into the harness (living-harness principle). Requires un-ignoring `.claude/agents/` + `.claude/plans/` so the role library and plans are shared (see `.gitignore`).
- **Architecture-improvement program → ROADMAP.** The full-codebase architect review (6-agent pass) findings are captured as a tiered backlog in `docs/ROADMAP.md`; each item runs through the Development Workflow in its own session rather than one large change.
- **Role library = local, version-controlled, shared; NO runtime injection.** Agent roles live in `.claude/agents/` (committed, grow over time, shared via the repo / packageable as a plugin later). We do **not** fetch role/skill text from online marketplaces at runtime and inject it into subagents — that's a prompt-injection / supply-chain / non-reproducibility risk. Marketplaces (incl. third-party aggregators like skills.sh / skillsmp.com) are **authoring-time discovery only**: review → vendor a pinned copy → commit. Prefer Anthropic first-party sources; treat aggregators as untrusted.
- **ARCHITECTURE_TREE enforced by hooks.** `.claude/settings.json` wires `scripts/check_architecture_tree.py --hook` as a `PostToolUse(Write)` nudge (prompts the agent to document a newly-created in-scope file, with a one-line description) + a `Stop` backstop (blocks finishing until the index is current). Script = deterministic presence/staleness gate; description authored by the agent that made the file.

## 2026-05 — PR #12 (myBlueprint+ tier)

- **Homeroom-enrollments fix.** `_homeroom_enrollments` now resolves the demographic student-ID column from the **Students** `User ID` mapping, not the schedule-oriented Enrollments `student_id_col`. Fixes silently-dropped homeroom enrollments for configs where the schedule and demographic files name the ID column differently (`myedbc`/`sd48`/`sd51`). No-op for `sd40`/`sd74` (they already override to `Student Number`).
- **Date of Birth → ISO.** Students DOB normalized to `yyyy-mm-dd` (was `dd-MMM-yyyy`) to match Classes dates. Pass-through for already-ISO / unrecognized formats (never blanks valid data).
- **`PreRegSchoolCode` → "Next school code"** (was "Previous school number"). ⚠️ PENDING: verify the real GDE column is named exactly `Next school code`, else it silently blanks for all districts. Currently untested.
