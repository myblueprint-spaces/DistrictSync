# Development + CI dependencies
# streamlit is pinned >=1.54,<1.55 in requirements.txt (single source) and is
# pulled in here via -r — the ui-marked smoke test depends on that pinned reach
# (private Runtime._session_mgr.num_active_sessions()). See DECISIONS.md.
-r requirements.txt

# Testing
pytest>=7.0
pytest-cov>=4.0
pytest-benchmark>=4.0
hypothesis>=6.100
pytest-playwright>=0.5
pytest-sftpserver>=1.3
requests>=2.28

# Linting / formatting / type checking
ruff>=0.15
# Type-check tooling - latest, uncapped. The project targets a single Python (3.13;
# see requires-python + ci.yml). Do not cap to old versions.
mypy>=1.0
pandas-stubs>=2.0
types-paramiko>=3.0
types-PyYAML>=6.0

# Security scanning (used in CI)
bandit>=1.7

# Documentation
mkdocs-material>=9.0
mkdocs-with-pdf>=0.9
