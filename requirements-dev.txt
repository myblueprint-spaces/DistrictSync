# Development + CI dependencies
-r requirements.txt

# Testing
pytest>=7.0
pytest-cov>=4.0
pytest-benchmark>=4.0
hypothesis>=6.100
pytest-playwright>=0.5
pytest-sftpserver>=1.3
requests>=2.28

# Linting / formatting / type checking
ruff>=0.15
# Pin type-check tooling to versions compatible with python_version=3.9 (pyproject [tool.mypy]).
# CI installs unpinned-latest, which broke the type-check step on 2026-06-22 (main was green
# 2026-06-19): mypy 1.15+ dropped 3.9 as a target ("must be 3.10 or higher"), and numpy 2.4+
# ships PEP 695 `type` stubs mypy rejects under a <3.12 target. These caps restore the known-good
# combo (mypy 1.14.x + numpy 2.2.x); the failure is in numpy's stub, not our code.
mypy>=1.0,<1.15
numpy<2.3
pandas-stubs>=2.0
types-paramiko>=3.0
types-PyYAML>=6.0

# Security scanning (used in CI)
bandit>=1.7

# Documentation
mkdocs-material>=9.0
mkdocs-with-pdf>=0.9
