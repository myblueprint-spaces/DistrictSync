# Development + CI dependencies
-r requirements.txt

# Testing
pytest>=7.0
pytest-cov>=4.0
pytest-benchmark>=4.0
hypothesis>=6.100
pytest-playwright>=0.5
pytest-sftpserver>=1.3
requests>=2.28

# Linting / formatting / type checking
ruff>=0.15
# Type-check tooling - latest, uncapped. The project targets a single Python (3.13;
# see requires-python + ci.yml). Do not cap to old versions.
mypy>=1.0
pandas-stubs>=2.0
types-paramiko>=3.0
types-PyYAML>=6.0

# Security scanning (used in CI)
bandit>=1.7

# Flet build/CI tooling (NOT shipped in the exe). flet-web pulls fastapi+uvicorn
# for headless screenshots; flet-cli provides `flet pack`. Pinned to match the
# runtime flet/flet-desktop pins in requirements.txt (test_flet_pin gates drift).
flet-web==0.85.3
flet-cli==0.85.3

# Documentation
mkdocs-material>=9.0
mkdocs-with-pdf>=0.9
